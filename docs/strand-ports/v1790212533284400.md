# Port record: Strand compile `v1790212533284400`

Handoff 32-A, Session 32, 24 September 2026. Written under ruling 597, outside every compile folder,
because a PR body dies with the thread and this is the record the next reader opens.

The compile is correction 25 (rulings 663 and 1102 with 1103), which supersedes correction 24
(`v1790160131064912`) and correction 23 (`v1790157541616309`). Neither of those two was committed
here; the last compile this tree ported from is correction 21's `v1789885868097915` (handoff 29-A),
so every "before" below is read from that bundle and every "after" from this one. Its bytes are at
`docs/strand/v1790212533284400/`, committed whole and never edited (ruling 907;
`docs/strand/README.md` carries the rules).

## 0. The compile, as read

The archive `_DNA_Design_System___4_.zip` measured 4,794,125 bytes at sha256
`e29ac79bfce079803d62ad6a9e19c7166b887ba2971cc0418a06840414cc948a`, equal to the handoff's
Attachments line 2.

The id was read from the artifact and then written down (873). The bundle's first line is
`/* @ds-compile-id: v1790212533284400 */` and the file assigns
`__DS_COMPILE_ID="v1790212533284400"`. Both agree. The archive-root `_ds_bundle.js` (308,140 bytes,
no id header) is not committed (899).

Content hash (995), produced from inside the committed directory with
`find . -type f -print0 | LC_ALL=C sort -z | xargs -0 sha256sum`: 69 lines, identical to Appendix A
of the handoff line for line (`diff` exits 0), aggregate
`7feb8cbbd7524b16ced7ac9db362e83a71f362b89a38a0397feaa8984c33ffb9`. `v1789720800167997` and
`v1789885868097915` are untouched.

Ruling 844 carries 618's lesson by name: for anything geometric the compile governs, and where the
port and the compile disagree about a size, a padding, a track or a floor the compile wins unless an
app ruling says otherwise. Ruling 862 has the port reconciled against the compile before anything
changes. Both are the rules the dispositions below are decided by, and every disagreement is
reported either way, including the ones the port kept.

Dispositions: **present** (already true of the port at `92fbdc3`), **ported** (changed in this PR),
**superseded** (a later item replaces it; the later item is named), **divergent** (kept against the
compile, with the app ruling that keeps it).

The reconciliation ran before anything changed (862): each part's port at `92fbdc3` against the
archive's `.jsx`, against this bundle's compiled function and against correction 21's, and each
finding put to an independent second read. Every part's `.jsx` and its compiled function are
byte-identical once both are compiled the same way (esbuild, whitespace and comments stripped), so
nowhere did the bundle have to overrule the source; where an extraction's prose and the bundle
disagree, the bundle governs and the line is in section 3.

---

## 1. Dispositions, part by part

### Which sections reach which part

| Section                                 | Pane    | PostCard | FacetRail | LensBar | Select  | Input  | Menu   |
| --------------------------------------- | ------- | -------- | --------- | ------- | ------- | ------ | ------ |
| 23 §0 the before-state                  | read    | read     | —         | —       | —       | —      | —      |
| 23 §1 stepping, keys, `selectedKey`     | ported  | —        | —         | —       | —       | —      | —      |
| 23 §2 `selected`                        | —       | ported   | —         | —       | —       | —      | —      |
| 23 §3 not touched                       | present | —        | present   | present | —       | —      | —      |
| 24 §0 the before-state                  | read    | —        | read      | —       | read    | —      | —      |
| 24 §1 G72 `headingAction`               | —       | —        | ported    | —       | —       | —      | —      |
| 24 §2 G75 wrapping options              | —       | —        | ported    | —       | —       | —      | —      |
| 24 §3 G76 per-axis `select`             | —       | —        | ported    | —       | —       | —      | —      |
| 24 §4 1060 ladders                      | —       | —        | ported    | —       | —       | —      | —      |
| 24 §5 1061 hint and error               | —       | —        | —         | —       | ported  | —      | —      |
| 24 §6 1064 `open`                       | ported  | —        | —         | —       | —       | —      | —      |
| 24 §7 not touched                       | present | present  | present   | present | —       | —      | —      |
| 25 §0 the before-state                  | —       | read     | read      | read    | —       | read   | read   |
| 25 §1 `presentation="discovery"`        | —       | ported   | —         | —       | —       | —      | —      |
| 25 §2 Menu, and `--z-menu`              | —       | —        | —         | —       | —       | —      | ported |
| 25 §3 displays, ladders, sticky heading | —       | —        | ported    | —       | —       | —      | —      |
| 25 §4 `trailing`                        | —       | —        | —         | ported  | —       | —      | —      |
| 25 §5 combobox, describedby             | —       | —        | —         | —       | —       | ported | —      |
| 25 §6 not touched                       | present | —        | —         | —       | present | —      | —      |
| 25 §7 the four part defects             | —       | ported   | —         | —       | —       | —      | ported |

"read" is a before-state table, confirmed against correction 21's bundle and the port at `92fbdc3`
(23 §0's rows are confirmed; 23 §1's claim about Escape and `defaultPrevented` is not, section 3 item
4). "—" is a section that names nothing in the part.

### Pane — `src/components/strand/Pane.tsx`

| Ref         | Item                                                                                                                    | Disposition                                                                                                                                                    |
| ----------- | ----------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 23 §1       | `onPrevious`, `onNext`, `previousLabel`, `nextLabel`, `hasPrevious`, `hasNext`, `selectedKey`                           | **ported**                                                                                                                                                     |
| 23 §1       | One cluster, Previous, Next, Close, `IconButton` chevron-left, chevron-right, x, `--space-1`                            | **ported** — the wrapper `div[data-pane-cluster]` renders for an `onClose`-only caller too; the close control's box, offset and name are unchanged (section 4) |
| 23 §1       | The cluster at the expanded pane's top right, and in the route form's header row                                        | **ported**                                                                                                                                                     |
| 23 §1       | Edge stops: `aria-disabled`, not `disabled`; opacity 0.45, cursor default; click and key inert; undefined reads as true | **ported**                                                                                                                                                     |
| 23 §1       | ArrowLeft and ArrowRight on the section's own keydown, guarded by an editable target and by `defaultPrevented`          | **ported**                                                                                                                                                     |
| 23 §1, 1084 | Escape yields to `defaultPrevented`                                                                                     | present — the port and correction 21 both had it (section 3 item 4); G86 is why 1084's outcome is still not met here                                           |
| 23 §1       | `selectedKey` re-runs the bring-into-view against `[data-selected]`, one routine with 607's                             | **ported** — inert in this shell, G85                                                                                                                          |
| 23 §1       | Loading keeps the cluster                                                                                               | **ported**                                                                                                                                                     |
| 23 §1       | Touch: the pair at 44                                                                                                   | present — `IconButton`'s default                                                                                                                               |
| 24 §6       | `open`, default true; one grid, list first and section second in both states                                            | **ported**                                                                                                                                                     |
| 24 §6       | Closed: `minmax(0,1fr) 0px`, gap 0; section inert, `aria-hidden`, no body, no cluster                                   | **ported** — `inert={true}` where the compile writes React 18's `''`; React 19 renders the same empty attribute                                                |
| 24 §6       | The track snaps; the section's opacity moves at `--dur-slow`; visibility flips at the ends                              | **ported** — G87 item 5 on what a close actually shows                                                                                                         |
| 24 §6       | `data-pane-open` on the grid, `data-pane-list` on the column; `box-sizing: border-box`                                  | **ported** — markers added at the default (section 4)                                                                                                          |

### PostCard — `src/components/strand/PostCard.tsx`

| Ref        | Item                                                                                             | Disposition                                                                                                                                                                                                                                  |
| ---------- | ------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 23 §2      | `selected`: a 2px `--ink` ring 2px outside the frame on a `--bg` gap                             | **ported**, on both faces                                                                                                                                                                                                                    |
| 23 §2      | `data-selected` and `aria-current="true"`; absent, unchanged                                     | **ported**                                                                                                                                                                                                                                   |
| 23 §2      | The 1.5px identity frame stays on selected and unselected cards                                  | present                                                                                                                                                                                                                                      |
| 23 §2      | The compile's `box-shadow` transition, on every article                                          | **ported** — not prop-gated as compiled, and nothing moves at rest (section 4)                                                                                                                                                               |
| 25 §1      | `presentation: "feed" \| "discovery"`, default feed; the feed face is the previous body          | **ported** — `PostCard` dispatches before any hook; the feed face is the app's body at `92fbdc3` plus 23 §2                                                                                                                                  |
| 25 §1      | The discovery face: fixed size at `--lane-card-width`, `flex: none`                              | **ported**                                                                                                                                                                                                                                   |
| 25 §1      | 16:9 media, `object-fit: cover`, the C glyph on `--bg-sunken` when there is none                 | **ported**                                                                                                                                                                                                                                   |
| 25 §1      | Title in the display face, two lines held and clamped                                            | **ported**                                                                                                                                                                                                                                   |
| 25 §1      | When; where; presenter (24 Avatar) and topic (a C Chip) on one row                               | **ported**                                                                                                                                                                                                                                   |
| 25 §1      | The reason row, two lines held, empty and `aria-hidden` without a reason                         | **ported**                                                                                                                                                                                                                                   |
| 25 §1      | One control: the ellipsis, `--surface` and a 1px `--line`, 44 touch and 36 pointer, opening Menu | **ported** — its label appends `": " + title` only when the title is a string, because this port's title may be a node                                                                                                                       |
| 25 §1      | No going row, no Report, no price, badge, count or number                                        | **ported**                                                                                                                                                                                                                                   |
| 25 §1      | The face opens on `onOpen`; a control does not; `onPreload` once on pointer enter or focus       | **ported**                                                                                                                                                                                                                                   |
| 25 §1      | Pointer hover underlines the title in `--line-strong`; the frame stays the C colour              | **ported**                                                                                                                                                                                                                                   |
| 25 §1      | `input` overrides the detected mode; presenter row 44 touch and 32 pointer                       | **ported** — the mode is `useMode()` from `src/lib/tier.ts`, 29-A's precedent (its item 53)                                                                                                                                                  |
| 25 §1      | 23's ring applies to the discovery face                                                          | **ported**                                                                                                                                                                                                                                   |
| 25 §1, G42 | The system frame                                                                                 | **divergent** — `var(--c-system)` on both faces where the compile writes `var(--line-strong)`; G42 is why, and `--c-system` holds `--line-strong`, so nothing computes differently. New on the discovery face, pre-existing on the feed face |
| 25 §7      | Faces shrank in a flex lane; fixed with `flex: none`                                             | **ported**                                                                                                                                                                                                                                   |
| 25 §7      | Title decoration in longhands only, so a re-render never mixes them with the shorthand           | **ported**                                                                                                                                                                                                                                   |

### FacetRail — `src/components/strand/FacetRail.tsx`

| Ref         | Item                                                                                                                                                     | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 24 §1 (G72) | `headingAction`: the heading a row, `h2` at flex 1, the slot at flex none, the row 44 and pulled back by `--space-2` when a control is passed            | **ported**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 24 §2 (G75) | No `nowrap`; `var(--text-s-lh)`, `text-align: left`, `overflow-wrap: anywhere`, `max-width: 100%`; padding `--space-2 --space-4`; `min-height` 36        | **ported** — not prop-gated; a one-line chip is 39.75, not 36 (section 3 item 1); closes G75                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 24 §3 (G76) | `select: "single"` on an axis: a radiogroup of radios, roving tab, choosing replaces; Clear all clears it                                                | **ported** — the rail-level `select` stays the strip's                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 24 §4       | `ladders`: rungs in ladder order, sub-labels only with more than one ladder, never numbered, rungs select as options                                     | **ported**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 25 §3       | `display`: `chips` (default), `segment` led by an Any seat, `checklist` in tokens, `combobox` through Input's combobox with chosen chips under it        | **ported** — Segment and Input need nothing more; ComboAxis is module-private                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 25 §3       | An axis with ladders is the ladder part whatever `display` says; `data-display="ladders"`                                                                | **ported** (section 3 item 3 on "as a radiogroup")                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 25 §3       | The rail its own scroller (`overflow-y: auto`, `overscroll-behavior: contain`); the heading sticky on `--surface` with a `--line` hairline once scrolled | **ported** — not prop-gated; at rest the axes sit 1px lower (section 3 item 2)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 25 §3       | `data-axis-id`, `data-display`, `data-select` on each axis; `data-option` on each chip                                                                   | **ported** — markers added at the default                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 25 §3       | Sheet at compact: unchanged, every display renders in it                                                                                                 | **divergent** in the Sheet it renders through — ruling 618 (`variant="sheet"` for `tier="compact"`), 480 (the part draws `[data-sheet-heading]`) and G30 (this Sheet has no pinned header, scrolling body or Close). The part now also draws the scrolling body, under a heading row that does not scroll, as this repository's other Sheet callers with a scroller do: 24 §2's taller chips pushed the Home axis past the sheet, 34px of it at 430 where none was before, and deeper at 360 and 390, where it already sat past the sheet at `92fbdc3` with nothing to scroll (194 and 71px, now 235 and 104). With the body it is reachable at all three; nothing moves at rest |

### LensBar — `src/components/strand/LensBar.tsx`

| Ref   | Item                                                                                                   | Disposition                                                                                                                             |
| ----- | ------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------- |
| 25 §4 | `trailing`: one seat on the track's row, outside the tablist, never a tab                              | **ported** — no row is drawn when it is absent, so the tablist stays the wrapper's own child (`tests/matrix.cjs:4427` reads its parent) |
| 25 §4 | `TRAIL_GAP` 8, in `metrics`                                                                            | **ported**                                                                                                                              |
| 25 §4 | The fit test prices the column less the seat and the gap; re-measures when the seat resizes or appears | **ported**                                                                                                                              |
| 25 §4 | Stretched tracks take what the seat leaves; `content` tracks leave the seat at the far end             | **ported**                                                                                                                              |
| 25 §4 | The caller supplies the control at the 44 floor; absent, the bar renders as before                     | present                                                                                                                                 |

### Select — `src/components/strand/Select.tsx`

| Ref         | Item                                                                                     | Disposition                                                                                                                                                                                                                         |
| ----------- | ---------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 24 §5       | `hint`, a 13px line in `--ink-3` under the control                                       | **ported**                                                                                                                                                                                                                          |
| 24 §5       | `error`: the line and the border in the error rung, `aria-invalid`, error replacing hint | **ported** — `error` and `hint` typed as words, as the compile's own types have them (548's boolean is Input's, and no Select carried one); `--error` under ruling 425 where the compile writes `--danger`, which is `var(--error)` |
| 24 §5       | `aria-describedby` tying the line to the control                                         | **ported**                                                                                                                                                                                                                          |
| 24 §5, 1085 | The focus rendering copied from Input: `--surface` ground, `--ink` border                | **ported** on every Select — ratified by Chat as the intended change (section 4)                                                                                                                                                    |

### Input — `src/components/strand/Input.tsx`

| Ref   | Item                                                                                                                                                    | Disposition                                                                                                                                                                                                                                 |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 25 §5 | `suggestions` makes the single-line field a WAI-ARIA 1.2 combobox: role, `aria-autocomplete`, `aria-expanded`, `aria-controls`, `aria-activedescendant` | **ported**                                                                                                                                                                                                                                  |
| 25 §5 | ArrowDown opens and moves, ArrowUp moves, Enter chooses, Escape closes with `preventDefault`; a press on an option does not blur                        | **ported**                                                                                                                                                                                                                                  |
| 25 §5 | `noMatch` as the list's only line, not an option                                                                                                        | **ported**                                                                                                                                                                                                                                  |
| 25 §5 | `listbox: "overlay" \| "inline"`; options floor at 44 on every mode; a `detail` line                                                                    | **ported**                                                                                                                                                                                                                                  |
| 25 §5 | `icon`, a leading glyph                                                                                                                                 | **ported**                                                                                                                                                                                                                                  |
| 25 §5 | Hint, error, focus rendering and every non-combobox call unchanged                                                                                      | **divergent in structure, under Chat's ruling on 25 §5** — the compile's two wrappers render only for the combobox (`suggestions` on a single-line field) or an `icon`; drawn on every call they move every textarea 7.5px (section 4, G83) |
| 25 §5 | The line tied by `aria-describedby`                                                                                                                     | **ported** — keyed on the line 548 renders (words or a hint), so a boolean error with no hint is described by nothing                                                                                                                       |

### Menu — `src/components/strand/Menu.tsx`, new

| Ref         | Item                                                                                                                                                 | Disposition                                                                                                                  |
| ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| 25 §2       | Items and rules; an item that does not apply is absent; no disabled item; rules never lead, trail or double                                          | **ported**                                                                                                                   |
| 25 §2       | `tone: "danger"`                                                                                                                                     | **ported** — `--error` under ruling 425                                                                                      |
| 25 §2       | WAI-ARIA menu: roving focus, wrapping arrows, Home and End, Enter and Space, Escape and Tab close; focus back to the anchor; Escape prevents default | **ported**                                                                                                                   |
| 25 §2       | Portal into `document.body`, fixed, bottom-end, flipped, clamped, repositioned on scroll and resize; `portal={false}` in place                       | **ported** — `createPortal` from `react-dom` where the compile reaches `window.ReactDOM`; G88 on a Menu inside a modal Sheet |
| 25 §2       | Items 44 touch and 36 pointer; `input` overrides                                                                                                     | **ported** — `useMode()` again                                                                                               |
| 25 §2, 1103 | `--z-menu`, never `--z-notification`                                                                                                                 | **ported** — `zIndex: var(--z-menu)`, the part's only z-index                                                                |
| 25 §7       | Focus moves once placed, never while `visibility: hidden`                                                                                            | **ported**                                                                                                                   |
| 25 §7       | Pointer items at 36, padding on the sides only                                                                                                       | **ported**                                                                                                                   |

---

## 2. Every port-against-compile disagreement, and how it was decided

New disagreements are the corrections'; the rest were in the port at `92fbdc3` and are carried
forward with the ruling that keeps them. Geometric means a size, a padding, a track or a floor (844).

| Part      | Disagreement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Geometric         | Decided                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Pane      | `inert: ''` in the compile, `inert={true}` here                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | no                | same attribute under React 19                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Pane      | `error` typed `string \| null`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | no                | kept, 29-A's prop table                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| PostCard  | System frame `--c-system` and label `--c-system-ink` against `--line-strong` and `--ink-3`                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | no                | kept, G42                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| PostCard  | App-only `data-c`, `data-expanded`, `data-kicker`, footer `data-testid`s and `aria-pressed`, `data-read-more` and `data-show-less` with `aria-expanded` false and true on Read more and Show less, the handle line, and an unused hover state on the article (vestigial since the B2 port)                                                                                                                                                                                                                                                                        | no                | kept, rulings 105 and 416, W54; the harness reads them; the hover state renders nothing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| PostCard  | The body clamps only while unexpanded; the body tap only while clamped; Read more as a link with hover intent; Show less                                                                                                                                                                                                                                                                                                                                                                                                                                          | clamp only        | kept, rulings 84 and 105                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| PostCard  | The discovery face carries neither `data-c` nor `data-expanded`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | no                | ported as compiled; 32-B's Discovery arm reads both today and will need them or a new reading                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| PostCard  | The Menu's label appends only a string title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | no                | a node title would stringify                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| PostCard  | `useMode()` for the compile's `useInputMode()` on the discovery face                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | a frame, on touch | kept, 29-A item 53: `useMode()` answers `pointer` on its first render, on the server and on the client, where the compile reads the query in its initialiser. A discovery face on touch first renders its ellipsis at 36 and its presenter row at 32, and `useMode()`'s passive effect corrects them to 44; the face has no layout effect that corrects them before paint, as Menu's placing effect does, so it can paint once at 36 and 32. No page binds the face at this head                                                                                                     |
| FacetRail | Compact Sheet: `variant`, `label`, the part's own heading and now its own scrolling body, no Close                                                                                                                                                                                                                                                                                                                                                                                                                                                                | heading row       | kept, 618, 480, G30; the body is drawn so 24 §2's chips leave every axis reachable                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| LensBar   | Descriptor 12 below the track, inside the collapsing box, against the compile's 6 gap; its size was 15 against the compile's 13                                                                                                                                                                                                                                                                                                                                                                                                                                   | **yes**           | decided under 844 by Chat (G82, closed): the size takes the compile's 13; the spacing stays 12, which both ruling-62 contracts give and 405's collapsing box holds                                                                                                                                                                                                                                                                                                                                                                                                                   |
| LensBar   | Track `--radius-m` and seat `--radius-badge` against 999; no hairline; `--shadow-1` for the chip                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | no                | kept, LENS_BAR_SPEC; 29-A item 36 for the hairline                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| LensBar   | Two probe spans, the 700 active weight, `labels` boolean, the C brand rung, the active tap toggling the descriptor and never calling `onChange` (the compile calls it on every press), the latched descriptor, the folded `aria-label`, `data-lens`, the focus ring, reduced-motion transitions, disabled seats                                                                                                                                                                                                                                                   | no                | kept, 29-A items 5, 6, 22, 52, 53, rulings 102, 405 and LENS_BAR_SPEC line 22; G49 on the ring                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| LensBar   | Carried from `92fbdc3` and listed in neither record until now: the label wrapped in a `span`; `className="strand-lens"` and `data-lens-bar` on the wrapper where the compile renders `data-lensbar-compact`; the wrapper's `min-width: 0`; the tablist's `position: relative` and `align-items: center` and the seat's `position: relative; z-index: 1`, left from the measured chip ruling 488 retired; the seat transitioning colour as well as background; `lenses` required with no default; `document.fonts` guarded as a whole rather than method by method | min-width only    | kept: nothing reads the attributes the compile draws, and at every call site the parents are block containers (AppHeader passes `min-width: 0` itself), so no box moves                                                                                                                                                                                                                                                                                                                                                                                                              |
| Select    | `--error` for `--danger`; `useId()` unconditional; `label` a node; a caller's `onFocus`, `onBlur`, `aria-invalid` or `aria-describedby` replaces the part's, as the compile draws it (Input chains its handlers)                                                                                                                                                                                                                                                                                                                                                  | no                | kept, 425, rules of hooks, ConveneForm's labels; no caller passes any of the four                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Input     | `--error` for `--danger`; 548's boolean; `useId()` unconditional; `label` and `hint` nodes, as at `92fbdc3`; `noMatch` a string, as the compile types it                                                                                                                                                                                                                                                                                                                                                                                                          | no                | kept, 425, 548                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Input     | The compile's wrappers on every call                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **yes**           | Chat's ruling on 25 §5 after the measurement: gated on the combobox (`suggestions` on a single-line field) or `icon`, G83                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Input     | The listbox named from `label` only when `label` is a string                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | no                | a node label would stringify; G87 item 7 on the unnamed case                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Input     | Enter chooses only when `opts[active]` exists; the compile calls `choose(opts[active])` unconditionally                                                                                                                                                                                                                                                                                                                                                                                                                                                           | no                | kept for `noUncheckedIndexedAccess`: the index is reset by an effect keyed on the options' values, so an Enter reaches the guard only if the list shrank under the active index before that effect ran, where the compile hands `onSuggestionSelect` `undefined` and closes and this leaves the list open with nothing chosen                                                                                                                                                                                                                                                        |
| Menu      | `useMode()` for the compile's `useInputMode()`; `createPortal` import                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | no                | 29-A item 53; module form. `useMode()` answers `pointer` on its first render where the compile's hook reads the query in its initialiser, so a Menu mounted open on touch first commits at 36 while hidden; `mode` is a dependency of the placing effect, so it is placed again at 44 in the same task, before it is first painted (measured in Chromium). One consequence the compile does not share: on touch, a Menu mounted open inside its own anchor is placed and focused once `useMode()` answers, where the compile's stays hidden (G87 item 16). No caller mounts one open |
| Menu      | The outside press closes through the current `close`, read from a ref, and the listener re-attaches when `anchorRef` changes; the compile's listener keeps the `close` of the render that opened it                                                                                                                                                                                                                                                                                                                                                               | no                | kept: a caller whose `onClose` changes while the menu is open is closed by its current handler rather than a stale one; no caller at this head                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Menu      | `--error` for `--danger` on the danger tone's label and icon                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | no                | kept, 425; `--danger` is `var(--error)`, so nothing computes differently, and Discovery does not use the tone (1097)                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

---

## 3. Where an extraction's prose and the compiled bundle disagree (the bundle governs)

1. 24 §2: "`minHeight: 36` still holds the one-line height". A one-line chip is 39.75.
2. 25 §3: "Net geometry unchanged at rest". The pin's transparent 1px bottom border moves the axes 1px.
3. 25 §3: a ladders axis "renders `data-display="ladders"` as a radiogroup". Only with `select: "single"`.
4. 23 §1 and doctrine flag 3: Escape "did not" honour `defaultPrevented` before. Correction 21's bundle
   (line 2257) and this port at `92fbdc3` already did.
5. 23 §1: "with neither handler supplied the part renders byte-for-byte as correction 21 compiled it".
   The cluster wrapper renders for an `onClose` caller, and 24 §6 adds markers at the default.
6. 24 §1: with `headingAction` "the label stays where it was". The `h2` moves 0.25, but the row is 44
   with an 8px pull-back, so the axes move 8.5 (69.5 to 78). Prop-gated; 32-B's control will do it.
7. 24 §2: "Cultural, heritage and religious" wraps to three lines at the 240 rail and two at 1082's 280. With this app's self-hosted Alegreya Sans in Chromium it wraps to two at 240 and not at all at
   260, which is this app's expanded rail; 1082's 280 is nowhere in the tree. At `92fbdc3` that chip
   already sat inside the 240 nav's box, 17.84 into its right padding, rather than overflowing it.
8. 24 §5 says Select copies `aria-describedby` from "the form Input had"; 25 §5 says Input's line "is
   now tied by `aria-describedby`". Correction 21's Input had no such tie. 25 §5 is the one the
   bundle bears out: Input's tie is ported as 25 §5's, and Select takes it under 24 §5.

Items 1 to 4, 6 and 7 are G87 items 1 to 4 and 8, which with the part quirks the reconciliation found
are G87 for Strand's next correction brief. Items 5 and 8 are recorded here only: 5 is this app's
default render (section 4), and 8 is settled by the bundle.

---

## 4. What changes with no prop passed

Handoff 32-A item 2 says every new prop defaults to today's rendering, and every new prop does. These
changes are not props, and each is decided:

- **FacetRail** (844, no app ruling keeping the old values; G75 asked for it): chips wrap and are 39.75
  one-line; the rail is its own scroller with its heading pinned, 1px lower at rest; new markers on
  axes and chips; and at compact the Sheet's body scrolls, so the axes the taller chips push down stay
  reachable. Read on `/convene` by the mount arms at 390, 820 and 1280: no chip past the content box
  it wraps in (the nav, and at 390 the Sheet's scrolling body), whose `scrollWidth` equals its
  `clientWidth`.
- **Select** (Chat, 1085): the focus rendering on every Select, a `border-color` transition, and
  `aria-invalid="false"`, the ARIA default. The mount arm on `/where` reads the ground and edge at
  rest, focused and blurred.
- **Input** (Chat, on 25 §5): the structure is 92fbdc3's for every call that is neither the combobox (`suggestions` on a single-line field) nor passes
  `icon`. Measured on 48 fields across nine callers at 390 and 1280, before and after: identical once
  gated; with the wrappers every textarea moved 7.5px and every single-line field held (G83). The line
  is now tied by `aria-describedby`, and focus handlers chain rather than replace (no caller passes one).
- **Pane**: the close control sits inside `div[data-pane-cluster]`; the grid and column carry
  `data-pane-open="true"` and `data-pane-list`; the section carries `box-sizing`, `opacity: 1`,
  `visibility: visible` and a transition. The close control's box, offset, name, Escape and click are
  unchanged, and so is every computed value at open.
- **PostCard**: a `box-shadow` transition on every article, over a shadow that is `none` at rest.
- **LensBar** (844, Chat on G82): the scope line is 13 italic where it was 15, on Feed, Connect and
  Discovery alike, still 12 below the track; a one-line descriptor is 30.2 tall where it was 33, so
  what sits under an open descriptor starts 2.8 higher.

The committed mount arms were run against a `92fbdc3` build as a control, in Chromium: 201 of 233
checks pass there, and all 233 pass on the ported build. The 32 that fail there are exactly the ones
that read the list above: FacetRail's markers, its pin and scroller and the compact Sheet's body;
Pane's markers and the list they mark; Select's focus rendering; and the Input line tied by
`aria-describedby`, on every Input that draws one. LensBar's scope line is not among them because no
arm reads its size. So the prop defaults hold as they were, and the non-prop changes are the ones
listed.

---

## 5. The token (1103)

`--z-menu: 62` is in `src/styles/strand.css` between `--z-sheet` 60 and `--z-dialog` 65, with the
compile's comment. Against correction 21's `tokens/spacing.css` this is the only line the compile's
copy changes, and `base.css`, `colors.css`, `fonts.css`, `typography.css` and `styles.css` are
byte-identical. `Menu.tsx` is the only reader. The token check reads 154 declared, every cited token
resolving in both themes.

---

## 6. The asset diff, both ways (994)

`docs/strand/v1790212533284400/assets/` against `public/strand/`, rerun. This is the corrected account
of handoff 32-A's paragraph, per Chat's ruling on it:

- **Compile only, not copied:** `icons/file-text.svg` (no part in the archive uses it) and
  `imagery/hero-professional.jpeg`, which correction 21's compile carried too and nothing in `src` or
  `public` references. The handoff's paragraph did not name the second.
- **App only, kept (additive under 994):** `icons/at-sign.svg`, `eye-off.svg`, `eye.svg`, `house.svg`;
  and `fonts/` (the self-hosted Alegreya Sans and Bodoni Moda files and their two OFL texts) and
  `marks/google.svg`, `marks/linkedin.svg`, which the handoff's paragraph did not name.
- **Differs:** `logo.png`; the app's wordmark is its own (CLAUDE.md's asset table) and stays.
- **Byte-identical:** the other 49, among them every glyph the ported parts use: `chevron-left`,
  `chevron-right`, `ellipsis`, `panel-left-close` and `x`, which the handoff named, and `chevron-down`
  (Select), `check` and `map-pin` (FacetRail's checklist and combobox), which it did not; every adinkra
  and every pattern.

---

## 7. The pages that bind a changed part (755, 556, 627)

Item 5's table was read from imports; the tree renders less and more than it said (555), and Chat
ruled on the two misses. The set the mount arms prove, in `tests/mount.cjs`, one cell per tier
(compact 390 light, medium 820 dark, expanded 1280 light), each arm read after the gate its route's own
suite already waits on:

| Route                                                 | What it binds, read on the live view                                                                                                                                                  | Arm                                             |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| `/feed`                                               | LensBar in the column; the header's LensBar once scrolled at compact and medium, none at expanded; PostCard                                                                           | `mount-feed`                                    |
| `/posts/$id`                                          | In place: the open PostCard and the column's LensBar; direct: the PostCard and no LensBar                                                                                             | `mount-post`                                    |
| every shell route, the composer                       | ComposerShell is one mount, in `src/routes/_shell.tsx`; ConveneForm's Inputs and Select, the link Input and the preview PostCard, read from `/feed` and `/connect` (Chat's ruling)    | `mount-composer-feed`, `mount-composer-connect` |
| `/convene`, `/convene/$lens`                          | LensBar (in content at compact, the layout's top above it), FacetRail (Browse Sheet at compact, rail above it), the corpus's PostCards; the header's LensBar once scrolled at compact | `mount-convene`, `mount-lens`                   |
| `/convene/events/$id`, expanded                       | The Pane, the collapsed 64 FacetRail, LensBar, the list's PostCards — **missing from the table** (Chat's ruling)                                                                      | `mount-event`                                   |
| `/connect`                                            | LensBar, the filter Selects (aside at expanded, sheet below it), IntroSheet's Input                                                                                                   | `mount-connect`                                 |
| `/m/$handle`                                          | ProfileSurface's and StanceBlock's Selects and Inputs in edit mode; the visitor's IntroSheet Input                                                                                    | `mount-profile`                                 |
| `/welcome`, `/where`                                  | OnboardingSurface's Inputs, and the country Select with its focus rendering                                                                                                           | `mount-welcome`, `mount-where`                  |
| `/relationship`                                       | No Select and no Input — the table said it bound both through StanceBlock, which renders only on the profile                                                                          | `mount-relationship`                            |
| `/e/$slug`                                            | GuestSheet's Input                                                                                                                                                                    | `mount-guest`                                   |
| `/sign-in`, `/reset`                                  | Input                                                                                                                                                                                 | `mount-sign-in`, `mount-reset`                  |
| `/connect`, `/m/$handle`, `/password`, `/collaborate` | No header LensBar once scrolled: AppHeader renders one only where a surface registers it (Feed; Discovery at compact) and never at expanded                                           | `mount-shell`                                   |

Also read in the tree: ConnectSurface imports Input and renders none (G91); the composer's main field
is a raw textarea, so its Strand Inputs are the verbs' fields and the link; `/password` renders
PasswordField, not Input.

46 arms per engine, 187 checks, declared in `tests/expected-counts.json` from a local Chromium
calibration (`EXPECT=write`, `SPECIAL=mount`), raised by one on `mount-post`'s three cells when the
direct form's absent LensBar came to be read, and mirrored to the WebKit keys, which the arms reach by
the same path with no engine branch; the enforcing WebKit job reads them. `scripts/deployment-serves.sh`
now also polls `/feed`, `/posts/seed-1`, `/m/thandiwe-dube` and `/convene/online`, which these arms
open by document load and which the shell, profile and Discovery suites already opened unlisted.

---

## 8. What the handoff read, where the tree disagreed (555)

1. **Item 5's first row.** "Every `_shell` route: LensBar, through `AppHeader`" holds for the import
   only. `AppShell` hands the header a lens bar only when the tier is not expanded, the centre column
   has scrolled past 72 and a surface has registered one, and only FeedSurface and DiscoverySurface
   (at compact) register one.
2. **Item 5's misses and false premises.** `/convene/events/$id` at expanded binds four changed parts
   and is not in the table. The composer mounts once on every shell route, not on `/feed` and
   `/posts/$id` alone. A direct `/posts/$id` renders no LensBar. ConnectSurface renders no Input.
   StanceBlock does not render on the onboarding screens, and `/relationship` renders neither part.
3. **Item 2's "every new prop defaults to today's behaviour ... the extractions state this for every
   item".** Every new prop does; six parts change the default render with no prop passed (section 4),
   and Chat ruled on the two the handoff's own table names and on LensBar's (G82).
4. **Item 2's table, PostCard: "whose feed face stays byte-for-byte the current body".** True of
   correction 25 against correction 24's compile. Against this app it is not: correction 23 added
   `selected` and an unconditional transition to that body, which the app had not ported, and the app's
   body carries twelve divergences of its own, all kept (section 2).
5. **The asset paragraph** named neither `imagery/hero-professional.jpeg` nor the app's `fonts/` and
   `marks/` (section 6).
6. **The grep in "What Chat read".** At `92fbdc3` none of `headingAction`, `ladders`, `presentation`,
   `data-selected`, `onPrevious`, `selectedKey`, `trailing`, `suggestions` or `aria-describedby`
   appears in the six ports. Confirmed.

The local readings in this record (the 48-field measurement, the mount arms' calibration and the
control run against `92fbdc3`) are Chromium against `wrangler pages dev` of each build. They are what
the change was validated against before it was pushed, and they are not the proof: ruling 916 makes
the readings CI's, and every exit criterion is read on the branch's deployed preview.
