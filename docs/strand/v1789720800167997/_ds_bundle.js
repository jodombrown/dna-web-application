/* @ds-compile-id: v1789720800167997 */
/* Strand (DNA design system) bundle export, session 25, correction 17 compile.
   Compile id: v1789720800167997. Namespace: StrandDNADesignSystem_3654dd.
   Supersedes the v1789619870985180 export. Everything below this block is the compiled bundle byte for byte.
   The two comment blocks above and the single __DS_COMPILE_ID assignment below are the only additions (854, 861).
   Static check:  the first line of this file holds the marker @ds-compile-id followed by v1789720800167997
   Runtime check: window.__DS_COMPILE_ID equals the string v1789720800167997
   Both must equal the compile id your page's literals name, or the folder and the prose disagree. */
(function(){try{var g=typeof globalThis!=='undefined'?globalThis:window;g.__DS_COMPILE_ID="v1789720800167997";g.__DS_NAMESPACE='StrandDNADesignSystem_3654dd';}catch(e){}})();
/* @ds-bundle: {"format":4,"namespace":"StrandDNADesignSystem_3654dd","components":[{"name":"Avatar","sourcePath":"components/core/Avatar.jsx"},{"name":"Button","sourcePath":"components/core/Button.jsx"},{"name":"Checkbox","sourcePath":"components/core/Checkbox.jsx"},{"name":"Chip","sourcePath":"components/core/Chip.jsx"},{"name":"Dialog","sourcePath":"components/core/Dialog.jsx"},{"name":"Icon","sourcePath":"components/core/Icon.jsx"},{"name":"IconButton","sourcePath":"components/core/IconButton.jsx"},{"name":"Input","sourcePath":"components/core/Input.jsx"},{"name":"Segment","sourcePath":"components/core/Segment.jsx"},{"name":"Select","sourcePath":"components/core/Select.jsx"},{"name":"Sheet","sourcePath":"components/core/Sheet.jsx"},{"name":"Switch","sourcePath":"components/core/Switch.jsx"},{"name":"Tabs","sourcePath":"components/core/Tabs.jsx"},{"name":"Toast","sourcePath":"components/core/Toast.jsx"},{"name":"Tooltip","sourcePath":"components/core/Tooltip.jsx"},{"name":"ActButton","sourcePath":"components/dna/ActButton.jsx"},{"name":"AppHeader","sourcePath":"components/dna/AppHeader.jsx"},{"name":"AudienceSelect","sourcePath":"components/dna/AudienceSelect.jsx"},{"name":"AUDIENCE_LABEL","sourcePath":"components/dna/AudienceSelect.jsx"},{"name":"CBadge","sourcePath":"components/dna/CBadge.jsx"},{"name":"VERB_SCHEMA","sourcePath":"components/dna/Composer.jsx"},{"name":"Composer","sourcePath":"components/dna/Composer.jsx"},{"name":"DiaLine","sourcePath":"components/dna/DiaLine.jsx"},{"name":"EmptyState","sourcePath":"components/dna/EmptyState.jsx"},{"name":"FacetRail","sourcePath":"components/dna/FacetRail.jsx"},{"name":"LensBar","sourcePath":"components/dna/LensBar.jsx"},{"name":"MediaBlock","sourcePath":"components/dna/MediaBlock.jsx"},{"name":"NotificationBell","sourcePath":"components/dna/NotificationBell.jsx"},{"name":"KIND_C","sourcePath":"components/dna/NotificationListItem.jsx"},{"name":"NotificationListItem","sourcePath":"components/dna/NotificationListItem.jsx"},{"name":"Pane","sourcePath":"components/dna/Pane.jsx"},{"name":"PostCard","sourcePath":"components/dna/PostCard.jsx"},{"name":"PulseDock","sourcePath":"components/dna/PulseDock.jsx"},{"name":"RailWidget","sourcePath":"components/dna/RailWidget.jsx"},{"name":"TierBadge","sourcePath":"components/dna/TierBadge.jsx"},{"name":"VERB_ACT","sourcePath":"components/dna/VerbChip.jsx"},{"name":"VerbChip","sourcePath":"components/dna/VerbChip.jsx"},{"name":"C_LABEL","sourcePath":"components/dna/cmeta.js"},{"name":"C_GLYPH","sourcePath":"components/dna/cmeta.js"},{"name":"C_ORDER","sourcePath":"components/dna/cmeta.js"}],"sourceHashes":{"components/core/Avatar.jsx":"0e6a23f92f22","components/core/Button.jsx":"d7eb419e2c37","components/core/Checkbox.jsx":"8512a3a8ebea","components/core/Chip.jsx":"98281a4adba3","components/core/Dialog.jsx":"8bcb1a0e243e","components/core/Icon.jsx":"b962d0245853","components/core/IconButton.jsx":"c9a120862c79","components/core/Input.jsx":"92302b4b5273","components/core/Segment.jsx":"c77f4a162501","components/core/Select.jsx":"7f960de8dc1d","components/core/Sheet.jsx":"f0d65cece32d","components/core/Switch.jsx":"9ef096029f60","components/core/Tabs.jsx":"873b1982d613","components/core/Toast.jsx":"c640a1b3fb0c","components/core/Tooltip.jsx":"7ba6da61983d","components/dna/ActButton.jsx":"9d4534e607ca","components/dna/AppHeader.jsx":"ec2b3da716c0","components/dna/AudienceSelect.jsx":"10fb07b9a3f3","components/dna/CBadge.jsx":"d8f579b2026f","components/dna/Composer.jsx":"dfff9f0f7d90","components/dna/DiaLine.jsx":"cd957a06e471","components/dna/EmptyState.jsx":"03a25132068c","components/dna/FacetRail.jsx":"af680e948c9b","components/dna/LensBar.jsx":"b68ae3817dd5","components/dna/MediaBlock.jsx":"8899470d4a65","components/dna/NotificationBell.jsx":"b34e38d4c623","components/dna/NotificationListItem.jsx":"0072b8f3b570","components/dna/Pane.jsx":"1a24816dd5fd","components/dna/PostCard.jsx":"8557920e8093","components/dna/PulseDock.jsx":"d9874b4fcc85","components/dna/RailWidget.jsx":"d8592d7f881c","components/dna/TierBadge.jsx":"bd26f3eb50e3","components/dna/VerbChip.jsx":"5907a0d4facb","components/dna/cmeta.js":"8d648e7ba561","ui_kits/dna/app.jsx":"c3ed406a320b","ui_kits/dna/collaborate.jsx":"860893c04fe9","ui_kits/dna/composer/dia-mock.js":"621404db474f","ui_kits/dna/composer/proto.jsx":"0e56969bce4c","ui_kits/dna/connect.jsx":"2da52d08591f","ui_kits/dna/contribute.jsx":"e15d29b8adb6","ui_kits/dna/convene.jsx":"c1e1d458abb5","ui_kits/dna/convene/shared.jsx":"eead76201513","ui_kits/dna/convey.jsx":"a63b4416b822","ui_kits/dna/feed.jsx":"23744bf55ab8","ui_kits/dna/inbox.jsx":"ab4a75447f32","ui_kits/dna/shell.jsx":"4d9a9630ec54"},"inlinedExternals":[],"unexposedExports":[{"name":"assetBase","sourcePath":"components/dna/cmeta.js"}]} */

(() => {

const __ds_ns = (window.StrandDNADesignSystem_3654dd = window.StrandDNADesignSystem_3654dd || {});

const __ds_scope = {};

(__ds_ns.__errors = __ds_ns.__errors || []);

// components/core/Avatar.jsx
try { (() => {
/** Member avatar. Photo when present, initials otherwise. Never a placeholder silhouette, never stacked. */
function Avatar({
  name = '',
  src,
  size = 40,
  style
}) {
  const initials = name.split(/\s+/).filter(Boolean).slice(0, 2).map(s => s[0].toUpperCase()).join('');
  const r = size >= 64 ? 14 : size >= 40 ? 10 : 8;
  return src ? /*#__PURE__*/React.createElement("img", {
    src: src,
    alt: name,
    width: size,
    height: size,
    style: {
      width: size,
      height: size,
      borderRadius: r,
      objectFit: 'cover',
      flex: 'none',
      display: 'block',
      ...style
    }
  }) : /*#__PURE__*/React.createElement("span", {
    "aria-label": name,
    style: {
      width: size,
      height: size,
      borderRadius: r,
      flex: 'none',
      background: 'var(--bg-sunken)',
      color: 'var(--ink-2)',
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      fontFamily: 'var(--font-sans)',
      fontWeight: 700,
      fontSize: Math.round(size * 0.4),
      letterSpacing: '0.02em',
      ...style
    }
  }, initials);
}
Object.assign(__ds_scope, { Avatar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Avatar.jsx", error: String((e && e.message) || e) }); }

// components/core/Button.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cVar = c => c ? 'var(--c-' + c + ')' : 'var(--ink)';
const cText = c => c ? 'var(--c-' + c + '-text)' : 'var(--ink)';
const cInk = c => c ? 'var(--c-' + c + '-ink)' : 'var(--on-fill)';
/** Button. variant: primary | secondary | ghost | danger. c colors the button in a C (rule 3: target C, not card C). */
function Button({
  variant = 'primary',
  c,
  size = 'md',
  disabled,
  full,
  children,
  style,
  ...rest
}) {
  const color = variant === 'danger' ? 'var(--danger)' : cVar(c);
  const text = variant === 'danger' ? 'var(--danger)' : cText(c);
  const onFill = variant === 'danger' ? 'var(--on-fill)' : cInk(c);
  const base = {
    fontFamily: 'var(--font-sans)',
    fontSize: size === 'sm' ? 15 : 17,
    fontWeight: 500,
    lineHeight: 1,
    minHeight: size === 'sm' ? 36 : 44,
    padding: size === 'sm' ? '0 14px' : '0 18px',
    borderRadius: 'var(--radius-m)',
    border: '1px solid transparent',
    cursor: disabled ? 'default' : 'pointer',
    display: 'inline-flex',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 8,
    width: full ? '100%' : undefined,
    opacity: disabled ? 0.45 : 1,
    transition: 'filter var(--dur-default) var(--ease), background var(--dur-default) var(--ease)',
    whiteSpace: 'nowrap'
  };
  const v = {
    primary: {
      background: color,
      color: onFill
    },
    secondary: {
      background: 'transparent',
      color: text,
      borderColor: c || variant === 'danger' ? color : 'var(--line-strong)'
    },
    ghost: {
      background: 'transparent',
      color: text
    },
    danger: {
      background: color,
      color: 'var(--on-fill)'
    }
  }[variant];
  const [hover, setHover] = React.useState(false),
    [down, setDown] = React.useState(false);
  const fx = disabled ? {} : variant === 'primary' || variant === 'danger' ? {
    filter: down ? 'brightness(0.86)' : hover ? 'brightness(0.92)' : 'none'
  } : {
    background: down ? 'var(--line)' : hover ? 'var(--bg-sunken)' : 'transparent'
  };
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button",
    disabled: disabled,
    style: {
      ...base,
      ...v,
      ...fx,
      ...style
    },
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => {
      setHover(false);
      setDown(false);
    },
    onMouseDown: () => setDown(true),
    onMouseUp: () => setDown(false)
  }, rest), children);
}
Object.assign(__ds_scope, { Button });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Button.jsx", error: String((e && e.message) || e) }); }

// components/core/Icon.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Resolve the design system's assets/ folder from wherever _ds_bundle.js was loaded. */
function iconBase() {
  if (typeof window !== 'undefined' && window.STRAND_ASSET_BASE) return window.STRAND_ASSET_BASE + 'icons/';
  if (typeof document !== 'undefined') {
    const s = document.querySelector('script[src*="_ds_bundle"]');
    if (s) return s.src.replace(/_ds_bundle[^/]*$/, 'assets/icons/');
  }
  return 'assets/icons/';
}
/** Lucide-style UI icon (1.5px stroke) from assets/icons, rendered as a currentColor mask. */
function Icon({
  name,
  size = 20,
  style,
  ...rest
}) {
  const m = 'url(' + iconBase() + name + '.svg) center / contain no-repeat';
  return /*#__PURE__*/React.createElement("span", _extends({
    "aria-hidden": "true",
    style: {
      display: 'inline-block',
      width: size,
      height: size,
      flex: 'none',
      background: 'currentColor',
      WebkitMask: m,
      mask: m,
      ...style
    }
  }, rest));
}
Object.assign(__ds_scope, { Icon });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Icon.jsx", error: String((e && e.message) || e) }); }

// components/core/Checkbox.jsx
try { (() => {
/** Checkbox with label. Controlled via checked/onChange. */
function Checkbox({
  label,
  checked,
  onChange,
  c,
  disabled,
  style
}) {
  const color = c ? 'var(--c-' + c + ')' : 'var(--ink)';
  return /*#__PURE__*/React.createElement("label", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 10,
      minHeight: 44,
      cursor: disabled ? 'default' : 'pointer',
      opacity: disabled ? 0.45 : 1,
      fontSize: 17,
      ...style
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: !!checked,
    disabled: disabled,
    onChange: e => onChange && onChange(e.target.checked),
    style: {
      position: 'absolute',
      opacity: 0,
      width: 0,
      height: 0
    }
  }), /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      width: 22,
      height: 22,
      borderRadius: 6,
      border: '1.5px solid ' + (checked ? color : 'var(--line-strong)'),
      background: checked ? color : 'var(--surface)',
      color: '#fff',
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      transition: 'background var(--dur-default) var(--ease)'
    }
  }, checked && /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "check",
    size: 14
  })), label);
}
Object.assign(__ds_scope, { Checkbox });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Checkbox.jsx", error: String((e && e.message) || e) }); }

// components/core/Chip.jsx
try { (() => {
/** Chip for filters and tags. c tints it in a C. selected fills it. onRemove adds an x. */
function Chip({
  children,
  c,
  selected,
  onRemove,
  onClick,
  style
}) {
  const color = c ? 'var(--c-' + c + ')' : 'var(--ink)';
  const text = c ? 'var(--c-' + c + '-text)' : 'var(--ink)';
  const onFill = c ? 'var(--c-' + c + '-ink)' : 'var(--on-fill)';
  const tint = c ? 'var(--c-' + c + '-tint)' : 'var(--bg-sunken)';
  const Tag = onClick ? 'button' : 'span';
  return /*#__PURE__*/React.createElement(Tag, {
    type: onClick ? 'button' : undefined,
    onClick: onClick,
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6,
      height: 32,
      padding: '0 12px',
      borderRadius: 999,
      fontFamily: 'var(--font-sans)',
      fontSize: 15,
      fontWeight: 500,
      lineHeight: 1,
      cursor: onClick ? 'pointer' : 'default',
      background: selected ? color : tint,
      color: selected ? onFill : text,
      border: '1px solid ' + (selected ? color : 'transparent'),
      ...style
    }
  }, children, onRemove && /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Remove",
    onClick: e => {
      e.stopPropagation();
      onRemove();
    },
    style: {
      all: 'unset',
      display: 'inline-flex',
      cursor: 'pointer',
      marginRight: -4
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "x",
    size: 14
  })));
}
Object.assign(__ds_scope, { Chip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Chip.jsx", error: String((e && e.message) || e) }); }

// components/core/IconButton.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** 44px square icon button. label is required for accessibility. */
function IconButton({
  name,
  label,
  c,
  active,
  size = 44,
  style,
  ...rest
}) {
  const [hover, setHover] = React.useState(false);
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button",
    "aria-label": label,
    title: label,
    style: {
      width: size,
      height: size,
      borderRadius: 'var(--radius-m)',
      border: '1px solid transparent',
      background: hover ? 'var(--bg-sunken)' : 'transparent',
      color: active ? c ? 'var(--c-' + c + ')' : 'var(--ink)' : 'var(--ink-2)',
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      cursor: 'pointer',
      transition: 'background var(--dur-default) var(--ease)',
      ...style
    },
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false)
  }, rest), /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: name,
    size: size >= 44 ? 22 : 18
  }));
}
Object.assign(__ds_scope, { IconButton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/IconButton.jsx", error: String((e && e.message) || e) }); }

// components/core/Input.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Text input or textarea with label, hint, error. */
function Input({
  label,
  hint,
  error,
  multiline,
  rows = 4,
  id,
  style,
  ...rest
}) {
  const uid = id || React.useId();
  const [focus, setFocus] = React.useState(false);
  const field = {
    width: '100%',
    boxSizing: 'border-box',
    fontFamily: 'var(--font-sans)',
    fontSize: 17,
    lineHeight: 1.5,
    color: 'var(--ink)',
    background: focus ? 'var(--surface)' : 'var(--bg-sunken)',
    border: '1px solid ' + (error ? 'var(--danger)' : focus ? 'var(--ink)' : 'var(--line)'),
    borderRadius: 'var(--radius-m)',
    padding: multiline ? '10px 14px' : '0 14px',
    minHeight: 44,
    outline: 'none',
    resize: 'vertical',
    transition: 'border-color var(--dur-default) var(--ease)'
  };
  const Tag = multiline ? 'textarea' : 'input';
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6,
      ...style
    }
  }, label && /*#__PURE__*/React.createElement("label", {
    htmlFor: uid,
    style: {
      fontSize: 15,
      fontWeight: 500,
      color: 'var(--ink-2)'
    }
  }, label), /*#__PURE__*/React.createElement(Tag, _extends({
    id: uid,
    rows: multiline ? rows : undefined,
    style: field,
    onFocus: () => setFocus(true),
    onBlur: () => setFocus(false),
    "aria-invalid": !!error
  }, rest)), (error || hint) && /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      lineHeight: 1.4,
      color: error ? 'var(--danger)' : 'var(--ink-3)'
    }
  }, error || hint));
}
Object.assign(__ds_scope, { Input });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Input.jsx", error: String((e && e.message) || e) }); }

// components/core/Segment.jsx
try { (() => {
/** Segment (673): the 44px pill radiogroup. Extracted from Composer's VERB_SCHEMA segment fields, which are its first caller; renders identically.
 *  Selected = --ink fill, --on-fill text; rest = 1px --line border, transparent. Disabled follows Button (0.45, aria-disabled, not-allowed), no tokens added.
 *  Options are strings or { value, label }. Nothing caller-specific: the caller owns labels, copy and what the choice means. */
function Segment({
  options = [],
  value,
  onChange,
  label,
  disabled,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    role: "radiogroup",
    "aria-label": label,
    "aria-disabled": disabled || undefined,
    style: {
      display: 'flex',
      gap: 8,
      flexWrap: 'wrap',
      opacity: disabled ? 0.45 : 1,
      ...style
    }
  }, options.map(o => {
    const val = typeof o === 'string' ? o : o.value,
      lab = typeof o === 'string' ? o : o.label;
    const on = value === val;
    return /*#__PURE__*/React.createElement("button", {
      key: val,
      type: "button",
      role: "radio",
      "aria-checked": on,
      "aria-disabled": disabled || undefined,
      onClick: disabled ? undefined : () => onChange && onChange(val),
      style: {
        all: 'unset',
        boxSizing: 'border-box',
        cursor: disabled ? 'not-allowed' : 'pointer',
        height: 44,
        padding: '0 16px',
        borderRadius: 999,
        fontFamily: 'var(--font-sans)',
        fontSize: 15,
        fontWeight: 500,
        color: on ? 'var(--on-fill)' : 'var(--ink)',
        background: on ? 'var(--ink)' : 'transparent',
        border: '1px solid ' + (on ? 'var(--ink)' : 'var(--line)')
      }
    }, lab);
  }));
}
Object.assign(__ds_scope, { Segment });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Segment.jsx", error: String((e && e.message) || e) }); }

// components/core/Select.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Native select styled to match Input. options: [{value,label}] */
function Select({
  label,
  options = [],
  id,
  style,
  ...rest
}) {
  const uid = id || React.useId();
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6,
      ...style
    }
  }, label && /*#__PURE__*/React.createElement("label", {
    htmlFor: uid,
    style: {
      fontSize: 15,
      fontWeight: 500,
      color: 'var(--ink-2)'
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative'
    }
  }, /*#__PURE__*/React.createElement("select", _extends({
    id: uid,
    style: {
      width: '100%',
      appearance: 'none',
      WebkitAppearance: 'none',
      fontFamily: 'var(--font-sans)',
      fontSize: 17,
      color: 'var(--ink)',
      background: 'var(--bg-sunken)',
      border: '1px solid var(--line)',
      borderRadius: 'var(--radius-m)',
      padding: '0 40px 0 14px',
      minHeight: 44,
      outline: 'none'
    }
  }, rest), options.map(o => /*#__PURE__*/React.createElement("option", {
    key: o.value,
    value: o.value
  }, o.label))), /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "chevron-down",
    size: 18,
    style: {
      position: 'absolute',
      right: 12,
      top: 13,
      color: 'var(--ink-3)',
      pointerEvents: 'none'
    }
  })));
}
Object.assign(__ds_scope, { Select });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Select.jsx", error: String((e && e.message) || e) }); }

// components/core/Sheet.jsx
try { (() => {
/** Sheet (561): holds one decision and returns the surface underneath unchanged. The surface underneath stays live.
 *  A sheet has no navigation of its own. Geometry per 584: compact bottom sheet at --sheet-compact-height;
 *  medium side-anchored at --sheet-medium-width; expanded centred at --sheet-expanded-width. Never full screen at any tier.
 *  size carries 492's two named exceptions (611): "composer" widens to --sheet-composer-ratio above compact and keeps the default
 *  height at compact, because the exception exists to give the stacked preview room and cannot make the composer shorter than an
 *  ordinary sheet. "notification" uses --sheet-notification-width and sits at --z-notification.
 *  Contract per 480: native <dialog>, focus trap, focus restore to the invoking control, aria-live for state changes inside.
 *  Motion per 604: --dur-slow. contained=true renders inside a position:relative parent for frames and specimens.
 *  636: title accepts a node (a consumer's own header row) and renders it without the display font when it is not a string.
 *  636: scroll lock while open. The host cannot scroll, wheel and touchmove on the scrim are cancelled, and wheel input inside
 *  the dialog is consumed by its nearest scrollable ancestor and never chains to the page, including at the edges. */
const GEO = {
  compact: {
    width: '100%',
    height: 'var(--sheet-compact-height)',
    inset: 'auto 0 0 0',
    margin: 0,
    radius: 'var(--sheet-radius) var(--sheet-radius) 0 0',
    anim: 'strand-sheet-rise'
  },
  medium: {
    width: 'var(--sheet-medium-width)',
    height: '100%',
    inset: '0 0 0 auto',
    margin: 0,
    radius: 0,
    anim: 'strand-sheet-slide'
  },
  expanded: {
    width: 'var(--sheet-expanded-width)',
    height: 'fit-content',
    maxHeight: 'var(--sheet-expanded-max-height)',
    inset: 0,
    margin: 'auto',
    radius: 'var(--sheet-radius)',
    anim: 'strand-sheet-rise'
  }
};
const FOCUSABLE = 'button:not([disabled]), [href], input:not([disabled]), select:not([disabled]), textarea:not([disabled]), [tabindex]:not([tabindex="-1"])';
function Sheet({
  open,
  onClose,
  tier = 'compact',
  size = 'default',
  label,
  title,
  footer,
  error,
  loading,
  phase = 'rest',
  contained,
  autoFocus = true,
  children,
  style
}) {
  const dlg = React.useRef(null),
    body = React.useRef(null),
    start = React.useRef(null);
  const [dy, setDy] = React.useState(0),
    [scrolled, setScrolled] = React.useState(false);
  const close = r => onClose && onClose(r);
  React.useEffect(() => {
    if (!open) return;
    const prev = document.activeElement;
    return () => {
      prev && prev.focus && prev.focus();
    };
  }, [open]);
  React.useEffect(() => {
    // 636: scroll lock
    const d = dlg.current;
    if (!open || !d) return;
    const host = contained ? d.parentElement : null;
    const targets = host ? [host] : [document.documentElement, document.body];
    const prevOverflow = targets.map(t => t.style.overflow);
    targets.forEach(t => {
      t.style.overflow = 'hidden';
    });
    const scrollableUp = (el, dy) => {
      for (let n = el; n && n !== d.parentElement; n = n.parentElement) {
        const cs = getComputedStyle(n);
        if (/(auto|scroll)/.test(cs.overflowY) && n.scrollHeight > n.clientHeight) {
          const atTop = n.scrollTop <= 0,
            atBottom = n.scrollTop + n.clientHeight >= n.scrollHeight - 1;
          if (!(dy < 0 && atTop || dy > 0 && atBottom)) return n;
        }
        if (n === d) break;
      }
      return null;
    };
    const onWheel = e => {
      const target = scrollableUp(e.target, e.deltaY);
      if (target) {
        target.scrollTop += e.deltaY;
      }
      e.preventDefault();
    };
    const onTouch = e => {
      if (!scrollableUp(e.target, 0) && !d.contains(e.target)) e.preventDefault();
    };
    const scrim = host ? host.querySelector('[data-sheet-scrim]') : null;
    const block = e => e.preventDefault();
    d.addEventListener('wheel', onWheel, {
      passive: false
    });
    if (scrim) {
      scrim.addEventListener('wheel', block, {
        passive: false
      });
      scrim.addEventListener('touchmove', block, {
        passive: false
      });
    }
    const onDocWheel = e => {
      if (!d.contains(e.target)) e.preventDefault();
    };
    if (!scrim) {
      document.addEventListener('touchmove', onTouch, {
        passive: false
      });
      document.addEventListener('wheel', onDocWheel, {
        passive: false
      });
    }
    return () => {
      targets.forEach((t, i) => {
        t.style.overflow = prevOverflow[i];
      });
      d.removeEventListener('wheel', onWheel);
      if (scrim) {
        scrim.removeEventListener('wheel', block);
        scrim.removeEventListener('touchmove', block);
      }
      document.removeEventListener('touchmove', onTouch);
      document.removeEventListener('wheel', onDocWheel);
    };
  }, [open, contained]);
  React.useEffect(() => {
    const d = dlg.current;
    if (!d) return;
    if (contained) {
      if (!d.open) d.show();
    } else if (!d.open) {
      try {
        d.showModal();
      } catch (e) {
        d.show();
      }
    }
    if (autoFocus) {
      const f = d.querySelector('[data-autofocus]') || d.querySelector(FOCUSABLE);
      f && f.focus();
    }
    const onCancel = e => {
      e.preventDefault();
      close('escape');
    };
    d.addEventListener('cancel', onCancel);
    return () => {
      d.removeEventListener('cancel', onCancel);
      try {
        d.close();
      } catch (e) {}
    };
  }, [contained]);
  if (!open) return null;
  const g = GEO[tier] || GEO.compact;
  const composer = size === 'composer',
    notification = size === 'notification';
  const width = composer && tier !== 'compact' ? 'var(--sheet-composer-ratio)' : notification && tier !== 'compact' ? 'var(--sheet-notification-width)' : g.width;
  const height = g.height; // 611: the composer exception is a width above compact; at compact the composer keeps --sheet-compact-height.
  const z = notification ? 'var(--z-notification)' : 'var(--z-sheet)';
  const onKeyDown = e => {
    if (contained && e.key === 'Escape') {
      e.preventDefault();
      close('escape');
      return;
    }
    if (e.key !== 'Tab' || !contained) return;
    const n = Array.prototype.slice.call(dlg.current.querySelectorAll(FOCUSABLE)).filter(el => el.offsetParent !== null);
    if (!n.length) return;
    const first = n[0],
      last = n[n.length - 1];
    if (e.shiftKey && document.activeElement === first) {
      e.preventDefault();
      last.focus();
    } else if (!e.shiftKey && document.activeElement === last) {
      e.preventDefault();
      first.focus();
    }
  };
  const threshold = () => parseFloat(getComputedStyle(dlg.current).getPropertyValue('--sheet-drag-dismiss')) || 120;
  const onDown = e => {
    start.current = e.clientY;
    e.currentTarget.setPointerCapture(e.pointerId);
  };
  const onMove = e => {
    if (start.current == null) return;
    setDy(Math.max(0, e.clientY - start.current));
  };
  const onUp = () => {
    if (dy > threshold()) close('gesture');
    start.current = null;
    setDy(0);
  };
  const dialog = /*#__PURE__*/React.createElement("dialog", {
    ref: dlg,
    className: "strand-sheet",
    "aria-label": label,
    "aria-busy": loading || undefined,
    onKeyDown: onKeyDown,
    onClick: e => {
      if (e.target === dlg.current && !contained) close('backdrop');
    },
    style: {
      position: contained ? 'absolute' : 'fixed',
      inset: g.inset,
      margin: g.margin,
      width,
      height,
      maxWidth: '100%',
      maxHeight: g.maxHeight || '100%',
      boxSizing: 'border-box',
      padding: 0,
      border: 0,
      borderLeft: tier === 'medium' ? 'var(--border-thin) solid var(--line)' : 0,
      borderRadius: g.radius,
      zIndex: z,
      background: 'var(--surface)',
      color: 'var(--ink)',
      fontFamily: 'var(--font-sans)',
      boxShadow: 'var(--shadow-3)',
      display: 'flex',
      flexDirection: 'column',
      overflow: 'hidden',
      paddingBottom: tier === 'compact' ? 'env(safe-area-inset-bottom, var(--safe-bottom, 0px))' : 0,
      transform: dy ? 'translateY(' + dy + 'px)' : 'none',
      transition: dy ? 'none' : 'transform var(--dur-slow) var(--ease)',
      animation: (phase === 'dismissing' ? g.anim + '-out' : g.anim) + ' var(--dur-slow) var(--ease)' + (phase === 'dismissing' ? ' forwards' : ''),
      ...style
    }
  }, /*#__PURE__*/React.createElement("style", null, '.strand-sheet::backdrop{background:var(--scrim)}@keyframes strand-sheet-rise{from{transform:translateY(var(--space-6));opacity:0}to{transform:none;opacity:1}}@keyframes strand-sheet-rise-out{from{transform:none;opacity:1}to{transform:translateY(var(--space-6));opacity:0}}@keyframes strand-sheet-slide{from{transform:translateX(var(--space-6));opacity:0}to{transform:none;opacity:1}}@keyframes strand-sheet-slide-out{from{transform:none;opacity:1}to{transform:translateX(var(--space-6));opacity:0}}'), tier === 'compact' && /*#__PURE__*/React.createElement("div", {
    onPointerDown: onDown,
    onPointerMove: onMove,
    onPointerUp: onUp,
    onPointerCancel: onUp,
    "aria-hidden": "true",
    style: {
      height: 'var(--space-6)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      cursor: 'grab',
      touchAction: 'none',
      flex: 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 36,
      height: 4,
      borderRadius: 'var(--radius-pill)',
      background: 'var(--ink-4)'
    }
  })), (title || onClose) && /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 'none',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 'var(--space-2)',
      padding: 'var(--space-3) var(--space-3) var(--space-3) var(--space-5)',
      borderBottom: scrolled ? 'var(--border-thin) solid var(--line)' : 'var(--border-thin) solid transparent',
      transition: 'border-color var(--dur-default) var(--ease)'
    }
  }, typeof title === 'string' || title == null ? /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: 0,
      fontFamily: 'var(--font-display)',
      fontSize: 'var(--display-s)',
      lineHeight: 'var(--display-s-lh)',
      fontWeight: 'var(--weight-regular)'
    }
  }, title) : /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, title), onClose && /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    name: "x",
    label: "Close",
    onClick: () => close('button')
  })), /*#__PURE__*/React.createElement("div", {
    ref: body,
    "data-sheet-body": true,
    onScroll: e => setScrolled(e.currentTarget.scrollTop > 0),
    style: {
      flex: tier === 'expanded' ? '0 1 auto' : 1,
      minHeight: 0,
      overflowY: 'auto',
      padding: 'var(--space-5)',
      fontSize: 'var(--text-m)',
      lineHeight: 'var(--text-m-lh)'
    }
  }, loading ? /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      color: 'var(--ink-3)'
    }
  }, "Loading") : children), /*#__PURE__*/React.createElement("div", {
    "aria-live": "polite",
    role: "status",
    style: {
      flex: 'none'
    }
  }, error && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      padding: 'var(--space-3) var(--space-5)',
      background: 'var(--error-tint)',
      color: 'var(--error)',
      fontSize: 'var(--text-s)',
      lineHeight: 1.45,
      borderTop: 'var(--border-thin) solid var(--line)'
    }
  }, error)), footer && /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 'none',
      display: 'flex',
      gap: 'var(--space-2)',
      justifyContent: 'flex-end',
      padding: 'var(--space-3) var(--space-5) var(--space-5)',
      borderTop: 'var(--border-thin) solid var(--line)'
    }
  }, footer));
  if (!contained) return dialog;
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    role: "presentation",
    "data-sheet-scrim": true,
    onClick: () => close('backdrop'),
    style: {
      position: 'absolute',
      inset: 0,
      background: 'var(--scrim)',
      zIndex: 'var(--z-scrim)',
      animation: 'strand-sheet-fade var(--dur-slow) var(--ease)'
    }
  }, /*#__PURE__*/React.createElement("style", null, '@keyframes strand-sheet-fade{from{opacity:0}to{opacity:1}}')), dialog);
}
Object.assign(__ds_scope, { Sheet });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Sheet.jsx", error: String((e && e.message) || e) }); }

// components/core/Dialog.jsx
try { (() => {
let warned = false;
/** Modal dialog, centred. Holds a decision that is not a full surface.
 *  DEPRECATED (605): the `sheet` boolean. Strand has one sheet and it is Sheet. Passing `sheet` routes to
 *  <Sheet tier="compact"> and warns once; it will be removed on the Sheet correction's schedule. */
function Dialog({
  open,
  title,
  children,
  footer,
  onClose,
  sheet,
  style
}) {
  if (sheet) {
    if (!warned && typeof console !== 'undefined') {
      warned = true;
      console.warn('Dialog: the `sheet` prop is deprecated (605). Use <Sheet tier="compact"> directly; Strand has one sheet.');
    }
    return /*#__PURE__*/React.createElement(__ds_scope.Sheet, {
      open: open,
      onClose: () => onClose && onClose(),
      tier: "compact",
      title: title,
      footer: footer,
      style: style
    }, children);
  }
  if (!open) return null;
  return /*#__PURE__*/React.createElement("div", {
    role: "presentation",
    onClick: onClose,
    style: {
      position: 'fixed',
      inset: 0,
      background: 'var(--scrim)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      zIndex: 'var(--z-dialog)',
      padding: 'var(--space-4)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    role: "dialog",
    "aria-modal": "true",
    "aria-label": title,
    onClick: e => e.stopPropagation(),
    style: {
      background: 'var(--surface)',
      borderRadius: 'var(--radius-l)',
      border: 'var(--border-thin) solid var(--line)',
      boxShadow: 'var(--shadow-3)',
      width: '100%',
      maxWidth: 480,
      maxHeight: '90vh',
      display: 'flex',
      flexDirection: 'column',
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: 'var(--space-3) var(--space-3) var(--space-3) var(--space-5)',
      borderBottom: 'var(--border-thin) solid var(--line)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: 'var(--font-display)',
      fontSize: 'var(--display-s)',
      lineHeight: 'var(--display-s-lh)'
    }
  }, title), /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    name: "x",
    label: "Close",
    onClick: onClose
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 'var(--space-5)',
      overflowY: 'auto',
      fontSize: 'var(--text-m)',
      lineHeight: 'var(--text-m-lh)'
    }
  }, children), footer && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 'var(--space-3) var(--space-5) var(--space-5)',
      display: 'flex',
      gap: 'var(--space-2)',
      justifyContent: 'flex-end'
    }
  }, footer)));
}
Object.assign(__ds_scope, { Dialog });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Dialog.jsx", error: String((e && e.message) || e) }); }

// components/core/Switch.jsx
try { (() => {
/** Toggle switch with label. 703 (correction 13): the label is a block beside the control, so it wraps in a narrow host
 *  and no caller needs white-space: nowrap. Callers that already fit render unchanged. */
function Switch({
  label,
  checked,
  onChange,
  disabled,
  style
}) {
  return /*#__PURE__*/React.createElement("label", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 12,
      minHeight: 44,
      cursor: disabled ? 'default' : 'pointer',
      opacity: disabled ? 0.45 : 1,
      fontSize: 17,
      ...style
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      flex: '1 1 auto',
      minWidth: 0,
      lineHeight: 1.3
    }
  }, label), /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    role: "switch",
    checked: !!checked,
    disabled: disabled,
    onChange: e => onChange && onChange(e.target.checked),
    style: {
      position: 'absolute',
      opacity: 0,
      width: 0,
      height: 0
    }
  }), /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      width: 44,
      height: 26,
      borderRadius: 999,
      background: checked ? 'var(--ink)' : 'var(--line-strong)',
      position: 'relative',
      flex: 'none',
      transition: 'background var(--dur-default) var(--ease)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      top: 3,
      left: checked ? 21 : 3,
      width: 20,
      height: 20,
      borderRadius: 999,
      background: 'var(--surface)',
      transition: 'left var(--dur-default) var(--ease)'
    }
  })));
}
Object.assign(__ds_scope, { Switch });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Switch.jsx", error: String((e && e.message) || e) }); }

// components/core/Tabs.jsx
try { (() => {
/** Underline tabs for sections within one page. items: [{id,label}] */
function Tabs({
  items = [],
  value,
  onChange,
  c,
  style
}) {
  const color = c ? 'var(--c-' + c + ')' : 'var(--ink)';
  const text = c ? 'var(--c-' + c + '-text)' : 'var(--ink)';
  return /*#__PURE__*/React.createElement("div", {
    role: "tablist",
    style: {
      display: 'flex',
      gap: 4,
      borderBottom: '1px solid var(--line)',
      overflowX: 'auto',
      ...style
    }
  }, items.map(it => {
    const on = it.id === value;
    return /*#__PURE__*/React.createElement("button", {
      key: it.id,
      role: "tab",
      "aria-selected": on,
      type: "button",
      onClick: () => onChange && onChange(it.id),
      style: {
        all: 'unset',
        cursor: 'pointer',
        minHeight: 44,
        padding: '0 12px',
        fontFamily: 'var(--font-sans)',
        fontSize: 15,
        fontWeight: 500,
        color: on ? text : 'var(--ink-3)',
        boxShadow: on ? 'inset 0 -2px 0 ' + color : 'none',
        whiteSpace: 'nowrap',
        display: 'inline-flex',
        alignItems: 'center'
      }
    }, it.label);
  }));
}
Object.assign(__ds_scope, { Tabs });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Tabs.jsx", error: String((e && e.message) || e) }); }

// components/core/Toast.jsx
try { (() => {
/** Transient confirmation. Static component; the host controls mounting. */
function Toast({
  children,
  action,
  onAction,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    role: "status",
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 16,
      background: 'var(--inverse-bg)',
      color: 'var(--inverse-ink)',
      borderRadius: 'var(--radius-m)',
      padding: '12px 16px',
      fontSize: 15,
      lineHeight: 1.4,
      boxShadow: 'var(--shadow-3)',
      maxWidth: 420,
      ...style
    }
  }, /*#__PURE__*/React.createElement("span", null, children), action && /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onAction,
    style: {
      all: 'unset',
      cursor: 'pointer',
      fontWeight: 700,
      textDecoration: 'underline',
      textUnderlineOffset: 2
    }
  }, action));
}
Object.assign(__ds_scope, { Toast });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Toast.jsx", error: String((e && e.message) || e) }); }

// components/core/Tooltip.jsx
try { (() => {
/** Hover/focus tooltip. Wraps one child. */
function Tooltip({
  label,
  children,
  style
}) {
  const [on, setOn] = React.useState(false);
  return /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'relative',
      display: 'inline-flex',
      ...style
    },
    onMouseEnter: () => setOn(true),
    onMouseLeave: () => setOn(false),
    onFocus: () => setOn(true),
    onBlur: () => setOn(false)
  }, children, on && /*#__PURE__*/React.createElement("span", {
    role: "tooltip",
    style: {
      position: 'absolute',
      bottom: 'calc(100% + 6px)',
      left: '50%',
      transform: 'translateX(-50%)',
      background: 'var(--inverse-bg)',
      color: 'var(--inverse-ink)',
      fontSize: 13,
      lineHeight: 1.3,
      padding: '6px 10px',
      borderRadius: 6,
      whiteSpace: 'nowrap',
      zIndex: 10
    }
  }, label));
}
Object.assign(__ds_scope, { Tooltip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Tooltip.jsx", error: String((e && e.message) || e) }); }

// components/dna/AudienceSelect.jsx
try { (() => {
/** Exactly three audiences (rulings 56, 27): everyone, connections, and the anchor when the post is anchored. No public web. */
function AudienceSelect({
  value = 'everyone',
  onChange,
  anchor,
  label = 'Who sees this',
  style
}) {
  const opts = [{
    id: 'everyone',
    icon: 'globe',
    text: 'Everyone on DNA'
  }, {
    id: 'connections',
    icon: 'users',
    text: 'My connections'
  }];
  if (anchor) opts.push({
    id: 'anchored',
    icon: 'hash',
    text: anchor
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6,
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 15,
      fontWeight: 500,
      color: 'var(--ink-2)'
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    role: "radiogroup",
    "aria-label": label,
    style: {
      display: 'flex',
      gap: 8,
      flexWrap: 'wrap'
    }
  }, opts.map(o => {
    const on = o.id === value;
    return /*#__PURE__*/React.createElement("button", {
      key: o.id,
      type: "button",
      role: "radio",
      "aria-checked": on,
      onClick: () => onChange && onChange(o.id),
      style: {
        all: 'unset',
        boxSizing: 'border-box',
        cursor: 'pointer',
        display: 'inline-flex',
        alignItems: 'center',
        gap: 8,
        height: 44,
        padding: '0 14px',
        borderRadius: 999,
        fontFamily: 'var(--font-sans)',
        fontSize: 15,
        fontWeight: 500,
        color: on ? 'var(--on-fill)' : 'var(--ink)',
        background: on ? 'var(--ink)' : 'transparent',
        border: on ? '1px solid var(--ink)' : '1px solid var(--line)',
        transition: 'background var(--dur-default) var(--ease)'
      }
    }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: o.icon,
      size: 18
    }), /*#__PURE__*/React.createElement("span", null, o.text));
  })));
}
const AUDIENCE_LABEL = (value, anchor) => value === 'connections' ? 'My connections' : value === 'anchored' && anchor ? anchor : 'Everyone on DNA';
Object.assign(__ds_scope, { AudienceSelect, AUDIENCE_LABEL });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dna/AudienceSelect.jsx", error: String((e && e.message) || e) }); }

// components/dna/DiaLine.jsx
try { (() => {
/** DIA's one quiet line (ruling 52). state "thinking": pending dot, resolves within 2.5s. state "done": what DIA did + "Not this?" escape.
 *  Render nothing when DIA has nothing to say (grounded-or-empty). Never a banner, modal, or prompt. */
function DiaLine({
  state,
  text,
  onNotThis,
  style
}) {
  if (!state) return null;
  const base = {
    display: 'flex',
    alignItems: 'center',
    gap: 8,
    minHeight: 24,
    fontFamily: 'var(--font-sans)',
    fontSize: 15,
    lineHeight: 1.4,
    color: 'var(--ink-3)',
    ...style
  };
  if (state === 'thinking') return /*#__PURE__*/React.createElement("div", {
    role: "status",
    "aria-live": "polite",
    style: base
  }, /*#__PURE__*/React.createElement("style", null, '@keyframes strand-breathe{0%,100%{opacity:0.25}50%{opacity:1}}'), /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      width: 8,
      height: 8,
      borderRadius: 999,
      background: 'var(--ink-3)',
      animation: 'strand-breathe 1.2s var(--ease) infinite'
    }
  }), /*#__PURE__*/React.createElement("span", null, "DIA is reading"));
  return /*#__PURE__*/React.createElement("div", {
    role: "status",
    "aria-live": "polite",
    style: {
      ...base,
      animation: 'strand-fade var(--dur-default) var(--ease)'
    }
  }, /*#__PURE__*/React.createElement("style", null, '@keyframes strand-fade{from{opacity:0}to{opacity:1}}'), /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      width: 8,
      height: 8,
      borderRadius: 999,
      background: 'var(--ink-3)'
    }
  }), /*#__PURE__*/React.createElement("span", null, text), onNotThis && /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onNotThis,
    style: {
      all: 'unset',
      cursor: 'pointer',
      minHeight: 44,
      display: 'inline-flex',
      alignItems: 'center',
      padding: '0 6px',
      margin: '-10px 0',
      fontSize: 15,
      fontWeight: 500,
      color: 'var(--ink)',
      textDecoration: 'underline',
      textDecorationColor: 'var(--line-strong)',
      textUnderlineOffset: 2
    }
  }, "Not this?"));
}
Object.assign(__ds_scope, { DiaLine });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dna/DiaLine.jsx", error: String((e && e.message) || e) }); }

// components/dna/FacetRail.jsx
try { (() => {
/** FacetRail (561 as amended by 586): a persistent control over the surface. Setting a facet is itself resolve-and-return,
 *  so there is no confirm step and no apply button. Under --tier-medium it renders through Sheet, which is the same
 *  component and not a variant. At --tier-medium and above it is a rail.
 *  Absolute: a FacetRail never carries a count per facet. Carried from 102: it filters within a surface and never navigates,
 *  facet state lives in the URL, icon uniqueness per surface is a build gate, and it does not replace LensBar.
 *  Motion per 589: --dur-default. "Rail" keeps 79's meaning and is a different object; see RailWidget.
 *  Correction 13 (687, 688): `mode` names three renderings of one part. `facets` is the behaviour above, unchanged.
 *  `single` is one selection across the rail's items, exposed through onChange as `{ [axisId]: [optionId] }`; the caller owns the route,
 *  the rail never navigates. `collapsed` is a 64px icon strip and expands back on a control the caller names. Correction 14 (725): the strip
 *  renders one square per axis (the axis's `icon`, name from the axis label, tinted --bg-sunken when any option in the axis is set), not per option.
 *  The through-Sheet rendering at compact is unchanged and is not a mode; `trigger` lets the caller supply the pill that opens it. */
const CHIP = (on, parked) => ({
  all: 'unset',
  boxSizing: 'border-box',
  cursor: parked ? 'default' : 'pointer',
  minHeight: 36,
  padding: '0 var(--space-4)',
  display: 'inline-flex',
  alignItems: 'center',
  borderRadius: 'var(--radius-pill)',
  fontFamily: 'var(--font-sans)',
  fontSize: 'var(--text-s)',
  fontWeight: 'var(--weight-medium)',
  whiteSpace: 'nowrap',
  color: parked ? 'var(--ink-4)' : on ? 'var(--ink)' : 'var(--ink-2)',
  background: on ? 'var(--bg-sunken)' : 'transparent',
  border: on ? 'var(--border-card) solid var(--ink)' : 'var(--border-thin) solid var(--line)',
  opacity: parked ? 0.6 : 1,
  transition: 'background var(--dur-default) var(--ease), border-color var(--dur-default) var(--ease)'
});
function FacetRail({
  axes = [],
  value = {},
  onChange,
  onClear,
  tier = 'expanded',
  label = 'Filters',
  open,
  onOpenChange,
  style,
  mode = 'facets',
  select,
  trigger,
  onExpand,
  expandLabel = 'Show ' + label.toLowerCase()
}) {
  // 718 addendum: a collapsed strip declares the selection model of the rail it stands for; `select="single"` is the Hub's strip.
  const single = mode === 'single' || mode === 'collapsed' && select === 'single';
  const set = (axisId, optionId) => {
    if (single) {
      onChange && onChange({
        [axisId]: [optionId]
      });
      return;
    }
    const cur = value[axisId] || [];
    const next = {
      ...value,
      [axisId]: cur.indexOf(optionId) > -1 ? cur.filter(o => o !== optionId) : cur.concat(optionId)
    };
    if (!next[axisId].length) delete next[axisId];
    onChange && onChange(next);
  };
  const isOn = (axisId, optionId) => (value[axisId] || []).indexOf(optionId) > -1;
  const any = Object.keys(value).some(k => (value[k] || []).length);
  const onAxisKey = e => {
    const fwd = e.key === 'ArrowRight' || e.key === 'ArrowDown',
      back = e.key === 'ArrowLeft' || e.key === 'ArrowUp';
    if (!fwd && !back) return;
    const opts = Array.prototype.slice.call(e.currentTarget.querySelectorAll('button:not([disabled])'));
    const i = opts.indexOf(document.activeElement);
    if (i < 0) return;
    e.preventDefault();
    opts[(i + (fwd ? 1 : opts.length - 1)) % opts.length].focus(); // 718: arrows move focus only; Enter, Space or click selects.
  };
  if (mode === 'collapsed') {
    // 612, as amended by 725: an icon strip at expanded when the pane is open. One 48px square per axis, named from the axis label, icon caller-supplied
    // from Strand's set, tinted when any option in the axis is set. A square is not a control over an option; it expands the rail (onExpand).
    return /*#__PURE__*/React.createElement("nav", {
      "aria-label": label,
      style: {
        position: 'sticky',
        top: 'var(--space-4)',
        zIndex: 'var(--z-rail)',
        alignSelf: 'flex-start',
        width: 64,
        boxSizing: 'border-box',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        gap: 'var(--space-2)',
        padding: 'var(--space-2)',
        background: 'var(--surface)',
        border: 'var(--border-thin) solid var(--line)',
        borderRadius: 'var(--radius-l)',
        boxShadow: 'var(--shadow-0)',
        ...style
      }
    }, /*#__PURE__*/React.createElement("button", {
      type: "button",
      "aria-label": expandLabel,
      title: expandLabel,
      onClick: () => onExpand && onExpand(),
      style: {
        all: 'unset',
        boxSizing: 'border-box',
        cursor: 'pointer',
        width: 48,
        height: 48,
        display: 'inline-flex',
        alignItems: 'center',
        justifyContent: 'center',
        borderRadius: 'var(--radius-m)',
        color: 'var(--ink-2)'
      }
    }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: "chevron-right",
      size: 20
    })), /*#__PURE__*/React.createElement("div", {
      role: "list",
      "aria-label": label,
      style: {
        display: 'flex',
        flexDirection: 'column',
        gap: 'var(--space-2)',
        paddingTop: 'var(--space-2)',
        borderTop: 'var(--border-thin) solid var(--line)'
      }
    }, axes.map(axis => {
      const on = (value[axis.id] || []).length > 0;
      return /*#__PURE__*/React.createElement("div", {
        key: axis.id,
        role: "listitem"
      }, /*#__PURE__*/React.createElement("button", {
        type: "button",
        "aria-label": axis.label,
        title: axis.label,
        "aria-pressed": on,
        "data-axis": axis.id,
        onClick: () => onExpand && onExpand(),
        style: {
          all: 'unset',
          boxSizing: 'border-box',
          cursor: 'pointer',
          width: 48,
          height: 48,
          display: 'inline-flex',
          alignItems: 'center',
          justifyContent: 'center',
          borderRadius: 'var(--radius-m)',
          fontFamily: 'var(--font-sans)',
          fontSize: 'var(--text-xs)',
          fontWeight: 'var(--weight-medium)',
          letterSpacing: 'var(--tracking-caps)',
          textTransform: 'uppercase',
          color: on ? 'var(--ink)' : 'var(--ink-2)',
          background: on ? 'var(--bg-sunken)' : 'transparent',
          border: on ? 'var(--border-card) solid var(--ink)' : 'var(--border-thin) solid transparent',
          transition: 'background var(--dur-default) var(--ease), border-color var(--dur-default) var(--ease)'
        }
      }, axis.icon ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
        name: axis.icon,
        size: 20
      }) : axis.label.slice(0, 2)));
    })));
  }
  const body = /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-6)',
      fontFamily: 'var(--font-sans)'
    }
  }, axes.map(axis => /*#__PURE__*/React.createElement("div", {
    key: axis.id,
    role: single ? 'radiogroup' : 'group',
    "aria-labelledby": 'facet-' + axis.id,
    onKeyDown: onAxisKey,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-3)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    id: 'facet-' + axis.id,
    style: {
      fontSize: 'var(--text-xs)',
      letterSpacing: 'var(--tracking-caps)',
      textTransform: 'uppercase',
      color: 'var(--ink-3)'
    }
  }, axis.label), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 'var(--space-2)'
    }
  }, (axis.options || []).map(o => {
    const on = isOn(axis.id, o.id);
    return /*#__PURE__*/React.createElement("button", {
      key: o.id,
      type: "button",
      role: single ? 'radio' : undefined,
      "aria-checked": single ? on : undefined,
      "aria-pressed": single ? undefined : on,
      tabIndex: single ? on || !any && axis === axes[0] && o === axis.options[0] ? 0 : -1 : undefined,
      disabled: o.parked,
      "aria-disabled": o.parked || undefined,
      onClick: () => set(axis.id, o.id),
      style: CHIP(on, o.parked)
    }, o.label);
  })))), !single && /*#__PURE__*/React.createElement("div", {
    "aria-live": "polite",
    role: "status",
    style: {
      minHeight: 0
    }
  }, any && /*#__PURE__*/React.createElement(__ds_scope.Button, {
    variant: "ghost",
    size: "sm",
    onClick: () => onClear && onClear()
  }, "Clear all")));
  if (tier === 'compact') {
    const openIt = () => onOpenChange && onOpenChange(true);
    return /*#__PURE__*/React.createElement(React.Fragment, null, trigger ? trigger({
      open: !!open,
      onOpen: openIt,
      label
    }) : /*#__PURE__*/React.createElement(__ds_scope.Button, {
      variant: "secondary",
      size: "sm",
      "aria-expanded": !!open,
      onClick: openIt,
      style: style
    }, label), open && /*#__PURE__*/React.createElement(__ds_scope.Sheet, {
      open: true,
      onClose: () => onOpenChange && onOpenChange(false),
      tier: "compact",
      title: label,
      contained: true
    }, body));
  }
  return /*#__PURE__*/React.createElement("nav", {
    "aria-label": label,
    style: {
      position: 'sticky',
      top: 'var(--space-4)',
      zIndex: 'var(--z-rail)',
      alignSelf: 'flex-start',
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-5)',
      padding: 'var(--space-5)',
      background: 'var(--surface)',
      border: 'var(--border-thin) solid var(--line)',
      borderRadius: 'var(--radius-l)',
      boxShadow: 'var(--shadow-0)',
      ...style
    }
  }, /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: 0,
      fontFamily: 'var(--font-display)',
      fontSize: 'var(--display-s)',
      lineHeight: 'var(--display-s-lh)',
      fontWeight: 'var(--weight-regular)'
    }
  }, label), body);
}
Object.assign(__ds_scope, { FacetRail });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dna/FacetRail.jsx", error: String((e && e.message) || e) }); }

// components/dna/LensBar.jsx
try { (() => {
/** Lens Bar: switches corpus on any list surface (rule 5). Segmented pill. `scope` is the italic line beneath that says what the
 *  selected lens shows (rulings 81, 83). Filters remain a separate control (Chip).
 *  Correction 14 (723): labels-fit. The bar measures whether every lens label fits its column; when it does, labels render as before;
 *  when it does not, the bar renders icon-first: the active lens shows its icon and label, every other lens shows its icon with an accessible
 *  name. Lenses take `icon` from Strand's set for this. The switch is a rendering decision inside the part, not a caller prop;
 *  `labels="always"` forces labels for sets that always fit. No motion on the switch. A set without icons on every lens never switches.
 *  Brief 2's icon-only rendering of `icon` is superseded by 723: an icon is what the bar falls back to, not a display mode.
 *  Correction 16 (794, 770, G37). The rule is 14's, unchanged; what changed is the measurement and the padding.
 *  (a) The test prices the widest label as the active tab plus once per inactive tab, with gaps and track padding, against the column.
 *      That is exact under `width="fill"`, where the active tab keeps its content width and the rest share what is left, and an upper
 *      bound under the default content-width packing, where each tab is only as wide as its own label. It never prices a tab narrower
 *      than it renders. Summing label widths passed while one long label overflowed its own share, which is the overlap at 390 (794).
 *  (b) It re-measures when the document's fonts settle and on every later font load (770); a label measured in the fallback face
 *      is not the label the reader gets.
 *  (c) G37: inactive tabs carry 15px of side padding against the active tab's 16px, which returns the medium tier to labels.
 *      No tolerance is added anywhere: when the exact test fails the bar still falls to icon-first.
 *  Correction 17 (868, 865, 869). Labels mode is label-only: no icon renders beside a label, which is why the fit test prices label
 *  plus padding and nothing else. Only icon-first draws icons (active icon + label, inactive icon alone). Adopted at 17: `label` (the
 *  tablist's accessible name, was the hardcoded "Lens"), `c` (the active lens reads in that C's text rung, never the brand fill — a 15px
 *  label on a teal fill is 3.9:1 and fails AA), `collapsed` (hides the `scope` descriptor; the latch under 405 is the caller's state),
 *  and `width` (`content`, the default and prior behaviour, or `fill`, where the active tab keeps its content width and inactive tabs take
 *  an equal share of the rest). Refused at 17: a `compact` seat size, which asks for 32px targets on the tier where rule 10's 44px bites
 *  hardest; the space need it answers is already served by icon-first. Caller-side at 17: a header that shows only the active lens's name
 *  is a different control, not a mode of this bar (561 as amended by 586).
 *  A measurement of zero is not a measurement, and neither is a measurement in the wrong space. Label widths come back in screen pixels,
 *  so a CSS transform on any ancestor scales them while the column is read in layout pixels. The bar divides the measured label by its own
 *  wrapper's scale, taken as rect width over `offsetWidth` so that both terms include the border box and the ratio is exactly 1 unscaled
 *  whatever a caller's `style` adds. If the widest label or the column measures 0 (the probe has not been laid out yet) the bar keeps the
 *  rendering it has and retries; it never reads a zero, or a scaled-down label, as "everything fits", which would silently revert the
 *  part to the overlap 794 fixed. */
const GAP = 2,
  TRACK = 8,
  PAD_ON = 16,
  PAD_OFF = 15; // TRACK: the track's 3px padding and 1px border, both sides
const TAB = (on, iconOnly, c, fill) => ({
  all: 'unset',
  cursor: 'pointer',
  minHeight: 38,
  minWidth: iconOnly ? 44 : undefined,
  flex: fill ? on ? '0 0 auto' : '1 1 0' : undefined,
  padding: iconOnly ? 0 : on ? '0 ' + PAD_ON + 'px' : '0 ' + PAD_OFF + 'px',
  gap: 8,
  borderRadius: 999,
  fontFamily: 'var(--font-sans)',
  fontSize: 15,
  fontWeight: 500,
  whiteSpace: 'nowrap',
  display: 'inline-flex',
  alignItems: 'center',
  justifyContent: 'center',
  background: on ? 'var(--surface)' : 'transparent',
  color: on ? c ? 'var(--c-' + c + '-text)' : 'var(--ink)' : 'var(--ink-3)',
  boxShadow: on ? '0 1px 2px rgba(26,26,24,0.08)' : 'none',
  transition: 'background var(--dur-default) var(--ease)'
});
const LABEL_TYPE = {
  fontFamily: 'var(--font-sans)',
  fontSize: 15,
  fontWeight: 500,
  whiteSpace: 'nowrap'
};
function LensBar({
  lenses = [],
  value,
  onChange,
  scope,
  labels,
  label = 'Lens',
  c,
  collapsed,
  width = 'content',
  style
}) {
  const wrap = React.useRef(null),
    probe = React.useRef(null);
  const [fit, setFit] = React.useState(true);
  const canSwitch = labels !== 'always' && lenses.length > 0 && lenses.every(l => l.icon);
  const key = lenses.map(l => l.label).join('\u0001');
  React.useLayoutEffect(() => {
    if (!canSwitch) {
      setFit(true);
      return;
    }
    let alive = true,
      raf = 0,
      tid = 0,
      retries = 0;
    const measure = () => {
      if (!alive || !wrap.current || !probe.current) return;
      let raw = 0;
      for (const s of probe.current.children) raw = Math.max(raw, s.getBoundingClientRect().width);
      const col = wrap.current.clientWidth;
      const box = wrap.current.offsetWidth;
      const scale = box > 0 ? wrap.current.getBoundingClientRect().width / box : 1; // screen px per layout px; both terms are border-box, so 1 unscaled
      const w = Math.ceil(raw / scale);
      // A hidden or backgrounded view pauses rAF, so the retry also runs on a timer.
      if ((w <= 0 || col <= 0) && retries < 20) {
        retries++;
        raf = requestAnimationFrame(measure);
        tid = setTimeout(measure, 120);
        return;
      }
      if (w <= 0 || col <= 0) return;
      retries = 0;
      const n = lenses.length;
      const need = TRACK + GAP * (n - 1) + (w + 2 * PAD_ON) + (n - 1) * (w + 2 * PAD_OFF);
      setFit(need <= col);
    };
    measure();
    raf = requestAnimationFrame(measure);
    const ro = typeof ResizeObserver !== 'undefined' && wrap.current ? new ResizeObserver(measure) : null;
    if (ro) ro.observe(wrap.current);
    const fonts = typeof document !== 'undefined' && document.fonts;
    if (fonts && fonts.ready && fonts.ready.then) fonts.ready.then(measure);
    if (fonts && fonts.addEventListener) fonts.addEventListener('loadingdone', measure);
    return () => {
      alive = false;
      if (raf) cancelAnimationFrame(raf);
      if (tid) clearTimeout(tid);
      if (ro) ro.disconnect();
      if (fonts && fonts.removeEventListener) fonts.removeEventListener('loadingdone', measure);
    };
  }, [canSwitch, key]);
  const iconFirst = canSwitch && !fit,
    fill = width === 'fill';
  return /*#__PURE__*/React.createElement("div", {
    ref: wrap,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6,
      fontFamily: 'var(--font-sans)',
      maxWidth: '100%',
      position: 'relative',
      ...style
    }
  }, canSwitch && /*#__PURE__*/React.createElement("div", {
    ref: probe,
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      top: 0,
      left: 0,
      visibility: 'hidden',
      pointerEvents: 'none',
      display: 'inline-flex'
    }
  }, lenses.map(l => /*#__PURE__*/React.createElement("span", {
    key: l.id,
    style: LABEL_TYPE
  }, l.label))), /*#__PURE__*/React.createElement("div", {
    role: "tablist",
    "aria-label": label,
    "data-lensbar": iconFirst ? 'icon-first' : 'labels',
    "data-lensbar-width": width,
    style: {
      display: 'flex',
      alignSelf: fill ? 'stretch' : 'flex-start',
      width: fill ? '100%' : undefined,
      gap: GAP,
      padding: 3,
      background: 'var(--bg-sunken)',
      borderRadius: 999,
      border: '1px solid var(--line)',
      maxWidth: '100%',
      overflowX: 'auto',
      boxSizing: 'border-box'
    }
  }, lenses.map(l => {
    const on = l.id === value,
      iconOnly = iconFirst && !on;
    return /*#__PURE__*/React.createElement("button", {
      key: l.id,
      role: "tab",
      "aria-selected": on,
      "aria-label": iconOnly ? l.label : undefined,
      title: iconOnly ? l.label : undefined,
      type: "button",
      onClick: () => onChange && onChange(l.id),
      style: TAB(on, iconOnly, c, fill)
    }, iconOnly ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: l.icon,
      size: 20
    }) : iconFirst ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: l.icon,
      size: 20
    }), l.label) : l.label);
  })), scope && !collapsed && /*#__PURE__*/React.createElement("div", {
    "aria-live": "polite",
    style: {
      fontSize: 13,
      fontStyle: 'italic',
      lineHeight: 1.4,
      color: 'var(--ink-3)',
      padding: '0 4px'
    }
  }, scope));
}
Object.assign(__ds_scope, { LensBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dna/LensBar.jsx", error: String((e && e.message) || e) }); }

// components/dna/MediaBlock.jsx
try { (() => {
/** Shared media block. kind: none | image | gallery | video | link | audio. Gallery capped at 4. */
function MediaBlock({
  kind = 'none',
  src,
  items = [],
  alt = '',
  title,
  domain,
  duration,
  style
}) {
  if (kind === 'none') return null;
  const frame = {
    borderRadius: 14,
    overflow: 'hidden',
    border: '1px solid var(--line)',
    background: 'var(--bg-sunken)',
    ...style
  };
  if (kind === 'image') return /*#__PURE__*/React.createElement("div", {
    style: frame
  }, /*#__PURE__*/React.createElement("img", {
    src: src,
    alt: alt,
    style: {
      display: 'block',
      width: '100%',
      maxHeight: 420,
      objectFit: 'cover'
    }
  }));
  if (kind === 'gallery') {
    const g = items.slice(0, 4);
    return /*#__PURE__*/React.createElement("div", {
      style: {
        ...frame,
        display: 'grid',
        gridTemplateColumns: '1fr 1fr',
        gap: 2,
        aspectRatio: '16/10'
      }
    }, g.map((s, i) => /*#__PURE__*/React.createElement("img", {
      key: i,
      src: s,
      alt: "",
      style: {
        width: '100%',
        height: '100%',
        objectFit: 'cover',
        display: 'block',
        gridColumn: g.length === 3 && i === 0 ? 'span 2' : undefined
      }
    })));
  }
  if (kind === 'video') return /*#__PURE__*/React.createElement("div", {
    style: {
      ...frame,
      position: 'relative',
      aspectRatio: '16/9'
    }
  }, src && /*#__PURE__*/React.createElement("img", {
    src: src,
    alt: alt,
    style: {
      width: '100%',
      height: '100%',
      objectFit: 'cover',
      display: 'block'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 56,
      height: 56,
      borderRadius: 999,
      background: 'var(--surface)',
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      color: 'var(--ink)'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "play",
    size: 24
  }))), duration && /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      right: 10,
      bottom: 10,
      background: 'var(--inverse-bg)',
      color: 'var(--inverse-ink)',
      fontSize: 13,
      padding: '2px 8px',
      borderRadius: 6
    }
  }, duration));
  if (kind === 'link') return /*#__PURE__*/React.createElement("a", {
    href: src,
    target: "_blank",
    rel: "noreferrer",
    style: {
      ...frame,
      display: 'flex',
      textDecoration: 'none',
      color: 'inherit'
    }
  }, items[0] && /*#__PURE__*/React.createElement("img", {
    src: items[0],
    alt: "",
    style: {
      width: 112,
      objectFit: 'cover',
      flex: 'none'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      padding: '12px 14px',
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      fontSize: 13,
      color: 'var(--ink-3)',
      letterSpacing: '0.04em',
      textTransform: 'uppercase'
    }
  }, domain), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      fontSize: 15,
      fontWeight: 500,
      lineHeight: 1.4,
      marginTop: 4
    }
  }, title)));
  if (kind === 'audio') return /*#__PURE__*/React.createElement("div", {
    style: {
      ...frame,
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      padding: '10px 12px',
      background: 'var(--surface)'
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Play",
    style: {
      all: 'unset',
      width: 44,
      height: 44,
      borderRadius: 999,
      background: 'var(--ink)',
      color: 'var(--on-fill)',
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      cursor: 'pointer',
      flex: 'none'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "play",
    size: 20
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      fontSize: 15,
      fontWeight: 500,
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap'
    }
  }, title), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      height: 3,
      background: 'var(--line)',
      borderRadius: 2,
      marginTop: 8
    }
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 13,
      color: 'var(--ink-3)',
      flex: 'none'
    }
  }, duration));
  return null;
}
Object.assign(__ds_scope, { MediaBlock });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dna/MediaBlock.jsx", error: String((e && e.message) || e) }); }

// components/dna/NotificationBell.jsx
try { (() => {
/** Bell with a dot, never a numeral (ruling 82). unread=false renders no dot: no row, no dot. */
function NotificationBell({
  unread,
  active,
  onClick,
  style
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'relative',
      display: 'inline-flex',
      flex: 'none',
      ...style
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    name: "bell",
    label: unread ? 'Notifications, unread' : 'Notifications',
    active: active,
    "aria-expanded": !!active,
    onClick: onClick
  }), unread && /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      top: 9,
      right: 9,
      width: 8,
      height: 8,
      borderRadius: 999,
      background: 'var(--pulse-for-you)',
      border: '2px solid var(--bg)',
      boxSizing: 'content-box',
      pointerEvents: 'none',
      transition: 'background var(--dur-default) var(--ease)'
    }
  }));
}
Object.assign(__ds_scope, { NotificationBell });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dna/NotificationBell.jsx", error: String((e && e.message) || e) }); }

// components/dna/Pane.jsx
try { (() => {
/** Pane (561): a second surface consulted alongside the first. Navigated into, never summoned.
 *  Above --tier-expanded it is a pane beside the list. Below it, the pane is its own route, full width, with its own
 *  back affordance; medium taking the pane was the counter-case 561 ruled against, so medium gets the route.
 *  A pane never collapses to a sheet at any tier. Its content is independently addressable at every tier, which is
 *  router work the host owns: the host supplies href and onBack, this component does not navigate.
 *  Cold arrival above --tier-expanded renders listFallback, the default corpus with the arrived-at item marked and
 *  scrolled to (607): a stranger arriving at one item from a shared link needs to find the others.
 *  612: a surface carrying a pane does not render 79's right rail. The pane occupies and extends through that region
 *  and DIA is absent while it is open. Below --tier-expanded the pane is its own route, so the list surface is a
 *  separate page and DIA is present there.
 *  Motion per 589: --dur-slow. Stacking per 588: --z-pane, below all chrome, because a pane is page content.
 *  700 (correction 13): `onClose`, when supplied, renders one close control at the pane's top right named by `closeLabel`,
 *  and fires on activation and on Escape while focus is within the pane (719). Absent, nothing renders and the pane is as before. */
function Pane({
  tier = 'expanded',
  list,
  listFallback,
  cold,
  children,
  selected,
  title,
  empty,
  loading,
  error,
  onBack,
  backLabel = 'Back',
  onClose,
  closeLabel = 'Close',
  style
}) {
  const listCol = React.useRef(null);
  // 719: Escape is handled on the pane section itself, so it fires only while focus is within the pane. No document listener.
  const onPaneKey = onClose ? e => {
    if (e.key === 'Escape' && !e.defaultPrevented) {
      e.preventDefault();
      onClose();
    }
  } : undefined;
  const closeBtn = onClose ? /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    name: "x",
    label: closeLabel,
    onClick: onClose
  }) : null;
  React.useEffect(() => {
    // 607: bring the arrived-at item into view without moving focus or the page.
    if (!cold || tier !== 'expanded' || !listCol.current) return;
    const el = listCol.current.querySelector('[data-arrived]');
    if (!el) return;
    const box = el.getBoundingClientRect(),
      col = listCol.current.getBoundingClientRect();
    listCol.current.scrollTop += box.top - col.top;
  }, [cold, tier]);
  const paneBody = loading ? /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      padding: 'var(--space-6)',
      color: 'var(--ink-3)',
      fontSize: 'var(--text-m)'
    }
  }, "Loading") : error ? /*#__PURE__*/React.createElement("div", {
    role: "alert",
    style: {
      margin: 'var(--space-5)',
      padding: 'var(--space-4) var(--space-5)',
      background: 'var(--error-tint)',
      color: 'var(--error)',
      borderRadius: 'var(--radius-m)',
      fontSize: 'var(--text-s)',
      lineHeight: 1.45
    }
  }, error) : selected === false ? empty : children;
  if (tier !== 'expanded') return /*#__PURE__*/React.createElement("section", {
    "aria-label": title,
    onKeyDown: onPaneKey,
    style: {
      display: 'flex',
      flexDirection: 'column',
      minHeight: 0,
      background: 'var(--bg)',
      fontFamily: 'var(--font-sans)',
      color: 'var(--ink)',
      animation: 'strand-pane-in var(--dur-slow) var(--ease)',
      ...style
    }
  }, /*#__PURE__*/React.createElement("style", null, '@keyframes strand-pane-in{from{opacity:0}to{opacity:1}}'), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 'none',
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-2)',
      padding: 'var(--space-2) var(--space-3)',
      borderBottom: 'var(--border-thin) solid var(--line)',
      background: 'var(--surface-glass)',
      position: 'sticky',
      top: 0,
      zIndex: 'var(--z-sticky)'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    name: "arrow-left",
    label: backLabel,
    onClick: onBack
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      minWidth: 0,
      fontSize: 'var(--text-m)',
      fontWeight: 'var(--weight-medium)',
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap'
    }
  }, cold ? backLabel : title), closeBtn), /*#__PURE__*/React.createElement("div", {
    "aria-live": "polite",
    style: {
      flex: 1,
      minHeight: 0,
      overflowY: 'auto'
    }
  }, paneBody));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'minmax(0,var(--pane-list-width)) minmax(0,1fr)',
      gap: 'var(--pane-gap)',
      alignItems: 'start',
      fontFamily: 'var(--font-sans)',
      color: 'var(--ink)',
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    ref: listCol,
    style: {
      minWidth: 0
    }
  }, cold ? listFallback : list), /*#__PURE__*/React.createElement("section", {
    "aria-label": title,
    "aria-live": "polite",
    onKeyDown: onPaneKey,
    style: {
      position: 'sticky',
      top: 'var(--space-4)',
      zIndex: 'var(--z-pane)',
      minWidth: 0,
      background: 'var(--surface)',
      border: 'var(--border-thin) solid var(--line)',
      borderRadius: 'var(--radius-l)',
      overflow: 'hidden',
      animation: 'strand-pane-in var(--dur-slow) var(--ease)'
    }
  }, /*#__PURE__*/React.createElement("style", null, '@keyframes strand-pane-in{from{opacity:0}to{opacity:1}}'), closeBtn && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 'var(--space-2)',
      right: 'var(--space-2)',
      zIndex: 1
    }
  }, closeBtn), paneBody));
}
Object.assign(__ds_scope, { Pane });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dna/Pane.jsx", error: String((e && e.message) || e) }); }

// components/dna/RailWidget.jsx
try { (() => {
/** Generic rail block (ruling 79). Context only, never navigation. Caps title, optional action, children or the honest empty line (grounded-or-empty). */
function RailWidget({
  title,
  action,
  empty,
  children,
  style
}) {
  const has = React.Children.toArray(children).some(Boolean);
  return /*#__PURE__*/React.createElement("section", {
    "aria-label": title,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10,
      fontFamily: 'var(--font-sans)',
      color: 'var(--ink)',
      ...style
    }
  }, /*#__PURE__*/React.createElement("header", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 8,
      minHeight: 24
    }
  }, /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: 0,
      fontSize: 13,
      letterSpacing: '0.06em',
      textTransform: 'uppercase',
      fontWeight: 500,
      color: 'var(--ink-3)'
    }
  }, title), action), has ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 4
    }
  }, children) : /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 15,
      lineHeight: 1.45,
      color: 'var(--ink-2)',
      textWrap: 'pretty'
    }
  }, empty));
}
Object.assign(__ds_scope, { RailWidget });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dna/RailWidget.jsx", error: String((e && e.message) || e) }); }

// components/dna/TierBadge.jsx
try { (() => {
/** Member tier as a word, never a number or percentage (rule 7). */
function TierBadge({
  tier,
  style
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6,
      height: 28,
      padding: '0 10px',
      borderRadius: 8,
      border: '1px solid var(--line-strong)',
      background: 'var(--surface)',
      fontFamily: 'var(--font-sans)',
      fontSize: 13,
      fontWeight: 500,
      letterSpacing: '0.06em',
      textTransform: 'uppercase',
      color: 'var(--ink-2)',
      ...style
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "shield-check",
    size: 14
  }), tier);
}
Object.assign(__ds_scope, { TierBadge });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dna/TierBadge.jsx", error: String((e && e.message) || e) }); }

// components/dna/cmeta.js
try { (() => {
const C_LABEL = {
  connect: 'Connect',
  convene: 'Convene',
  collaborate: 'Collaborate',
  contribute: 'Contribute',
  convey: 'Convey'
};
const C_GLYPH = {
  connect: 'nkonsonkonson',
  convene: 'mpatapo',
  collaborate: 'funtunfunefu-denkyemfunefu',
  contribute: 'adinkrahene',
  convey: 'sankofa',
  brand: 'mate-masie'
};
const C_ORDER = ['connect', 'convene', 'collaborate', 'contribute', 'convey'];
/** Resolve the design system's assets/ folder from wherever _ds_bundle.js was loaded. */
function assetBase() {
  if (typeof window !== 'undefined' && window.STRAND_ASSET_BASE) return window.STRAND_ASSET_BASE;
  if (typeof document !== 'undefined') {
    const s = document.querySelector('script[src*="_ds_bundle"]');
    if (s) return s.src.replace(/_ds_bundle[^/]*$/, 'assets/');
  }
  return 'assets/';
}
Object.assign(__ds_scope, { C_LABEL, C_GLYPH, C_ORDER, assetBase });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dna/cmeta.js", error: String((e && e.message) || e) }); }

// components/dna/AppHeader.jsx
try { (() => {
/** App shell header (ruling 69). Mounted once at the app root; no surface composes its own.
 *  variant "compact": touch and medium tiers. Logo is Home. Composer pill, then children (the NotificationBell slot), then avatar.
 *  variant "expanded": logo plus a labelled Home item; the five-C row (PulseDock bar) sits directly beneath. */
function AppHeader({
  variant = 'compact',
  homeActive,
  onHome,
  member = {},
  onCompose,
  composePlaceholder = 'What is going on with you?',
  onAvatar,
  children,
  style
}) {
  const exp = variant === 'expanded';
  const logo = __ds_scope.assetBase() + 'logo.png';
  const [hh, setHh] = React.useState(false);
  return /*#__PURE__*/React.createElement("header", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: exp ? 16 : 10,
      height: exp ? 64 : 56,
      padding: exp ? '0 32px' : '0 8px 0 16px',
      background: 'var(--bg)',
      borderBottom: exp ? 'none' : '1px solid var(--line)',
      fontFamily: 'var(--font-sans)',
      color: 'var(--ink)',
      boxSizing: 'border-box',
      flex: 'none',
      ...style
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onHome,
    "aria-label": "Home",
    "aria-current": homeActive && !exp ? 'page' : undefined,
    style: {
      all: 'unset',
      cursor: 'pointer',
      display: 'inline-flex',
      alignItems: 'center',
      minHeight: 44,
      flex: 'none'
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: logo,
    alt: "DNA",
    style: {
      height: exp ? 28 : 24,
      display: 'block'
    }
  })), exp && /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onHome,
    "aria-current": homeActive ? 'page' : undefined,
    onMouseEnter: () => setHh(true),
    onMouseLeave: () => setHh(false),
    style: {
      all: 'unset',
      cursor: 'pointer',
      minHeight: 44,
      padding: '0 12px',
      display: 'inline-flex',
      alignItems: 'center',
      borderRadius: 10,
      fontSize: 15,
      fontWeight: 500,
      color: homeActive ? 'var(--ink)' : 'var(--ink-3)',
      background: hh ? 'var(--bg-sunken)' : 'transparent',
      transition: 'background var(--dur-default) var(--ease)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      lineHeight: 1.2,
      borderBottom: homeActive ? '2px solid var(--ink)' : '2px solid transparent'
    }
  }, "Home")), exp && /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onCompose,
    "aria-label": "Open the composer",
    style: {
      all: 'unset',
      boxSizing: 'border-box',
      cursor: 'text',
      flex: exp ? 'none' : 1,
      width: exp ? 320 : undefined,
      minWidth: 0,
      minHeight: 44,
      display: 'flex',
      alignItems: 'center',
      gap: 10,
      padding: '0 14px',
      borderRadius: 999,
      background: 'var(--bg-sunken)',
      color: 'var(--ink-3)',
      fontSize: 15
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "pen-line",
    size: 16
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap'
    }
  }, composePlaceholder)), children, /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onAvatar,
    "aria-label": "Your profile",
    style: {
      all: 'unset',
      cursor: 'pointer',
      display: 'inline-flex',
      minWidth: 44,
      minHeight: 44,
      alignItems: 'center',
      justifyContent: 'center',
      flex: 'none'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Avatar, {
    name: member.name,
    src: member.src,
    size: exp ? 36 : 32
  })));
}
Object.assign(__ds_scope, { AppHeader });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dna/AppHeader.jsx", error: String((e && e.message) || e) }); }

// components/dna/CBadge.jsx
try { (() => {
/** Adinkra glyph in a rounded-square badge on the C tint. The only place C identity is drawn as an icon. */
function CBadge({
  c = 'connect',
  size = 32,
  label,
  style
}) {
  const url = __ds_scope.assetBase() + 'adinkra/' + (__ds_scope.C_GLYPH[c] || __ds_scope.C_GLYPH.brand) + '.svg';
  const m = 'url(' + url + ') center / contain no-repeat';
  const isBrand = c === 'brand';
  const bg = isBrand ? 'var(--bg-sunken)' : 'var(--c-' + c + '-tint)';
  const fg = isBrand ? 'var(--ink)' : 'var(--c-' + c + ')';
  const badge = /*#__PURE__*/React.createElement("span", {
    role: "img",
    "aria-label": __ds_scope.C_LABEL[c] || 'DNA',
    style: {
      width: size,
      height: size,
      borderRadius: size >= 48 ? 10 : 8,
      background: bg,
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      flex: 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      width: Math.round(size * 0.56),
      height: Math.round(size * 0.56),
      background: fg,
      WebkitMask: m,
      mask: m
    }
  }));
  if (!label) return React.cloneElement(badge, {
    style: {
      ...badge.props.style,
      ...style
    }
  });
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      ...style
    }
  }, badge, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: size >= 48 ? 17 : 13,
      fontWeight: 500,
      letterSpacing: size >= 48 ? 0 : '0.06em',
      textTransform: size >= 48 ? 'none' : 'uppercase',
      color: fg
    }
  }, __ds_scope.C_LABEL[c]));
}
Object.assign(__ds_scope, { CBadge });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dna/CBadge.jsx", error: String((e && e.message) || e) }); }

// components/dna/ActButton.jsx
try { (() => {
/** Composer entry point (rule 8): plain-language act first, C name and color secondary. */
function ActButton({
  c,
  act,
  onClick,
  style
}) {
  const [hover, setHover] = React.useState(false);
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      all: 'unset',
      boxSizing: 'border-box',
      cursor: 'pointer',
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      width: '100%',
      minHeight: 56,
      padding: '8px 12px',
      borderRadius: 10,
      background: hover ? 'var(--bg-sunken)' : 'transparent',
      fontFamily: 'var(--font-sans)',
      transition: 'background var(--dur-default) var(--ease)',
      ...style
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.CBadge, {
    c: c,
    size: 32
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 2,
      textAlign: 'left'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 17,
      fontWeight: 500,
      color: 'var(--ink)',
      lineHeight: 1.3
    }
  }, act), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 13,
      color: 'var(--c-' + c + '-text)',
      letterSpacing: '0.04em'
    }
  }, __ds_scope.C_LABEL[c])));
}
Object.assign(__ds_scope, { ActButton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dna/ActButton.jsx", error: String((e && e.message) || e) }); }

// components/dna/EmptyState.jsx
try { (() => {
/** Grounded-or-empty (rule 6). Names the state and offers one act. Never a zero. */
function EmptyState({
  c = 'brand',
  title,
  body,
  action,
  pattern = 'adinkra',
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      textAlign: 'center',
      padding: '40px 24px',
      borderRadius: 14,
      background: 'var(--bg) url(' + __ds_scope.assetBase() + 'patterns/' + pattern + '-pattern.svg)',
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      gap: 12,
      fontFamily: 'var(--font-sans)',
      ...style
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.CBadge, {
    c: c,
    size: 48
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: 'var(--font-display)',
      fontSize: 22,
      lineHeight: 1.25,
      color: 'var(--ink)',
      maxWidth: 360
    }
  }, title), body && /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 15,
      lineHeight: 1.45,
      color: 'var(--ink-2)',
      maxWidth: 360
    }
  }, body), action && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 8
    }
  }, action));
}
Object.assign(__ds_scope, { EmptyState });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dna/EmptyState.jsx", error: String((e && e.message) || e) }); }

// components/dna/NotificationListItem.jsx
try { (() => {
const KIND_C = {
  connection_accepted: 'connect',
  attestation_received: 'contribute',
  space_role_approved: 'collaborate',
  event_reminder: 'convene',
  role_invitation: 'convene'
};
function parts({
  kind,
  actor,
  object,
  detail,
  text
}) {
  switch (kind) {
    case 'connection_accepted':
      return [[actor, 1], ' accepted your intro.'];
    case 'attestation_received':
      return [[actor, 1], ' attested your contribution to ', [object, 1], '.'];
    case 'space_role_approved':
      return ['You are now ', detail, ' in ', [object, 1], '.'];
    case 'event_reminder':
      return [[object, 1], ' starts ', detail, '.'];
    case 'role_invitation':
      return [text || ''];
    // 736: the caller's string, verbatim. Strand ships no invitation copy.
    default:
      return [''];
  }
}
const ROW = (hover, style) => ({
  all: 'unset',
  boxSizing: 'border-box',
  cursor: 'pointer',
  width: '100%',
  display: 'flex',
  alignItems: 'flex-start',
  gap: 12,
  padding: '12px 16px',
  minHeight: 56,
  background: hover ? 'var(--bg-sunken)' : 'transparent',
  fontFamily: 'var(--font-sans)',
  color: 'var(--ink)',
  textAlign: 'left',
  transition: 'background var(--dur-default) var(--ease)',
  ...style
});
/** One notification row (ruling 82). kind is the closed set; the C glyph marks which engine wrote the row (ruling 66). Unread: dot and 500 weight.
 *  Correction 15 (736): `role_invitation` resolves to Convene and carries one Respond act. Its sentence is the caller's `text`, verbatim; the act's
 *  accessible name is "Respond" followed by that string. The other kinds render exactly as before. */
function NotificationListItem({
  kind,
  actor,
  object,
  detail,
  text,
  time,
  unread,
  onClick,
  onRespond,
  style
}) {
  const [hover, setHover] = React.useState(false);
  const body = /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(__ds_scope.CBadge, {
    c: KIND_C[kind] || 'connect',
    size: 32
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 15,
      lineHeight: 1.4,
      fontWeight: unread ? 500 : 400,
      textWrap: 'pretty'
    }
  }, parts({
    kind,
    actor,
    object,
    detail,
    text
  }).map((p, i) => Array.isArray(p) ? /*#__PURE__*/React.createElement("b", {
    key: i,
    style: {
      fontWeight: 700
    }
  }, p[0]) : p)), time && /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 13,
      color: 'var(--ink-3)',
      lineHeight: 1.4
    }
  }, time)), unread && /*#__PURE__*/React.createElement("span", {
    "aria-label": "Unread",
    style: {
      width: 8,
      height: 8,
      borderRadius: 999,
      background: 'var(--pulse-for-you)',
      flex: 'none',
      marginTop: 8
    }
  }));
  if (kind === 'role_invitation') {
    // The row and its act are two controls, so the row is a div and not a button here; the row's hit target is the sentence.
    return /*#__PURE__*/React.createElement("div", {
      "data-kind": kind,
      onMouseEnter: () => setHover(true),
      onMouseLeave: () => setHover(false),
      style: {
        ...ROW(hover, style),
        cursor: 'default',
        alignItems: 'center'
      }
    }, /*#__PURE__*/React.createElement("button", {
      type: "button",
      onClick: onClick,
      style: {
        all: 'unset',
        boxSizing: 'border-box',
        cursor: 'pointer',
        flex: 1,
        minWidth: 0,
        display: 'flex',
        alignItems: 'flex-start',
        gap: 12,
        textAlign: 'left',
        color: 'inherit',
        fontFamily: 'inherit'
      }
    }, body), /*#__PURE__*/React.createElement(__ds_scope.Button, {
      variant: "secondary",
      size: "sm",
      c: "convene",
      "aria-label": 'Respond' + (text ? ': ' + text : ''),
      onClick: onRespond,
      style: {
        flex: 'none'
      }
    }, "Respond"));
  }
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: ROW(hover, style)
  }, body);
}
Object.assign(__ds_scope, { KIND_C, NotificationListItem });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dna/NotificationListItem.jsx", error: String((e && e.message) || e) }); }

// components/dna/PostCard.jsx
try { (() => {
/** The one card chassis (rule 1). Identity marker is a 1.5px full-frame border in the C color (rule 2).
 *  Brief 2 adds the Feed anatomy (ruling 69) behind `feed`: C-coloured meta line, overflow menu (onMenu), clamped body with an explicit
 *  "Read more" (onReadMore), divider, four icon-only actions (React, Respond, Save, Share; ruling 72: a single act, no counts). No cross-C logic in the card.
 *  Without `feed` the card renders exactly as Brief 1 shipped it (composer preview). c="system" stays for platform-authored items. */
function PostCard({
  c = 'connect',
  author,
  authorKind = 'member',
  meta,
  avatarSrc,
  anchor,
  audience,
  kicker,
  title,
  children,
  fields,
  media,
  link,
  actions,
  saved,
  onSave,
  onShare,
  onRespond,
  respondLabel = 'Respond',
  onClick,
  preview,
  feed,
  onMenu,
  onReadMore,
  reacted,
  onReact,
  style
}) {
  const sys = c === 'system';
  const frame = sys ? 'var(--line-strong)' : 'var(--c-' + c + ')';
  const cColor = sys ? 'var(--ink-3)' : 'var(--c-' + c + '-text)';
  const rows = (fields || []).filter(f => f && f.value);
  const linkStyle = {
    all: 'unset',
    cursor: 'pointer',
    alignSelf: 'flex-start',
    minHeight: 44,
    display: 'inline-flex',
    alignItems: 'center',
    fontSize: 15,
    fontWeight: 500,
    color: 'var(--ink)',
    textDecoration: 'underline',
    textDecorationColor: 'var(--line-strong)',
    textUnderlineOffset: 2,
    margin: '-10px 0'
  };
  return /*#__PURE__*/React.createElement("article", {
    "aria-label": preview ? 'Preview of your post' : undefined,
    style: {
      background: 'var(--surface)',
      borderRadius: 14,
      border: '1.5px solid ' + frame,
      padding: 16,
      display: 'flex',
      flexDirection: 'column',
      gap: 12,
      fontFamily: 'var(--font-sans)',
      color: 'var(--ink)',
      boxSizing: 'border-box',
      ...style
    }
  }, /*#__PURE__*/React.createElement("header", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10
    }
  }, author && /*#__PURE__*/React.createElement(__ds_scope.Avatar, {
    name: author,
    src: avatarSrc,
    size: 40,
    style: authorKind === 'space' ? {
      borderRadius: 6
    } : undefined
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, author && /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 15,
      fontWeight: 700,
      lineHeight: 1.3,
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap'
    }
  }, author, authorKind === 'space' && /*#__PURE__*/React.createElement("span", {
    style: {
      fontWeight: 400,
      color: 'var(--ink-3)'
    }
  }, " \xB7 Space")), (meta || anchor || audience) && /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      color: feed ? cColor : 'var(--ink-3)',
      lineHeight: 1.4,
      display: 'flex',
      gap: 6,
      flexWrap: 'wrap'
    }
  }, meta && /*#__PURE__*/React.createElement("span", null, meta), anchor && /*#__PURE__*/React.createElement("span", null, meta ? '· ' : '', "In ", anchor), audience && /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 3
    }
  }, meta || anchor ? '· ' : '', /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: audience === 'Everyone on DNA' ? 'globe' : audience === 'My connections' ? 'users' : 'hash',
    size: 12
  }), audience))), !sys && /*#__PURE__*/React.createElement(__ds_scope.CBadge, {
    c: c,
    size: 32
  }), onMenu && /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    name: "ellipsis",
    label: "More",
    size: 36,
    onClick: onMenu,
    style: {
      marginRight: -8
    }
  })), /*#__PURE__*/React.createElement("div", {
    onClick: onClick,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8,
      cursor: onClick ? 'pointer' : 'default'
    }
  }, kicker && /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      letterSpacing: '0.06em',
      textTransform: 'uppercase',
      fontWeight: 500,
      color: cColor
    }
  }, kicker), title && /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontFamily: 'var(--font-display)',
      fontWeight: 400,
      fontSize: 22,
      lineHeight: 1.25,
      textWrap: 'pretty'
    }
  }, title), children && /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 17,
      lineHeight: 1.5,
      whiteSpace: 'pre-wrap',
      overflowWrap: 'anywhere',
      ...(onReadMore ? {
        display: '-webkit-box',
        WebkitLineClamp: 4,
        WebkitBoxOrient: 'vertical',
        overflow: 'hidden'
      } : {})
    }
  }, children), onReadMore && /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: e => {
      e.stopPropagation();
      onReadMore();
    },
    style: linkStyle
  }, "Read more"), rows.length > 0 && /*#__PURE__*/React.createElement("dl", {
    style: {
      margin: 0,
      display: 'grid',
      gridTemplateColumns: 'auto 1fr',
      gap: '6px 12px',
      fontSize: 15,
      lineHeight: 1.45,
      padding: '10px 12px',
      background: 'var(--bg-sunken)',
      borderRadius: 10
    }
  }, rows.map(f => /*#__PURE__*/React.createElement(React.Fragment, {
    key: f.label
  }, /*#__PURE__*/React.createElement("dt", {
    style: {
      color: 'var(--ink-3)',
      display: 'flex',
      alignItems: 'center',
      gap: 6
    }
  }, f.icon && /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: f.icon,
    size: 16
  }), f.label), /*#__PURE__*/React.createElement("dd", {
    style: {
      margin: 0,
      fontWeight: 500,
      display: 'flex',
      alignItems: 'center',
      gap: 6
    }
  }, f.value, preview && f.mine && /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "pen-line",
    size: 14,
    title: "Written by you",
    style: {
      color: 'var(--ink-3)'
    }
  })))))), media && media.kind && media.kind !== 'none' && /*#__PURE__*/React.createElement(__ds_scope.MediaBlock, media), link && link.url && /*#__PURE__*/React.createElement(__ds_scope.MediaBlock, {
    kind: "link",
    src: link.url,
    domain: link.domain,
    title: link.title,
    items: link.image ? [link.image] : []
  }), actions && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 8,
      flexWrap: 'wrap'
    }
  }, actions), feed ? /*#__PURE__*/React.createElement("footer", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 4,
      marginLeft: -10,
      marginRight: -10,
      marginBottom: -8,
      paddingTop: 8,
      borderTop: '1px solid var(--line)',
      pointerEvents: preview ? 'none' : 'auto'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    name: "heart",
    label: reacted ? 'Reacted' : 'React',
    active: reacted,
    onClick: onReact
  }), /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    name: "message-circle",
    label: respondLabel,
    onClick: onRespond
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    name: "bookmark",
    label: saved ? 'Saved' : 'Save',
    active: saved,
    onClick: onSave
  }), /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    name: "share",
    label: "Share",
    onClick: onShare
  })) : /*#__PURE__*/React.createElement("footer", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 4,
      marginTop: -4,
      marginLeft: -10,
      marginRight: -10,
      pointerEvents: preview ? 'none' : 'auto'
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onRespond,
    style: {
      all: 'unset',
      cursor: 'pointer',
      minHeight: 44,
      padding: '0 10px',
      fontSize: 15,
      fontWeight: 500,
      color: 'var(--ink-2)',
      display: 'inline-flex',
      alignItems: 'center'
    }
  }, respondLabel), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    name: "bookmark",
    label: saved ? 'Saved' : 'Save',
    active: saved,
    onClick: onSave
  }), /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    name: "share",
    label: "Share",
    onClick: onShare
  })));
}
Object.assign(__ds_scope, { PostCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dna/PostCard.jsx", error: String((e && e.message) || e) }); }

// components/dna/PulseDock.jsx
try { (() => {
const DOT = {
  none: 'transparent',
  activity: 'var(--pulse-activity)',
  'for-you': 'var(--pulse-for-you)',
  urgent: 'var(--pulse-urgent)'
};
/** Pulse family. Mobile bottom dock (default) or desktop horizontal bar (bar). states: {connect:'none'|'activity'|'for-you'|'urgent', ...}. State is the signal, not C color (rule 4). */
function PulseDock({
  active,
  states = {},
  onSelect,
  bar,
  fixed,
  style
}) {
  const items = __ds_scope.C_ORDER.map(c => {
    const on = c === active,
      st = states[c] || 'none';
    return /*#__PURE__*/React.createElement("button", {
      key: c,
      type: "button",
      "aria-current": on ? 'page' : undefined,
      "aria-label": __ds_scope.C_LABEL[c] + (st !== 'none' ? ', ' + st.replace('-', ' ') : ''),
      onClick: () => onSelect && onSelect(c),
      style: {
        all: 'unset',
        cursor: 'pointer',
        position: 'relative',
        flex: bar ? 'none' : 1,
        minWidth: 44,
        minHeight: 44,
        display: 'flex',
        flexDirection: bar ? 'row' : 'column',
        alignItems: 'center',
        justifyContent: 'center',
        gap: bar ? 8 : 2,
        padding: bar ? '0 14px' : '6px 0',
        borderRadius: 10,
        fontFamily: 'var(--font-sans)',
        fontSize: bar ? 15 : 12,
        fontWeight: 500,
        color: on ? 'var(--ink)' : 'var(--ink-3)'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        position: 'relative',
        display: 'inline-flex'
      }
    }, /*#__PURE__*/React.createElement(__ds_scope.CBadge, {
      c: c,
      size: bar ? 24 : 32,
      style: {
        opacity: on ? 1 : 0.6,
        filter: on ? 'none' : 'grayscale(1)'
      }
    }), /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        position: 'absolute',
        top: -2,
        right: -2,
        width: 10,
        height: 10,
        borderRadius: 999,
        background: DOT[st],
        border: st === 'none' ? 'none' : '2px solid var(--bg)',
        boxSizing: 'content-box',
        transition: 'background var(--dur-default) var(--ease)'
      }
    })), /*#__PURE__*/React.createElement("span", {
      style: {
        lineHeight: 1.2,
        borderBottom: bar && on ? '2px solid var(--ink)' : 'none'
      }
    }, __ds_scope.C_LABEL[c]));
  });
  if (bar) return /*#__PURE__*/React.createElement("nav", {
    "aria-label": "Pulse",
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 4,
      height: 56,
      padding: '0 12px',
      background: 'var(--bg)',
      borderBottom: '1px solid var(--line)',
      position: fixed ? 'sticky' : 'static',
      top: 0,
      zIndex: 20,
      ...style
    }
  }, items);
  return /*#__PURE__*/React.createElement("nav", {
    "aria-label": "Pulse",
    style: {
      display: 'flex',
      alignItems: 'stretch',
      height: 64,
      padding: '0 8px',
      paddingBottom: 'env(safe-area-inset-bottom)',
      background: 'var(--surface-glass)',
      backdropFilter: 'blur(12px)',
      WebkitBackdropFilter: 'blur(12px)',
      borderTop: '1px solid var(--line)',
      position: fixed ? 'fixed' : 'static',
      bottom: 0,
      left: 0,
      right: 0,
      zIndex: 20,
      ...style
    }
  }, items);
}
Object.assign(__ds_scope, { PulseDock });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dna/PulseDock.jsx", error: String((e && e.message) || e) }); }

// components/dna/VerbChip.jsx
try { (() => {
const VERB_ACT = {
  connect: 'Make an Intro',
  convene: 'Host an Event',
  collaborate: 'Start a Space',
  contribute: 'Post a Need',
  convey: 'Share a Story'
};
/** Composer verb override (ruling 53). Always visible, never the required first step. Selected = tint fill, 1.5px C border. 44px tall.
 * disabled + reason (53, inside an event): the chip stays in the row at Button's disabled opacity (0.45), aria-disabled, and carries the reason as its title and aria-description. It never leaves the DOM. */
function VerbChip({
  c,
  selected,
  onClick,
  compact,
  disabled,
  reason,
  style
}) {
  const [hover, setHover] = React.useState(false);
  const color = 'var(--c-' + c + ')';
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    role: "radio",
    "aria-checked": !!selected,
    "aria-disabled": disabled || undefined,
    "aria-description": disabled ? reason : undefined,
    title: disabled ? reason : undefined,
    "aria-label": VERB_ACT[c] + ' (' + __ds_scope.C_LABEL[c] + ')',
    onClick: disabled ? undefined : onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      all: 'unset',
      boxSizing: 'border-box',
      cursor: disabled ? 'not-allowed' : 'pointer',
      opacity: disabled ? 0.45 : 1,
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      height: 44,
      padding: compact ? '0 12px 0 8px' : '0 14px 0 8px',
      borderRadius: 999,
      whiteSpace: 'nowrap',
      fontFamily: 'var(--font-sans)',
      fontSize: 15,
      fontWeight: 500,
      lineHeight: 1,
      color: selected ? 'var(--c-' + c + '-text)' : 'var(--ink)',
      background: selected ? 'var(--c-' + c + '-tint)' : hover && !disabled ? 'var(--bg-sunken)' : 'transparent',
      border: selected ? '1.5px solid ' + color : '1px solid var(--line)',
      transition: 'background var(--dur-default) var(--ease), border-color var(--dur-default) var(--ease)',
      ...style
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.CBadge, {
    c: c,
    size: 28
  }), /*#__PURE__*/React.createElement("span", null, VERB_ACT[c]));
}
Object.assign(__ds_scope, { VERB_ACT, VerbChip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dna/VerbChip.jsx", error: String((e && e.message) || e) }); }

// components/dna/Composer.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Structured fields of the object each verb creates (brief: backing data shape). */
const VERB_SCHEMA = {
  connect: {
    kicker: 'Intro',
    action: 'Accept the intro',
    fields: [{
      key: 'who',
      label: 'Who',
      icon: 'user-plus'
    }, {
      key: 'why',
      label: 'Why',
      icon: 'message-circle',
      multiline: true
    }]
  },
  convene: {
    kicker: 'Event',
    action: 'Get a ticket',
    fields: [{
      key: 'title',
      label: 'Title',
      title: true
    }, {
      key: 'date',
      label: 'Date',
      icon: 'calendar'
    }, {
      key: 'time',
      label: 'Time',
      icon: 'clock'
    }, {
      key: 'place',
      label: 'Location or link',
      icon: 'map-pin'
    }, {
      key: 'hybrid',
      label: 'Hybrid',
      kind: 'toggle'
    }, {
      key: 'ticket',
      label: 'Ticket',
      icon: 'ticket',
      kind: 'segment',
      options: ['Free', 'Paid']
    }]
  },
  collaborate: {
    kicker: 'Space',
    action: 'Join the Space',
    fields: [{
      key: 'title',
      label: 'Title',
      title: true
    }, {
      key: 'category',
      label: 'Category',
      icon: 'hash'
    }, {
      key: 'roles',
      label: 'Roles sought',
      icon: 'users'
    }]
  },
  contribute: {
    kicker: 'Need',
    action: 'Offer to help',
    fields: [{
      key: 'title',
      label: 'Title',
      title: true
    }, {
      key: 'instrument',
      label: 'Instrument',
      icon: 'briefcase',
      kind: 'segment',
      options: ['Time', 'Skills', 'In-kind']
    }, {
      key: 'need',
      label: 'What is needed',
      icon: 'circle-dot',
      multiline: true
    }, {
      key: 'by',
      label: 'By when',
      icon: 'calendar'
    }]
  },
  convey: {
    kicker: 'Story',
    action: 'Read the story',
    fields: [{
      key: 'title',
      label: 'Title',
      title: true
    }]
  }
};
/** Untyped member posts fall back to Convey with no kicker, title, or action. c="system" on PostCard is reserved for platform-authored items. */
const UNTYPED = {
  kicker: null,
  action: null,
  fields: []
};
const DRAFT_KEY = 'dna.composer.draft';
const THINK_BUDGET = 2500;
/** Software keyboard height on touch devices, from visualViewport. 0 when no keyboard or API. */
function useKeyboardHeight(active) {
  const [kb, setKb] = React.useState(0);
  React.useEffect(() => {
    const vv = window.visualViewport;
    if (!active || !vv) {
      setKb(0);
      return;
    }
    const f = () => setKb(Math.max(0, window.innerHeight - vv.height - vv.offsetTop));
    f();
    vv.addEventListener('resize', f);
    vv.addEventListener('scroll', f);
    return () => {
      vv.removeEventListener('resize', f);
      vv.removeEventListener('scroll', f);
    };
  }, [active]);
  return kb;
}

/** The Universal Post Composer (Brief 1). One shell for every verb; DIA suggests, the member decides.
 * 636 items 6 to 9: `forms` per-verb form slot (580 third state), `disabledVerbs` with reasons (53), async `onPublish` with in-flight and failed states, and proposal-without-selection for the DIA read (635).
 * 10 to 12 (671, 672, 673): a supplied form's preview reads only `{c}.title` and `{c}.meta` and carries the card's fixed actions; a touch tap on a disabled chip writes its reason into the DiaLine slot; segments are the Segment part. */
function Composer({
  open,
  onClose,
  onPublish,
  tier = 'compact',
  mode,
  author = {
    name: 'You'
  },
  spaces = [],
  anchor,
  infer,
  initialVerb,
  initial,
  contained,
  maxImages = 4,
  unfurl,
  forms = {},
  disabledVerbs = {}
}) {
  const touch = (mode || (tier === 'compact' ? 'touch' : 'pointer')) === 'touch';
  const draft = React.useMemo(() => {
    try {
      return JSON.parse(localStorage.getItem(DRAFT_KEY) || 'null');
    } catch {
      return null;
    }
  }, [open]);
  const seed = initial || !initialVerb && draft || {};
  const [text, setText] = React.useState(seed.text || '');
  const [verb, setVerb] = React.useState(seed.verb || initialVerb || null); // the member's chosen verb; only a tap sets it (635)
  const [proposed, setProposed] = React.useState(seed.proposed || null); // DIA's proposal; never marks a chip
  const [overridden, setOverridden] = React.useState(!!(seed.overridden || initialVerb));
  const [fv, setFv] = React.useState(seed.fields || {});
  const [images, setImages] = React.useState(seed.images || []);
  const [link, setLink] = React.useState(seed.link || null);
  const [linkOpen, setLinkOpen] = React.useState(false);
  const [url, setUrl] = React.useState('');
  const [audience, setAudience] = React.useState(seed.audience || (anchor ? 'anchored' : 'everyone'));
  const [asSpace, setAsSpace] = React.useState(seed.asSpace || '');
  const [dia, setDia] = React.useState(seed.dia || {
    state: null
  });
  const [drafted, setDrafted] = React.useState(!!draft && !initial);
  const [picker, setPicker] = React.useState(false);
  const fileRef = React.useRef(),
    camRef = React.useRef(),
    taRef = React.useRef(),
    run = React.useRef(0);
  const [armed, setArmed] = React.useState(false),
    depth = React.useRef(0); // 636: armed drop, pointer only
  const [pending, setPending] = React.useState(false),
    [fail, setFail] = React.useState(null); // item 8: publish in flight / failed
  const [formValid, setFormValid] = React.useState(false); // 664: the supplied form's single validity report; unset means invalid
  const [reasonShown, setReasonShown] = React.useState(null); // 672: a disabled chip's reason, shown in the DiaLine slot after a touch tap; the next tap in the row clears it

  const kb = useKeyboardHeight(open && touch && tier === 'compact');
  const active = verb || proposed; // the verb whose form mounts: chosen, else proposed (635)
  const c = active || 'convey';
  const schema = active ? VERB_SCHEMA[active] : UNTYPED;
  const has = text.trim().length > 0 || images.length > 0 || !!link || Object.values(fv).some(f => f && f.value);
  const setField = (key, value, mine = true) => setFv(s => ({
    ...s,
    [key]: {
      value,
      mine
    }
  }));
  // 664: a supplied form's keys are namespaced `{verb}.{key}` in the store; the form sees them un-prefixed.
  const ns = active ? active + '.' : '';
  const formFields = React.useMemo(() => Object.fromEntries(Object.entries(fv).filter(([k]) => k.startsWith(ns)).map(([k, v]) => [k.slice(ns.length), v])), [fv, ns]);
  const formSetField = (key, value, mine = true) => setField(ns + key, value, mine);
  const formMine = k => !!(fv[ns + k] && fv[ns + k].mine);
  const prevActive = React.useRef(active);
  React.useEffect(() => {
    if (prevActive.current !== active) {
      prevActive.current = active;
      setFormValid(false);
    }
  }, [active]); // a new verb's form has not reported yet; mount itself does not reset a report already made

  // Inference: debounce, pending state, resolve or stay silent. Never after the member has chosen.
  React.useEffect(() => {
    if (!open || overridden || !infer || initial && initial.hold) return;
    const t = text.trim();
    if (t.length < 8) {
      if (!overridden) {
        setProposed(null);
        setDia({
          state: null
        });
      }
      return;
    }
    const id = ++run.current;
    const h = setTimeout(async () => {
      setDia({
        state: 'thinking'
      });
      const res = await Promise.race([infer(t), new Promise(r => setTimeout(() => r(null), THINK_BUDGET))]).catch(() => null);
      if (id !== run.current) return;
      if (!res || !res.c || disabledVerbs[res.c]) {
        setDia({
          state: null
        });
        return;
      } // a disabled verb is never proposed (53)
      setProposed(res.c); // 635: proposed, never selected. The chip stays unchecked; the DiaLine and the proposed fields carry the read.
      setFv(s => {
        const n = {
          ...s
        };
        Object.entries(res.fields || {}).forEach(([k, v]) => {
          if (!n[k] || !n[k].mine) n[k] = {
            value: v,
            mine: false
          };
        });
        return n;
      });
      setDia({
        state: 'done',
        text: res.line || 'DIA read this as ' + (VERB_SCHEMA[res.c].kicker === 'Intro' ? 'an Intro.' : VERB_SCHEMA[res.c].kicker === 'Event' ? 'an Event.' : 'a ' + VERB_SCHEMA[res.c].kicker + '.')
      });
    }, 700);
    return () => clearTimeout(h);
  }, [text, open, overridden]);

  // Draft autosave (ruling 56). The indicator is the only trace.
  React.useEffect(() => {
    if (!open) return;
    const h = setTimeout(() => {
      if (has) {
        localStorage.setItem(DRAFT_KEY, JSON.stringify({
          text,
          verb,
          proposed,
          overridden,
          fields: fv,
          images: images.filter(i => !i.startsWith('data:')),
          link,
          audience,
          asSpace,
          dia
        }));
        setDrafted(true);
      } else {
        localStorage.removeItem(DRAFT_KEY);
        setDrafted(false);
      }
    }, 800);
    return () => clearTimeout(h);
  }, [text, verb, proposed, fv, images, link, audience, asSpace, has, open]);
  React.useEffect(() => {
    // 641: focus the text field explicitly by ref on open, not by DOM position. Deferred one frame so it lands after Sheet's own initial focus move.
    if (!open || touch) return;
    const id = requestAnimationFrame(() => {
      taRef.current && taRef.current.focus();
    });
    return () => cancelAnimationFrame(id);
  }, [open]);
  React.useEffect(() => {
    if (!open || touch) return;
    const k = e => {
      if ((e.metaKey || e.ctrlKey) && e.key === 'Enter' && canPublish) publish();
    };
    window.addEventListener('keydown', k);
    return () => window.removeEventListener('keydown', k);
  });
  const notThis = () => {
    setOverridden(true);
    setProposed(null);
    setVerb(null);
    setDia({
      state: null
    });
    setFv(s => Object.fromEntries(Object.entries(s).filter(([, f]) => f.mine)));
  };
  const choose = v => {
    if (disabledVerbs[v]) return;
    setOverridden(true);
    setVerb(v);
    setProposed(null);
    setDia({
      state: null
    });
  }; // a hand-picked verb silences the read (53, 635). 668: a tap on the proposed chip keeps DIA's fills.
  const addFiles = files => {
    const list = Array.from(files || []).slice(0, maxImages - images.length);
    list.forEach(f => {
      const r = new FileReader();
      r.onload = () => setImages(s => s.length < maxImages ? [...s, r.result] : s);
      r.readAsDataURL(f);
    });
    setPicker(false);
  };
  const addLink = async () => {
    const u = url.trim();
    if (!u) return;
    let host = u;
    try {
      host = new URL(u.startsWith('http') ? u : 'https://' + u).hostname.replace(/^www\./, '');
    } catch {}
    setLink({
      url: u,
      domain: host
    });
    setLinkOpen(false);
    setUrl('');
    if (unfurl) {
      const meta = await unfurl(u).catch(() => null);
      if (meta) setLink(l => l && l.url === u ? {
        ...l,
        ...meta
      } : l);
    }
  };
  const space = spaces.find(s => s.id === asSpace);
  const rows = schema.fields.filter(f => !f.title).map(f => {
    const v = fv[f.key];
    if (!v || v.value === '' || v.value == null || v.value === false) return null;
    return {
      label: f.label,
      icon: f.icon,
      value: v.value === true ? 'Yes' : v.value,
      mine: v.mine
    };
  }).filter(Boolean);
  const post = {
    author_kind: space ? 'space' : 'member',
    author: space ? space.name : author.name,
    avatar: space ? undefined : author.avatar,
    c_category: c,
    typed: !!active,
    body: text,
    anchor: anchor && anchor.name,
    created_object: active ? Object.fromEntries(Object.entries(fv).map(([k, v]) => [k, v.value])) : null,
    audience,
    media: images,
    link_unfurl: link,
    dia_line: dia.state === 'done' ? dia.text : null,
    status: 'draft'
  };
  // 671, preview by convention: with a supplied form the preview reads exactly `{c}.title` and `{c}.meta` from the store and nothing else of the form's; its action row is the card's fixed vocabulary, never VERB_SCHEMA[c].action. Without a form, unchanged.
  const form = active && forms[active];
  const convTitle = form ? fv[ns + 'title'] && fv[ns + 'title'].value || undefined : active ? fv.title && fv.title.value : undefined;
  const convMeta = form ? fv[ns + 'meta'] && fv[ns + 'meta'].value || undefined : undefined;
  // Item 8 / 666: onPublish may return a promise. In flight: fields stay mounted and read-only, Publish reads "Publishing". Failure: Sheet's error slot carries the message, the draft is untouched. Resolution: draft cleared, pending reset, onClose('published').
  const publish = async () => {
    if (!canPublish) return;
    setFail(null);
    setPending(true);
    try {
      await (onPublish && onPublish({
        ...post,
        status: 'published',
        fields: form ? [] : rows,
        title: convTitle,
        meta: convMeta,
        kicker: schema.kicker,
        action: form ? null : schema.action,
        audienceLabel: __ds_scope.AUDIENCE_LABEL(audience, anchor && anchor.name)
      }));
      localStorage.removeItem(DRAFT_KEY);
      setPending(false);
      onClose && onClose('published');
    } catch (e) {
      setFail(e && e.message || 'Publishing failed. Your draft is kept. Try again.');
      setPending(false);
    }
  };
  const preview = has ? /*#__PURE__*/React.createElement(__ds_scope.PostCard, {
    preview: true,
    c: c,
    author: post.author,
    authorKind: post.author_kind,
    avatarSrc: post.avatar,
    anchor: post.anchor,
    audience: __ds_scope.AUDIENCE_LABEL(audience, anchor && anchor.name),
    kicker: schema.kicker,
    title: convTitle,
    meta: convMeta,
    fields: form ? [] : rows,
    media: images.length > 1 ? {
      kind: 'gallery',
      items: images
    } : images.length === 1 ? {
      kind: 'image',
      src: images[0],
      alt: ''
    } : undefined,
    link: link,
    actions: !form && schema.action && /*#__PURE__*/React.createElement(__ds_scope.Button, {
      c: c,
      size: "sm"
    }, schema.action),
    respondLabel: c === 'convene' ? 'Ask the host' : 'Respond'
  }, text) : null;
  const diaTag = /*#__PURE__*/React.createElement("span", {
    style: {
      marginLeft: 8,
      fontSize: 11,
      letterSpacing: '0.06em',
      textTransform: 'uppercase',
      color: 'var(--ink-3)',
      fontWeight: 500
    }
  }, "DIA");
  const fieldEl = f => {
    const v = fv[f.key] || {};
    const lab = /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'inline-flex',
        alignItems: 'center'
      }
    }, f.label, v.value != null && v.value !== '' && !v.mine && diaTag);
    if (f.kind === 'toggle') return /*#__PURE__*/React.createElement(__ds_scope.Switch, {
      key: f.key,
      label: f.label,
      checked: !!v.value,
      onChange: x => setField(f.key, x),
      style: {
        width: '100%',
        paddingRight: 2
      }
    });
    if (f.kind === 'segment') return /*#__PURE__*/React.createElement("div", {
      key: f.key,
      style: {
        display: 'flex',
        flexDirection: 'column',
        gap: 6
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontSize: 15,
        fontWeight: 500,
        color: 'var(--ink-2)'
      }
    }, lab), /*#__PURE__*/React.createElement(__ds_scope.Segment, {
      label: f.label,
      options: f.options,
      value: v.value,
      onChange: o => setField(f.key, o)
    })); // 673: the Segment part, Composer its first caller
    return /*#__PURE__*/React.createElement(__ds_scope.Input, {
      key: f.key,
      label: lab,
      multiline: f.multiline,
      rows: 2,
      value: v.value || '',
      onChange: e => setField(f.key, e.target.value)
    });
  };
  const attach = /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 4,
      flexWrap: 'wrap'
    }
  }, touch ? /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    name: "image",
    label: "Add an image",
    active: picker,
    onClick: () => setPicker(p => !p),
    disabled: images.length >= maxImages
  }) : /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    name: "image",
    label: "Add an image",
    onClick: () => fileRef.current && fileRef.current.click(),
    disabled: images.length >= maxImages
  }), /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    name: "link",
    label: "Add a link",
    active: linkOpen,
    onClick: () => setLinkOpen(o => !o)
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), !touch && /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 13,
      color: armed ? 'var(--c-' + c + '-text)' : 'var(--ink-3)'
    }
  }, armed ? 'Drop to add' : 'Drop images on the text'), /*#__PURE__*/React.createElement("input", {
    ref: fileRef,
    type: "file",
    accept: "image/*",
    multiple: true,
    hidden: true,
    onChange: e => addFiles(e.target.files)
  }), /*#__PURE__*/React.createElement("input", {
    ref: camRef,
    type: "file",
    accept: "image/*",
    capture: "environment",
    hidden: true,
    onChange: e => addFiles(e.target.files)
  })), picker && touch && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Button, {
    variant: "secondary",
    onClick: () => camRef.current && camRef.current.click()
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "camera",
    size: 20
  }), "Take a photo"), /*#__PURE__*/React.createElement(__ds_scope.Button, {
    variant: "secondary",
    onClick: () => fileRef.current && fileRef.current.click()
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "images",
    size: 20
  }), "Choose from library")), linkOpen && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 8,
      alignItems: 'flex-end'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Input, {
    label: "Link",
    placeholder: "https://",
    value: url,
    onChange: e => setUrl(e.target.value),
    onKeyDown: e => {
      if (e.key === 'Enter') {
        e.preventDefault();
        addLink();
      }
    },
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement(__ds_scope.Button, {
    variant: "secondary",
    onClick: addLink,
    disabled: !url.trim()
  }, "Add")), images.length > 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 8,
      flexWrap: 'wrap'
    }
  }, images.map((src, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      position: 'relative',
      width: 72,
      height: 72,
      borderRadius: 10,
      overflow: 'hidden',
      border: '1px solid var(--line)'
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: src,
    alt: "",
    style: {
      width: '100%',
      height: '100%',
      objectFit: 'cover',
      display: 'block'
    }
  }), /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    name: "x",
    label: "Remove image",
    size: 44,
    onClick: () => setImages(s => s.filter((_, j) => j !== i)),
    style: {
      position: 'absolute',
      top: -8,
      right: -8,
      color: 'var(--on-fill)',
      background: 'var(--scrim)'
    }
  })))), link && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10,
      padding: '6px 6px 6px 12px',
      border: '1px solid var(--line)',
      borderRadius: 10,
      fontSize: 15
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "link",
    size: 18,
    style: {
      color: 'var(--ink-3)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      minWidth: 0,
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap'
    }
  }, link.title || link.domain), /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    name: "x",
    label: "Remove link",
    onClick: () => setLink(null)
  })));
  const dropProps = touch ? {} : {
    onDragEnter: e => {
      e.preventDefault();
      depth.current++;
      setArmed(true);
    },
    onDragOver: e => {
      e.preventDefault();
    },
    onDragLeave: () => {
      depth.current = Math.max(0, depth.current - 1);
      if (!depth.current) setArmed(false);
    },
    onDrop: e => {
      e.preventDefault();
      depth.current = 0;
      setArmed(false);
      addFiles(e.dataTransfer.files);
    }
  };
  const armedStyle = armed ? {
    outline: '1.5px dashed ' + (active ? 'var(--c-' + active + ')' : 'var(--ink)'),
    outlineOffset: 8,
    borderRadius: 'var(--radius-m)',
    background: active ? 'var(--c-' + active + '-tint)' : 'var(--bg-sunken)',
    transition: 'background var(--dur-default) var(--ease)'
  } : {};
  // Item 6: the verb's owner authors its form as content inside the locked shell. Falls back to VERB_SCHEMA's fields when no form is given for the verb.
  const mine = k => !!(fv[k] && fv[k].mine);
  // 672: touch only. A tap on a disabled chip changes no selection and shows its reason in the DiaLine slot; the next tap anywhere in the row clears it. Pointer keeps title and aria-description (correction 7). VerbChip is untouched: the tap is read at the row.
  const tapRow = e => {
    if (reasonShown) {
      setReasonShown(null);
      return;
    }
    const el = e.target.closest && e.target.closest('[role=radio]');
    if (!el || el.getAttribute('aria-disabled') !== 'true') return;
    const v = __ds_scope.C_ORDER[Array.prototype.indexOf.call(e.currentTarget.children, el)];
    if (v && disabledVerbs[v]) setReasonShown(disabledVerbs[v]);
  };
  const formBody = form ? form({
    fields: formFields,
    setField: formSetField,
    mine: formMine,
    tier,
    diaTag,
    proposed: !verb && !!proposed,
    reportValidity: setFormValid
  }) : schema.fields.map(fieldEl);
  const fields = /*#__PURE__*/React.createElement("fieldset", _extends({
    disabled: pending || undefined,
    "aria-busy": pending || undefined
  }, dropProps, {
    style: {
      all: 'unset',
      boxSizing: 'border-box',
      width: '100%',
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column',
      gap: 20,
      opacity: pending ? 0.6 : 1,
      transition: 'opacity var(--dur-default) var(--ease)',
      ...armedStyle
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("div", {
    role: "radiogroup",
    "aria-label": "What kind of post",
    onClick: touch ? tapRow : undefined,
    style: tier === 'compact' ? {
      display: 'flex',
      gap: 8,
      overflowX: 'auto',
      margin: '0 calc(-1 * var(--space-5))',
      padding: '2px var(--space-5)',
      scrollbarWidth: 'none'
    } : {
      display: 'flex',
      gap: 8,
      flexWrap: 'wrap'
    }
  }, __ds_scope.C_ORDER.map(v => /*#__PURE__*/React.createElement(__ds_scope.VerbChip, {
    key: v,
    c: v,
    compact: tier === 'compact',
    selected: verb === v,
    disabled: !!disabledVerbs[v],
    reason: disabledVerbs[v],
    onClick: () => choose(v)
  }))), reasonShown ? /*#__PURE__*/React.createElement("div", {
    role: "status",
    "aria-live": "polite",
    style: {
      display: 'flex',
      alignItems: 'center',
      minHeight: 24,
      fontFamily: 'var(--font-sans)',
      fontSize: 15,
      lineHeight: 1.4,
      color: 'var(--ink-3)'
    }
  }, reasonShown) : /*#__PURE__*/React.createElement(__ds_scope.DiaLine, {
    state: dia.state,
    text: dia.text,
    onNotThis: notThis
  })), /*#__PURE__*/React.createElement("textarea", {
    ref: taRef,
    "aria-label": "What is going on with you",
    placeholder: "What is going on with you?",
    value: text,
    onChange: e => setText(e.target.value),
    rows: tier === 'compact' ? 4 : 6,
    style: {
      width: '100%',
      boxSizing: 'border-box',
      border: 'none',
      outline: 'none',
      resize: 'none',
      background: 'transparent',
      color: 'var(--ink)',
      fontFamily: 'var(--font-sans)',
      fontSize: 19,
      lineHeight: 1.5,
      padding: 0,
      minHeight: 44
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 20
    }
  }, active && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 14,
      paddingTop: 4,
      borderTop: '1px solid var(--line)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      letterSpacing: '0.06em',
      textTransform: 'uppercase',
      fontWeight: 500,
      color: 'var(--c-' + active + '-text)',
      paddingTop: 12
    }
  }, __ds_scope.VERB_ACT[active]), formBody), attach, /*#__PURE__*/React.createElement(__ds_scope.AudienceSelect, {
    value: audience,
    onChange: setAudience,
    anchor: anchor && anchor.name
  })));
  const header = /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Avatar, {
    name: post.author,
    src: post.avatar,
    size: 32,
    style: space ? {
      borderRadius: 6
    } : undefined
  }), spaces.length ? /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      display: 'inline-flex',
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement("select", {
    "aria-label": "Post as",
    value: asSpace,
    onChange: e => setAsSpace(e.target.value),
    style: {
      appearance: 'none',
      WebkitAppearance: 'none',
      background: 'transparent',
      border: 'none',
      outline: 'none',
      fontFamily: 'var(--font-sans)',
      fontSize: 15,
      fontWeight: 700,
      color: 'var(--ink)',
      padding: '0 24px 0 4px',
      minHeight: 44,
      cursor: 'pointer'
    }
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, author.name), spaces.map((s, i) => /*#__PURE__*/React.createElement("option", {
    key: s.id != null ? s.id : 'space-' + i,
    value: s.id != null ? s.id : ''
  }, s.name))), /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "chevron-down",
    size: 16,
    style: {
      position: 'absolute',
      right: 4,
      color: 'var(--ink-3)',
      pointerEvents: 'none'
    }
  })) : /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 15,
      fontWeight: 700,
      padding: '0 4px'
    }
  }, author.name), anchor && /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 13,
      color: 'var(--ink-3)',
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap'
    }
  }, "in ", anchor.name), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), drafted && has && /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 13,
      color: 'var(--ink-3)'
    }
  }, "Draft saved"));

  // 664: a supplied form gates Publish on its report alone. 668: proposed alone never publishes. VERB_SCHEMA verbs keep `has`.
  const canPublish = !pending && !(proposed && !verb) && (form ? formValid : has);
  const publishBtn = /*#__PURE__*/React.createElement(__ds_scope.Button, {
    c: active || undefined,
    disabled: !canPublish,
    "aria-busy": pending || undefined,
    onClick: publish,
    full: tier === 'compact'
  }, pending ? 'Publishing' : 'Publish');
  const col = {
    width: '100%',
    maxWidth: 'var(--content-max, 680px)',
    margin: '0 auto',
    boxSizing: 'border-box'
  };
  const footerRow = tier === 'compact' ? /*#__PURE__*/React.createElement("div", {
    style: col
  }, publishBtn) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 13,
      color: 'var(--ink-3)',
      marginRight: 'auto'
    }
  }, "\u2318 Enter to publish \xB7 Esc to close"), publishBtn);
  const previewBlock = preview && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      letterSpacing: '0.06em',
      textTransform: 'uppercase',
      fontWeight: 500,
      color: 'var(--ink-3)'
    }
  }, "How it will appear"), preview);

  // 636: Sheet owns the chrome. tier and size="composer" select 584/611 geometry; title and footer are Sheet's slots; Sheet's body padding is the only gutter.
  return /*#__PURE__*/React.createElement(__ds_scope.Sheet, {
    open: open,
    onClose: onClose,
    tier: tier,
    size: "composer",
    label: "Compose",
    contained: contained,
    title: header,
    footer: footerRow,
    error: fail,
    style: kb ? {
      height: 'calc(100% - ' + kb + 'px)',
      paddingBottom: 0
    } : undefined
  }, tier === 'expanded' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(auto-fit, minmax(400px, 1fr))',
      gap: 'var(--space-6)',
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, fields), previewBlock && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'sticky',
      top: 0,
      minWidth: 0
    }
  }, previewBlock)) : /*#__PURE__*/React.createElement("div", {
    style: {
      ...col,
      display: 'flex',
      flexDirection: 'column',
      gap: 24
    }
  }, fields, previewBlock));
}
Object.assign(__ds_scope, { VERB_SCHEMA, Composer });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dna/Composer.jsx", error: String((e && e.message) || e) }); }

// ui_kits/dna/app.jsx
try { (() => {
function App() {
  const [desktop, setDesktop] = React.useState(false);
  const [route, setRoute] = React.useState({
    name: 'feed',
    data: null
  });
  const [composer, setComposer] = React.useState(false);
  const go = (name, data) => {
    if (name === 'composer') return setComposer(true);
    setRoute({
      name,
      data
    });
  };
  const active = C_OF[route.name];
  const states = {
    connect: 'activity',
    convene: 'urgent',
    convey: 'for-you'
  };
  const screens = {
    feed: Feed,
    directory: Directory,
    profile: Profile,
    event: EventPage,
    space: SpacePage,
    opportunity: OpportunityPage,
    story: StoryPage,
    notifications: Notifications,
    thread: Thread
  };
  const Screen = screens[route.name] || Feed;
  const onSelect = c => setRoute({
    name: HOME[c],
    data: null
  });
  const frame = desktop ? {
    width: '100%',
    minHeight: '100vh'
  } : {
    width: 390,
    height: 844,
    borderRadius: 36,
    border: '8px solid var(--ink)',
    overflow: 'hidden',
    boxShadow: 'var(--shadow-3)'
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      minHeight: '100vh',
      background: desktop ? 'var(--bg)' : 'var(--bg-sunken)',
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      padding: desktop ? 0 : '24px 0 40px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'fixed',
      bottom: 16,
      right: 16,
      zIndex: 100
    }
  }, /*#__PURE__*/React.createElement(LensBar, {
    lenses: [{
      id: 'm',
      label: 'Mobile'
    }, {
      id: 'd',
      label: 'Desktop'
    }],
    value: desktop ? 'd' : 'm',
    onChange: v => setDesktop(v === 'd')
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      ...frame,
      background: 'var(--bg)',
      position: 'relative',
      display: 'flex',
      flexDirection: 'column'
    }
  }, desktop && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'sticky',
      top: 0,
      zIndex: 30,
      display: 'flex',
      alignItems: 'center',
      background: 'var(--bg)',
      borderBottom: '1px solid var(--line)'
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: "../../assets/logo.png",
    alt: "DNA",
    style: {
      height: 28,
      margin: '0 24px'
    },
    onClick: () => go('feed')
  }), /*#__PURE__*/React.createElement(PulseDock, {
    bar: true,
    active: active,
    states: states,
    onSelect: onSelect,
    style: {
      borderBottom: 'none',
      flex: 1
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 4,
      marginRight: 16
    }
  }, /*#__PURE__*/React.createElement(IconButton, {
    name: "bell",
    label: "Notifications",
    onClick: () => go('notifications')
  }), /*#__PURE__*/React.createElement(IconButton, {
    name: "message-circle",
    label: "Messages",
    onClick: () => go('thread')
  }), /*#__PURE__*/React.createElement(Button, {
    c: active || undefined,
    size: "sm",
    onClick: () => setComposer(true)
  }, "Do something"))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      overflowY: desktop ? 'visible' : 'auto',
      width: '100%',
      maxWidth: desktop ? 720 : '100%',
      margin: '0 auto',
      paddingBottom: desktop ? 40 : 80
    }
  }, /*#__PURE__*/React.createElement(Screen, {
    key: route.name + (route.data ? route.data.name : ''),
    go: go,
    desktop: desktop,
    member: route.data,
    openComposer: () => setComposer(true)
  })), !desktop && /*#__PURE__*/React.createElement(PulseDock, {
    active: active,
    states: states,
    onSelect: onSelect,
    style: {
      position: 'absolute',
      bottom: 0,
      left: 0,
      right: 0
    }
  }), /*#__PURE__*/React.createElement(FeedComposerMock, {
    open: composer,
    onClose: () => setComposer(false),
    go: go,
    desktop: desktop
  })));
}
ReactDOM.createRoot(document.getElementById('root')).render(/*#__PURE__*/React.createElement(App, null));
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/dna/app.jsx", error: String((e && e.message) || e) }); }

// ui_kits/dna/collaborate.jsx
try { (() => {
function SpacePage({
  go,
  desktop
}) {
  const [tab, setTab] = React.useState('work');
  const [joined, setJoined] = React.useState(false);
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(TopBar, {
    back: true,
    onBack: () => go('feed'),
    title: "Space",
    desktop: desktop,
    right: /*#__PURE__*/React.createElement(IconButton, {
      name: "ellipsis",
      label: "More"
    })
  }), /*#__PURE__*/React.createElement(PageHead, {
    c: "collaborate",
    title: "Coastal Clinics Cold Chain",
    meta: "Getting vaccine refrigeration into twelve clinics between Takoradi and Cape Coast by March."
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 8,
      marginTop: 4
    }
  }, joined ? /*#__PURE__*/React.createElement(Button, {
    c: "collaborate",
    variant: "secondary",
    full: true,
    onClick: () => setJoined(false)
  }, "In this Space") : /*#__PURE__*/React.createElement(Button, {
    c: "collaborate",
    full: true,
    onClick: () => setJoined(true)
  }, "Join Space"), /*#__PURE__*/React.createElement(Button, {
    variant: "secondary",
    onClick: () => go('thread', {
      name: 'Coastal Clinics Cold Chain'
    })
  }, "Discuss"))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '8px 16px 0'
    }
  }, /*#__PURE__*/React.createElement(Tabs, {
    c: "collaborate",
    items: [{
      id: 'work',
      label: 'Work'
    }, {
      id: 'needs',
      label: 'Open needs'
    }, {
      id: 'people',
      label: 'People'
    }],
    value: tab,
    onChange: setTab
  })), tab === 'work' && /*#__PURE__*/React.createElement(Section, null, /*#__PURE__*/React.createElement(Card, null, /*#__PURE__*/React.createElement(ListRow, {
    leading: /*#__PURE__*/React.createElement(Icon, {
      name: "circle-check",
      size: 20,
      style: {
        color: 'var(--c-collaborate)'
      }
    }),
    title: "Site survey, Takoradi",
    sub: "Done. Report in files."
  }), /*#__PURE__*/React.createElement(ListRow, {
    leading: /*#__PURE__*/React.createElement(Icon, {
      name: "circle-dot",
      size: 20,
      style: {
        color: 'var(--ink-3)'
      }
    }),
    title: "Procurement shortlist",
    sub: "In progress, Kwame Mensah"
  }), /*#__PURE__*/React.createElement(ListRow, {
    leading: /*#__PURE__*/React.createElement(Icon, {
      name: "circle",
      size: 20,
      style: {
        color: 'var(--ink-4)'
      }
    }),
    title: "Cold chain training plan",
    sub: "Not started"
  }))), tab === 'needs' && /*#__PURE__*/React.createElement(Section, null, /*#__PURE__*/React.createElement(PostCard, {
    c: "contribute",
    author: "Amara Osei",
    meta: "Space lead. Posted this week",
    kicker: "Need",
    title: "Refrigeration procurement review",
    actions: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Button, {
      c: "contribute",
      size: "sm",
      onClick: () => go('opportunity')
    }, "Offer help"), /*#__PURE__*/React.createElement(Button, {
      c: "connect",
      variant: "secondary",
      size: "sm"
    }, "Make an intro")),
    respondLabel: "Ask a question"
  }, "Two weeks reviewing supplier quotes for solar direct-drive fridges. Remote. Attested on completion.")), tab === 'people' && /*#__PURE__*/React.createElement(Section, null, /*#__PURE__*/React.createElement(Card, null, /*#__PURE__*/React.createElement(ListRow, {
    leading: /*#__PURE__*/React.createElement(Avatar, {
      name: "Amara Osei",
      src: IMG,
      size: 40
    }),
    title: "Amara Osei",
    sub: "Lead. Accra, GMT",
    onClick: () => go('profile', MEMBERS[0])
  }), /*#__PURE__*/React.createElement(ListRow, {
    leading: /*#__PURE__*/React.createElement(Avatar, {
      name: "Thandiwe Dube",
      size: 40
    }),
    title: "Thandiwe Dube",
    sub: "Solar engineering. Johannesburg",
    onClick: () => go('profile', MEMBERS[3])
  }), /*#__PURE__*/React.createElement(ListRow, {
    leading: /*#__PURE__*/React.createElement(Avatar, {
      name: "Kwame Mensah",
      size: 40
    }),
    title: "Kwame Mensah",
    sub: "Procurement. Houston",
    onClick: () => go('profile', MEMBERS[1])
  }))));
}
Object.assign(window, {
  SpacePage
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/dna/collaborate.jsx", error: String((e && e.message) || e) }); }

// ui_kits/dna/composer/dia-mock.js
try { (() => {
// Mock DIA for the prototype. Production inference belongs to Claude Code; only the contract matters here:
// resolve { c, fields, line } or null (silence). Latency 900–1600ms, inside the 2.5s budget.
const RULES = [{
  c: 'convene',
  re: /\b(event|dinner|meetup|gather|workshop|host(ing)?|join us|doors|tickets?|panel|screening|brunch)\b/i
}, {
  c: 'collaborate',
  re: /\b(space|working group|project|start(ing)? a|build(ing)? together|cohort|initiative|team up)\b/i
}, {
  c: 'contribute',
  re: /\b(need|looking for|volunteer|help with|mentor|donat|in-kind|can anyone|who can)\b/i
}, {
  c: 'connect',
  re: /\b(intro|introduce|connect|meet|put .* in touch|know anyone|reach)\b/i
}, {
  c: 'convey',
  re: /\b(story|wrote|reflection|thread|piece|essay|remember|lesson|learned)\b/i
}];
const MONTHS = 'Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec';
function extract(c, t) {
  const f = {};
  const first = t.split(/[.!?\n]/)[0].trim();
  const date = t.match(new RegExp('\\b((Mon|Tue|Wed|Thu|Fri|Sat|Sun)[a-z]*\\s+)?(\\d{1,2}\\s+(' + MONTHS + ')[a-z]*|(' + MONTHS + ')[a-z]*\\s+\\d{1,2})', 'i'));
  const time = t.match(/\b(\d{1,2}(:\d{2})?\s?(am|pm)|\d{1,2}:\d{2})\b/i);
  const place = t.match(/\b(?:in|at)\s+([A-Z][\w']+(?:\s+[A-Z][\w']+)*)/);
  if (c === 'convene') {
    const m = t.match(/(?:hosting|host)\s+(?:a |an |the )?([^.,\n]+?)(?=\s+(?:in|at|on)\b|[.,\n]|$)/i);
    f.title = m ? m[1].trim() : first.length < 70 ? first.replace(/^(we are|we're|i am|i'm)\s+/i, '') : undefined;
    if (!f.title) delete f.title;
    if (date) f.date = date[0];
    if (time) f.time = time[0];
    if (place) f.place = place[1];
    f.ticket = /\b(paid|ticket|kes|usd|\$)\b/i.test(t) && !/\bfree\b/i.test(t) ? 'Paid' : 'Free';
    if (/\b(online|zoom|hybrid|stream)/i.test(t)) f.hybrid = true;
  }
  if (c === 'collaborate') {
    const m = t.match(/(?:space|group|project)\s+(?:for|on|about|called)\s+([^.,\n]+)/i);
    if (m) f.title = m[1].trim();
    const r = t.match(/(?:looking for|need|want)\s+(?:a |an |some )?([^.,\n]+)/i);
    if (r) f.roles = r[1].trim();
    if (/\b(health|clinic|medic)/i.test(t)) f.category = 'Health';else if (/\b(farm|harvest|agri|cassava)/i.test(t)) f.category = 'Agriculture';else if (/\b(school|teach|educat)/i.test(t)) f.category = 'Education';else if (/\b(fund|invest|capital|finance)/i.test(t)) f.category = 'Finance';
  }
  if (c === 'contribute') {
    const m = t.match(/(?:need|looking for)\s+([^.,\n]+)/i);
    if (m) {
      f.need = m[1].trim();
      f.title = f.need.charAt(0).toUpperCase() + f.need.slice(1);
    }
    f.instrument = /\b(mentor|advis|review|design|legal|account)/i.test(t) ? 'Skills' : /\b(laptop|equipment|books|seeds|space to|venue)/i.test(t) ? 'In-kind' : 'Time';
    const by = t.match(/\bby\s+([^.,\n]+)/i);
    if (by) f.by = by[1].trim();
  }
  if (c === 'connect') {
    const w = t.match(/(?:intro(?:duce)?(?: me)?\s+to|meet|know anyone|connect (?:me )?(?:with|to))\s+([^.,\n]+)/i);
    if (w) f.who = w[1].trim();
    const y = t.match(/\b(?:because|so that|to help|who can)\s+([^.\n]+)/i);
    if (y) f.why = y[1].trim();
  }
  if (c === 'convey') {
    if (first.length < 80) f.title = first;
  }
  return f;
}
const LINE = {
  connect: 'DIA read this as an Intro.',
  convene: 'DIA read this as an Event.',
  collaborate: 'DIA read this as a Space.',
  contribute: 'DIA read this as a Need.',
  convey: 'DIA read this as a Story.'
};
window.diaInfer = (opts = {}) => text => new Promise(res => {
  const delay = 900 + Math.random() * 700;
  setTimeout(() => {
    if (opts.silent) return res(null);
    const hit = RULES.find(r => r.re.test(text));
    if (!hit) return res(null);
    res({
      c: hit.c,
      fields: extract(hit.c, text),
      line: LINE[hit.c]
    });
  }, delay);
});
window.mockUnfurl = url => new Promise(res => setTimeout(() => {
  const known = {
    'theafricareport.com': {
      title: 'Diaspora remittances reach a new high in 2026',
      image: '../../../assets/imagery/hero-professional.jpeg'
    },
    'nation.africa': {
      title: 'Nairobi to host continental builders summit'
    }
  };
  let host = '';
  try {
    host = new URL(url.startsWith('http') ? url : 'https://' + url).hostname.replace(/^www\./, '');
  } catch {}
  res(known[host] ? {
    domain: host,
    ...known[host]
  } : null);
}, 900));
// Review presets: one populated text per verb, plus fields the member has typed (mine) where the story needs them.
window.PRESETS = {
  connect: {
    text: 'Can someone introduce me to Kwame Mensah at the Accra fintech hub? I am building a savings product for market traders and he has done this before.'
  },
  convene: {
    text: 'We are hosting a Diaspora Builders Dinner in Nairobi on Thu 16 Oct at 19:00. Doors at 18:30. Free for members, bring one person who should be in the room.'
  },
  collaborate: {
    text: 'Starting a Space for diaspora health workers who want to run short clinics back home. Looking for two coordinators and one person who knows Kenyan licensing.'
  },
  contribute: {
    text: 'We need a volunteer accountant to review the cooperative books before the audit by 30 Nov. Two evenings, remote is fine.'
  },
  convey: {
    text: 'Three intros that changed a harvest. I wrote down how a cassava cooperative in Oyo found its first buyer through two members in Houston.'
  },
  system: {
    text: 'Back in Nairobi after three weeks in Houston. The jet lag is winning and the mangoes are not.'
  }
};
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/dna/composer/dia-mock.js", error: String((e && e.message) || e) }); }

// ui_kits/dna/composer/proto.jsx
try { (() => {
const DS = window.StrandDNADesignSystem_3654dd;
const {
  Button,
  IconButton,
  Icon,
  Avatar,
  CBadge,
  PulseDock,
  PostCard,
  VerbChip,
  Composer,
  Toast
} = DS;
const IMG = '../../../assets/imagery/hero-professional.jpeg';
const ME = {
  name: 'Amara Osei'
};
const SPACES = [{
  id: 'nbo',
  name: 'Nairobi chapter'
}, {
  id: 'health',
  name: 'Diaspora health workers'
}];
const C_ORDER = ['connect', 'convene', 'collaborate', 'contribute', 'convey'];
const VERBS = ['connect', 'convene', 'collaborate', 'contribute', 'convey', 'system'];

/* The host surface: a Feed with the composer entry. Enough to open the composer from, nothing more. */
function Feed({
  compact,
  posts,
  onOpen,
  onOpenVerb
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12,
      padding: compact ? '12px 16px 96px' : '24px 32px 48px',
      maxWidth: 680,
      margin: '0 auto',
      width: '100%',
      boxSizing: 'border-box'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      background: 'var(--surface)',
      border: '1px solid var(--line)',
      borderRadius: 14,
      padding: 12,
      display: 'flex',
      flexDirection: 'column',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => onOpen(),
    style: {
      all: 'unset',
      boxSizing: 'border-box',
      cursor: 'text',
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      minHeight: 44,
      padding: '0 4px'
    }
  }, /*#__PURE__*/React.createElement(Avatar, {
    name: ME.name,
    size: 36
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      fontFamily: 'var(--font-sans)',
      fontSize: 17,
      color: 'var(--ink-3)'
    }
  }, "What is going on with you?")), /*#__PURE__*/React.createElement("div", {
    role: "group",
    "aria-label": "Start with a verb",
    style: {
      display: 'flex',
      gap: 8,
      overflowX: 'auto',
      margin: '0 -12px',
      padding: '0 12px',
      scrollbarWidth: 'none'
    }
  }, C_ORDER.map(c => /*#__PURE__*/React.createElement(VerbChip, {
    key: c,
    c: c,
    compact: true,
    onClick: () => onOpenVerb(c)
  })))), posts.map((p, i) => /*#__PURE__*/React.createElement(PostCard, {
    key: i,
    c: p.c_category,
    author: p.author,
    authorKind: p.author_kind,
    avatarSrc: p.avatar,
    meta: p.meta,
    anchor: p.anchor,
    audience: p.audienceLabel,
    kicker: p.kicker,
    title: p.title,
    fields: p.fields,
    media: p.media && p.media.length > 1 ? {
      kind: 'gallery',
      items: p.media
    } : p.media && p.media.length === 1 ? {
      kind: 'image',
      src: p.media[0],
      alt: ''
    } : p.mediaBlock,
    link: p.link_unfurl,
    actions: p.action && /*#__PURE__*/React.createElement(Button, {
      c: p.c_category === 'system' ? undefined : p.c_category,
      size: "sm"
    }, p.action),
    respondLabel: p.c_category === 'convene' ? 'Ask the host' : 'Respond'
  }, p.body)));
}
const SEED = [{
  c_category: 'convene',
  author: 'Nairobi chapter',
  author_kind: 'space',
  meta: 'Nairobi, EAT. Thu 16 Oct, 19:00',
  kicker: 'Event',
  title: 'Diaspora Builders Dinner',
  body: 'Tickets open to members. Doors at 18:30.',
  mediaBlock: {
    kind: 'image',
    src: IMG,
    alt: ''
  },
  action: 'Get a ticket'
}, {
  c_category: 'convey',
  author: 'Adaeze Nwosu',
  meta: 'Lagos, WAT. Daily Edition',
  kicker: 'Story',
  title: 'Three intros that changed a harvest',
  body: 'How a cassava cooperative in Oyo found its first buyer through two members in Houston.',
  action: 'Read the story'
}];
function Frame({
  theme,
  tier,
  children
}) {
  const compact = tier === 'compact';
  const w = compact ? 390 : tier === 'medium' ? 900 : 1280,
    h = compact ? 844 : 800;
  const ref = React.useRef();
  const [scale, setScale] = React.useState(1);
  React.useEffect(() => {
    const ro = new ResizeObserver(([e]) => setScale(Math.min(1, (e.contentRect.width - 8) / w)));
    ro.observe(ref.current);
    return () => ro.disconnect();
  }, [w]);
  return /*#__PURE__*/React.createElement("div", {
    ref: ref,
    style: {
      width: '100%',
      display: 'flex',
      justifyContent: 'center'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: w * scale,
      height: h * scale,
      flex: 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    "data-theme": theme,
    style: {
      width: w,
      height: h,
      transform: 'scale(' + scale + ')',
      transformOrigin: 'top left',
      position: 'relative',
      overflow: 'hidden',
      background: 'var(--bg)',
      color: 'var(--ink)',
      borderRadius: compact ? 44 : 12,
      border: '1px solid var(--line-strong)',
      boxSizing: 'border-box',
      display: 'flex',
      flexDirection: 'column',
      fontFamily: 'var(--font-sans)',
      '--safe-top': compact ? '47px' : '0px',
      '--safe-bottom': compact ? '34px' : '0px'
    }
  }, children)));
}
function App() {
  const [theme, setTheme] = React.useState(localStorage.getItem('dna.proto.theme') || 'light');
  const [tier, setTier] = React.useState(localStorage.getItem('dna.proto.tier') || 'compact');
  const [silent, setSilent] = React.useState(false);
  const [inEvent, setInEvent] = React.useState(false); // item 7: the chip disabled inside an event (53)
  const [failNext, setFailNext] = React.useState(false); // item 8: publish fails, draft kept
  const [open, setOpen] = React.useState(false);
  const [seedKey, setSeedKey] = React.useState(0);
  const [initial, setInitial] = React.useState(null);
  const [initialVerb, setInitialVerb] = React.useState(null);
  const [anchored, setAnchored] = React.useState(false);
  const [posts, setPosts] = React.useState(SEED);
  const [toast, setToast] = React.useState(null);
  React.useEffect(() => {
    localStorage.setItem('dna.proto.theme', theme);
    localStorage.setItem('dna.proto.tier', tier);
  }, [theme, tier]);
  const compact = tier === 'compact';
  const anchor = anchored ? {
    kind: 'space',
    name: 'Nairobi chapter'
  } : undefined;
  const infer = React.useMemo(() => window.diaInfer({
    silent
  }), [silent]);
  const launch = (init, verb) => {
    setInitial(init || null);
    setInitialVerb(verb || null);
    setSeedKey(k => k + 1);
    setOpen(true);
  };
  const jump = async state => {
    if (state === 'empty') return launch({
      text: ''
    });
    if (state === 'thinking') return launch({
      text: window.PRESETS.convene.text,
      hold: true,
      dia: {
        state: 'thinking'
      }
    });
    if (state === 'silence') {
      setSilent(true);
      return launch({
        text: window.PRESETS.system.text,
        overridden: false,
        dia: {
          state: null
        }
      });
    }
    const p = window.PRESETS[state];
    if (!p) return;
    if (state === 'system') return launch({
      text: p.text,
      verb: null,
      overridden: true,
      dia: {
        state: null
      }
    });
    const res = await window.diaInfer()(p.text).then(r => r);
    // 635: the read proposes; no chip is selected until the member taps one.
    launch({
      text: p.text,
      proposed: state,
      overridden: false,
      fields: Object.fromEntries(Object.entries(res.fields).map(([k, v]) => [k, {
        value: v,
        mine: k === 'time' || k === 'roles'
      }])),
      dia: {
        state: 'done',
        text: res.line
      },
      images: state === 'convene' ? [IMG] : [],
      hold: true
    });
  };
  const publish = post => new Promise((ok, no) => setTimeout(() => {
    if (failNext) return no(new Error('Publishing failed. Your draft is kept. Try again.'));
    setPosts(s => [{
      ...post,
      meta: 'Just now'
    }, ...s]);
    ok();
  }, 900));
  const close = reason => {
    setOpen(false);
    if (reason === 'published') {
      setToast('Published. It is in the Feed.');
      setTimeout(() => setToast(null), 2600);
    }
  }; // 666: the Composer closes itself on resolve; B1 reacts in onClose
  const disabledVerbs = inEvent ? {
    convene: 'You are inside an event. Events are hosted from the Feed.'
  } : undefined;
  const ctl = {
    fontSize: 13,
    fontFamily: 'var(--font-sans)'
  };
  const Seg = ({
    value,
    onChange,
    options
  }) => /*#__PURE__*/React.createElement("div", {
    role: "radiogroup",
    style: {
      display: 'inline-flex',
      border: '1px solid var(--line)',
      borderRadius: 999,
      padding: 2,
      background: 'var(--surface)'
    }
  }, options.map(([v, l]) => /*#__PURE__*/React.createElement("button", {
    key: v,
    type: "button",
    role: "radio",
    "aria-checked": value === v,
    onClick: () => onChange(v),
    style: {
      all: 'unset',
      cursor: 'pointer',
      padding: '0 12px',
      minHeight: 32,
      display: 'inline-flex',
      alignItems: 'center',
      borderRadius: 999,
      fontSize: 13,
      fontWeight: 500,
      background: value === v ? 'var(--ink)' : 'transparent',
      color: value === v ? 'var(--on-fill)' : 'var(--ink-2)'
    }
  }, l)));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      minHeight: '100vh',
      background: 'var(--bg-sunken)',
      display: 'flex',
      flexDirection: 'column',
      gap: 16,
      padding: '16px 12px 32px',
      boxSizing: 'border-box',
      fontFamily: 'var(--font-sans)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      alignItems: 'center',
      gap: 10,
      ...ctl
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      letterSpacing: '0.06em',
      textTransform: 'uppercase',
      color: 'var(--ink-3)',
      fontWeight: 500
    }
  }, "Review"), /*#__PURE__*/React.createElement(Seg, {
    value: theme,
    onChange: setTheme,
    options: [['light', 'Light'], ['dark', 'Dark']]
  }), /*#__PURE__*/React.createElement(Seg, {
    value: tier,
    onChange: setTier,
    options: [['compact', 'Compact 390, touch'], ['medium', 'Medium 900, pointer'], ['expanded', 'Expanded 1280, pointer']]
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--ink-3)'
    }
  }, "Jump to"), /*#__PURE__*/React.createElement(Seg, {
    value: null,
    onChange: jump,
    options: [['empty', 'Empty'], ['thinking', 'Thinking'], ['silence', 'Silence']]
  }), /*#__PURE__*/React.createElement(Seg, {
    value: null,
    onChange: jump,
    options: VERBS.map(v => [v, v === 'system' ? 'System' : v[0].toUpperCase() + v.slice(1)])
  }), /*#__PURE__*/React.createElement("label", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6,
      cursor: 'pointer'
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: anchored,
    onChange: e => setAnchored(e.target.checked)
  }), "Opened inside Nairobi chapter"), /*#__PURE__*/React.createElement("label", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6,
      cursor: 'pointer'
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: silent,
    onChange: e => setSilent(e.target.checked)
  }), "DIA has nothing"), /*#__PURE__*/React.createElement("label", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6,
      cursor: 'pointer'
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: inEvent,
    onChange: e => setInEvent(e.target.checked)
  }), "Inside an event"), /*#__PURE__*/React.createElement("label", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6,
      cursor: 'pointer'
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: failNext,
    onChange: e => setFailNext(e.target.checked)
  }), "Publish fails")), /*#__PURE__*/React.createElement(Frame, {
    theme: theme,
    tier: tier
  }, compact && /*#__PURE__*/React.createElement("div", {
    style: {
      height: 47,
      flex: 'none',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      fontSize: 15,
      fontWeight: 700
    }
  }, "9:41"), !compact && /*#__PURE__*/React.createElement(PulseDock, {
    bar: true,
    active: "convene",
    states: {
      connect: 'activity',
      convey: 'for-you'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      overflowY: 'auto'
    }
  }, /*#__PURE__*/React.createElement(Feed, {
    compact: compact,
    posts: posts,
    onOpen: () => launch(null),
    onOpenVerb: v => launch(null, v)
  })), compact && /*#__PURE__*/React.createElement(PulseDock, {
    active: "convey",
    states: {
      connect: 'activity',
      convey: 'for-you'
    },
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      bottom: 0,
      paddingBottom: 'var(--safe-bottom)'
    }
  }), compact && /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      bottom: 8,
      left: '50%',
      width: 134,
      height: 5,
      marginLeft: -67,
      borderRadius: 3,
      background: 'var(--ink)',
      opacity: 0.9,
      zIndex: 60,
      pointerEvents: 'none'
    }
  }), toast && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      bottom: compact ? 88 : 24,
      display: 'flex',
      justifyContent: 'center',
      zIndex: 70,
      pointerEvents: 'none'
    }
  }, /*#__PURE__*/React.createElement(Toast, null, toast)), /*#__PURE__*/React.createElement(Composer, {
    key: seedKey,
    open: open,
    onClose: close,
    onPublish: publish,
    tier: tier,
    author: ME,
    spaces: SPACES,
    anchor: anchor,
    infer: infer,
    unfurl: window.mockUnfurl,
    initial: initial,
    initialVerb: initialVerb,
    disabledVerbs: disabledVerbs,
    contained: true
  })));
}
ReactDOM.createRoot(document.getElementById('root')).render(/*#__PURE__*/React.createElement(App, null));
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/dna/composer/proto.jsx", error: String((e && e.message) || e) }); }

// ui_kits/dna/connect.jsx
try { (() => {
const MEMBERS = [{
  name: 'Amara Osei',
  city: 'Accra, GMT',
  role: 'Public health, cold chain logistics',
  tier: 'Steward',
  photo: IMG
}, {
  name: 'Kwame Mensah',
  city: 'Houston, CST',
  role: 'Agri-finance, cooperatives',
  tier: 'Member'
}, {
  name: 'Adaeze Nwosu',
  city: 'Lagos, WAT',
  role: 'Editor, Daily Edition',
  tier: 'Member'
}, {
  name: 'Thandiwe Dube',
  city: 'Johannesburg, SAST',
  role: 'Solar engineering',
  tier: 'Founding member'
}, {
  name: 'Yusuf Diallo',
  city: 'Paris, CET',
  role: 'Trade law',
  tier: 'Member'
}];
function Directory({
  go,
  desktop
}) {
  const [lens, setLens] = React.useState('all');
  const [filters, setFilters] = React.useState(['Health']);
  const list = lens === 'city' ? [] : lens === 'net' ? MEMBERS.slice(0, 2) : MEMBERS;
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(TopBar, {
    title: "Connect",
    desktop: desktop,
    right: /*#__PURE__*/React.createElement(IconButton, {
      name: "search",
      label: "Search members"
    })
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 16px 0',
      display: 'flex',
      flexDirection: 'column',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement(LensBar, {
    lenses: [{
      id: 'all',
      label: 'Everyone',
      icon: 'globe'
    }, {
      id: 'net',
      label: 'My network',
      icon: 'users'
    }, {
      id: 'city',
      label: 'My city',
      icon: 'map-pin'
    }],
    value: lens,
    onChange: setLens
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 8,
      flexWrap: 'wrap'
    }
  }, ['Health', 'Agriculture', 'Energy', 'Open to intros'].map(f => /*#__PURE__*/React.createElement(Chip, {
    key: f,
    selected: filters.includes(f),
    onClick: () => setFilters(s => s.includes(f) ? s.filter(x => x !== f) : [...s, f])
  }, f)))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 16,
      display: 'flex',
      flexDirection: 'column',
      gap: 12
    }
  }, list.length === 0 ? /*#__PURE__*/React.createElement(EmptyState, {
    c: "connect",
    title: "Nobody in this lens yet.",
    body: "Widen the lens or make an intro.",
    action: /*#__PURE__*/React.createElement(Button, {
      c: "connect",
      onClick: () => go('composer')
    }, "Make an Intro")
  }) : list.map(m => /*#__PURE__*/React.createElement(Card, {
    key: m.name,
    onClick: () => go('profile', m)
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 12,
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement(Avatar, {
    name: m.name,
    src: m.photo,
    size: 48
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 17,
      fontWeight: 700,
      lineHeight: 1.3
    }
  }, m.name), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 15,
      color: 'var(--ink-2)',
      lineHeight: 1.4
    }
  }, m.role), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      color: 'var(--ink-3)',
      marginTop: 2
    }
  }, m.city)), /*#__PURE__*/React.createElement(Button, {
    c: "connect",
    variant: "secondary",
    size: "sm",
    onClick: e => {
      e.stopPropagation();
      go('thread', m);
    }
  }, "Intro"))))));
}
function Profile({
  go,
  desktop,
  member
}) {
  const m = member || MEMBERS[0];
  const [tab, setTab] = React.useState('about');
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(TopBar, {
    back: true,
    onBack: () => go('directory'),
    title: "",
    desktop: desktop,
    right: /*#__PURE__*/React.createElement(IconButton, {
      name: "ellipsis",
      label: "More"
    })
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '8px 16px 0',
      display: 'flex',
      flexDirection: 'column',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 14,
      alignItems: 'flex-start'
    }
  }, /*#__PURE__*/React.createElement(Avatar, {
    name: m.name,
    src: m.photo,
    size: 72
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: 0,
      fontFamily: 'var(--font-display)',
      fontWeight: 400,
      fontSize: 26,
      lineHeight: 1.2
    }
  }, m.name), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 15,
      color: 'var(--ink-2)'
    }
  }, m.role), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 8,
      alignItems: 'center',
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement(TierBadge, {
    tier: m.tier
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 13,
      color: 'var(--ink-3)'
    }
  }, m.city)))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement(Button, {
    c: "connect",
    full: true,
    onClick: () => go('thread', m)
  }, "Make an intro"), /*#__PURE__*/React.createElement(Button, {
    variant: "secondary",
    full: true,
    onClick: () => go('thread', m)
  }, "Message")), /*#__PURE__*/React.createElement(Tabs, {
    c: "connect",
    items: [{
      id: 'about',
      label: 'About'
    }, {
      id: 'activity',
      label: 'Completed activity'
    }, {
      id: 'contrib',
      label: 'Attested contributions'
    }],
    value: tab,
    onChange: setTab
  })), tab === 'about' && /*#__PURE__*/React.createElement(Section, null, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 17,
      lineHeight: 1.5
    }
  }, "Works on last-mile vaccine cold chains across coastal Ghana. Looking for members with logistics software or refrigeration procurement experience. Happy to make intros into Accra's public health ministry."), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 8,
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement(Chip, {
    c: "collaborate"
  }, "Health"), /*#__PURE__*/React.createElement(Chip, {
    c: "collaborate"
  }, "Logistics"), /*#__PURE__*/React.createElement(Chip, {
    c: "connect"
  }, "Open to intros"))), tab === 'activity' && /*#__PURE__*/React.createElement(Section, null, /*#__PURE__*/React.createElement(Card, null, /*#__PURE__*/React.createElement(ListRow, {
    leading: /*#__PURE__*/React.createElement(CBadge, {
      c: "convene"
    }),
    title: "Hosted Diaspora Builders Dinner",
    sub: "Accra, Sep 2026",
    onClick: () => go('event')
  }), /*#__PURE__*/React.createElement(ListRow, {
    leading: /*#__PURE__*/React.createElement(CBadge, {
      c: "collaborate"
    }),
    title: "Lead, Coastal Clinics Cold Chain",
    sub: "Space, active",
    onClick: () => go('space')
  }), /*#__PURE__*/React.createElement(ListRow, {
    leading: /*#__PURE__*/React.createElement(CBadge, {
      c: "convey"
    }),
    title: "Wrote for the Daily Edition",
    sub: "Three intros that changed a harvest",
    onClick: () => go('story')
  }))), tab === 'contrib' && /*#__PURE__*/React.createElement(Section, null, /*#__PURE__*/React.createElement(Card, null, /*#__PURE__*/React.createElement(ListRow, {
    leading: /*#__PURE__*/React.createElement(CBadge, {
      c: "contribute"
    }),
    title: "Refrigeration audit, Takoradi clinic",
    sub: "Attested by Thandiwe Dube, Space lead"
  }), /*#__PURE__*/React.createElement(ListRow, {
    leading: /*#__PURE__*/React.createElement(CBadge, {
      c: "contribute"
    }),
    title: "Procurement review, two weeks",
    sub: "Attested by Kwame Mensah"
  }))));
}
Object.assign(window, {
  Directory,
  Profile,
  MEMBERS
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/dna/connect.jsx", error: String((e && e.message) || e) }); }

// ui_kits/dna/contribute.jsx
try { (() => {
function OpportunityPage({
  go,
  desktop
}) {
  const [offered, setOffered] = React.useState(false);
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(TopBar, {
    back: true,
    onBack: () => go('space'),
    title: "Need",
    desktop: desktop,
    right: /*#__PURE__*/React.createElement(IconButton, {
      name: "bookmark",
      label: "Save"
    })
  }), /*#__PURE__*/React.createElement(PageHead, {
    c: "contribute",
    kicker: "Coastal Clinics Cold Chain",
    title: "Refrigeration procurement review",
    meta: "Posted by Amara Osei, Space lead"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '0 16px',
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement(Meta, {
    icon: "clock"
  }, "Two weeks, part time, remote"), /*#__PURE__*/React.createElement(Meta, {
    icon: "briefcase"
  }, "Procurement, refrigeration, solar"), /*#__PURE__*/React.createElement(Meta, {
    icon: "shield-check"
  }, "Attested on completion and listed on your profile")), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 16,
      display: 'flex',
      gap: 8
    }
  }, offered ? /*#__PURE__*/React.createElement(Button, {
    c: "contribute",
    variant: "secondary",
    full: true
  }, "Offer sent") : /*#__PURE__*/React.createElement(Button, {
    c: "contribute",
    full: true,
    onClick: () => setOffered(true)
  }, "Offer help"), /*#__PURE__*/React.createElement(Button, {
    c: "connect",
    variant: "secondary",
    onClick: () => go('composer')
  }, "Make an intro")), /*#__PURE__*/React.createElement(Section, {
    title: "What is needed"
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 17,
      lineHeight: 1.5
    }
  }, "Review quotes from four suppliers of solar direct-drive vaccine refrigerators against WHO PQS requirements. Flag total cost of ownership over five years and recommend two. You will work with Kwame Mensah and hand over a two-page memo.")), /*#__PURE__*/React.createElement(Section, {
    title: "Where this goes"
  }, /*#__PURE__*/React.createElement(Card, null, /*#__PURE__*/React.createElement(ListRow, {
    leading: /*#__PURE__*/React.createElement(CBadge, {
      c: "collaborate"
    }),
    title: "Coastal Clinics Cold Chain",
    sub: "Space. Twelve clinics by March.",
    onClick: () => go('space'),
    trailing: /*#__PURE__*/React.createElement(Icon, {
      name: "chevron-right",
      size: 20,
      style: {
        color: 'var(--ink-3)'
      }
    })
  }))));
}
Object.assign(window, {
  OpportunityPage
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/dna/contribute.jsx", error: String((e && e.message) || e) }); }

// ui_kits/dna/convene.jsx
try { (() => {
function EventPage({
  go,
  desktop
}) {
  const [ticket, setTicket] = React.useState(false);
  const [open, setOpen] = React.useState(false);
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(TopBar, {
    back: true,
    onBack: () => go('feed'),
    title: "Event",
    desktop: desktop,
    right: /*#__PURE__*/React.createElement(IconButton, {
      name: "share",
      label: "Share"
    })
  }), /*#__PURE__*/React.createElement(MediaBlock, {
    kind: "image",
    src: IMG,
    alt: "Members at the Nairobi chapter dinner",
    style: {
      margin: '12px 16px 0'
    }
  }), /*#__PURE__*/React.createElement(PageHead, {
    c: "convene",
    title: "Diaspora Builders Dinner, Nairobi",
    meta: "Hosted by the Nairobi chapter"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '0 16px',
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement(Meta, {
    icon: "calendar"
  }, "Thu 16 Oct, 19:00 to 22:00 EAT"), /*#__PURE__*/React.createElement(Meta, {
    icon: "map-pin"
  }, "Trademark Hotel, Gigiri. Address sent with your ticket."), /*#__PURE__*/React.createElement(Meta, {
    icon: "ticket"
  }, "Member ticket. Guests by invitation of a member.")), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 16,
      display: 'flex',
      gap: 8
    }
  }, ticket ? /*#__PURE__*/React.createElement(Button, {
    c: "convene",
    variant: "secondary",
    full: true,
    onClick: () => setOpen(true)
  }, "You have a ticket") : /*#__PURE__*/React.createElement(Button, {
    c: "convene",
    full: true,
    onClick: () => setTicket(true)
  }, "Get a ticket"), /*#__PURE__*/React.createElement(Button, {
    variant: "secondary",
    onClick: () => go('thread', {
      name: 'Nairobi chapter'
    })
  }, "Ask the host")), /*#__PURE__*/React.createElement(Section, {
    title: "About"
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 17,
      lineHeight: 1.5
    }
  }, "A working dinner for members building across East Africa and the diaspora. Short introductions, then tables by sector: health, energy, agri-finance, media. Bring one need and one offer.")), /*#__PURE__*/React.createElement(Section, {
    title: "From this event"
  }, /*#__PURE__*/React.createElement(Card, null, /*#__PURE__*/React.createElement(ListRow, {
    leading: /*#__PURE__*/React.createElement(CBadge, {
      c: "collaborate"
    }),
    title: "Coastal Clinics Cold Chain",
    sub: "Space started after the Accra dinner",
    onClick: () => go('space'),
    trailing: /*#__PURE__*/React.createElement(Button, {
      c: "collaborate",
      size: "sm",
      variant: "ghost"
    }, "Open")
  }))), /*#__PURE__*/React.createElement(Dialog, {
    open: open,
    title: "Your ticket",
    onClose: () => setOpen(false),
    sheet: !desktop,
    footer: /*#__PURE__*/React.createElement(Button, {
      variant: "secondary",
      onClick: () => setOpen(false)
    }, "Done")
  }, "It is in your wallet and your email. Show the code at the door."));
}
Object.assign(window, {
  EventPage
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/dna/convene.jsx", error: String((e && e.message) || e) }); }

// ui_kits/dna/convene/shared.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Convene Pass A shared pieces. Consumes Strand's Sheet, FacetRail and Pane as they exist; nothing here reimplements them.
const DS = window.StrandDNADesignSystem_3654dd;
const {
  Sheet,
  FacetRail,
  Pane,
  Button,
  EmptyState,
  Avatar,
  IconButton,
  Icon,
  CBadge,
  AppHeader,
  PulseDock,
  Input,
  DiaLine,
  NotificationBell
} = DS;
const S = 0.45;
const W = {
  compact: 390,
  medium: 760,
  expanded: 1120
};
const H = {
  compact: 780,
  medium: 640,
  expanded: 720
};
const POINTER = {
  compact: 'touch',
  medium: 'pointer',
  expanded: 'pointer'
};
const VIEWER = {
  name: 'Amara Osei'
};
const AXES = [{
  id: 'corridor',
  label: 'Corridor',
  options: [{
    id: 'us-gh',
    label: 'US to Ghana'
  }, {
    id: 'uk-gh',
    label: 'UK to Ghana'
  }, {
    id: 'us-ng',
    label: 'US to Nigeria'
  }]
}, {
  id: 'sector',
  label: 'Sector',
  options: [{
    id: 'agri',
    label: 'Agriculture'
  }, {
    id: 'fin',
    label: 'Finance'
  }, {
    id: 'health',
    label: 'Health'
  }]
}, {
  id: 'place',
  label: 'Place',
  options: [{
    id: 'accra',
    label: 'Accra'
  }, {
    id: 'la',
    label: 'Los Angeles'
  }, {
    id: 'lagos',
    label: 'Lagos'
  }]
}, {
  id: 'window',
  label: 'Date window',
  options: [{
    id: 'week',
    label: 'This week'
  }, {
    id: 'month',
    label: 'This month'
  }, {
    id: 'later',
    label: 'Later'
  }]
}, {
  id: 'format',
  label: 'Format',
  options: [{
    id: 'person',
    label: 'In person'
  }, {
    id: 'online',
    label: 'Online'
  }, {
    id: 'hybrid',
    label: 'Hybrid',
    parked: true
  }]
}];
const EVENTS = [{
  id: 'dinner',
  title: 'Diaspora Builders Dinner',
  by: 'Kwame Mensah',
  when: 'Thursday evening, sixteenth of October',
  tz: 'Accra, GMT',
  place: 'Osu, Accra',
  format: 'In person',
  proof: ['Kwame Mensah', 'Adaeze Nwosu', 'Nia Asante'],
  body: 'An evening for members building between Los Angeles and Accra in agriculture. Introductions are made by the hosts rather than left to the room, so come with one thing you need and one thing you can offer.',
  intent: 'Doors open at seven. The address is shared with everyone who says they are coming.'
}, {
  id: 'buyers',
  title: 'Corridor call: cassava buyers',
  by: 'Adaeze Nwosu',
  when: 'This month, date to be confirmed',
  tz: 'Online',
  place: null,
  format: 'Online',
  proof: ['Adaeze Nwosu'],
  body: 'A working call for cooperative members and diaspora buyers to compare what is planted and what is wanted this season.',
  intent: 'The meeting link is shared with everyone who says they are coming.'
}, {
  id: 'kumasi',
  title: 'Kumasi cooperative visit',
  by: 'Yaw Boateng',
  when: 'Late October, weekend',
  tz: 'Kumasi, GMT',
  place: 'Ejisu, Ashanti',
  format: 'In person',
  proof: [],
  body: 'A day on the farm with the Ejisu cooperative. Transport from Kumasi is arranged by the host.',
  intent: 'Meeting point and time are shared the week before.'
}, {
  id: 'lagos',
  title: 'Lagos investor circle',
  by: 'Tunde Bakare',
  when: 'Saturday morning, first week of November',
  tz: 'Lagos, WAT',
  place: 'Victoria Island, Lagos',
  format: 'Hybrid',
  proof: ['Tunde Bakare', 'Zainab Diallo'],
  body: 'Founders and diaspora investors in one room and one call. Short pitches, long conversations.',
  intent: 'Room address and stream link are shared with everyone who says they are coming.'
}];
const CANCELLED = {
  ...EVENTS[0],
  id: 'nairobi',
  title: 'Nairobi chapter call',
  by: 'Nia Asante',
  when: 'Was planned for Saturday morning, late September',
  tz: 'Nairobi, EAT',
  place: null,
  format: 'Online',
  cancelled: true,
  reason: 'The host is travelling that week and will set a new date. Everyone who said they were coming has been told by email.'
};
const PAST = {
  ...EVENTS[0],
  id: 'past',
  when: 'Happened on Thursday evening, second of October',
  past: true
};
const ATTENDEES_ABOVE = ['Kwame Mensah', 'Adaeze Nwosu', 'Nia Asante', 'Yaw Boateng', 'Tunde Bakare', 'Zainab Diallo', 'Ama Serwaa'];
const ATTENDEES_BELOW = ['Kwame Mensah', 'Adaeze Nwosu', 'Nia Asante'];
const CONNECTIONS = ['Kwame Mensah', 'Adaeze Nwosu', 'Nia Asante'];
function Frame({
  tier,
  label,
  children
}) {
  const w = W[tier],
    h = H[tier];
  return /*#__PURE__*/React.createElement("figure", null, /*#__PURE__*/React.createElement("div", {
    className: "fl"
  }, tier, " \xB7 ", POINTER[tier], " \xB7 ", w, "px", label ? ' · ' + label : ''), /*#__PURE__*/React.createElement("div", {
    className: "fw",
    style: {
      width: Math.round(w * S),
      height: Math.round(h * S)
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "fr",
    style: {
      width: w,
      height: h,
      transform: 'scale(' + S + ')',
      transformOrigin: 'top left'
    }
  }, children)));
}
function Row({
  label,
  desc,
  children
}) {
  const ref = React.useRef(null);
  const [on, setOn] = React.useState(false);
  React.useEffect(() => {
    const el = ref.current;
    if (!el || on) return;
    const io = new IntersectionObserver(es => {
      if (es.some(e => e.isIntersecting)) {
        setOn(true);
        io.disconnect();
      }
    }, {
      rootMargin: '600px 0px'
    });
    io.observe(el);
    return () => io.disconnect();
  }, [on]);
  return /*#__PURE__*/React.createElement("div", {
    className: "row",
    ref: ref
  }, /*#__PURE__*/React.createElement("div", {
    className: "rl"
  }, label), desc && /*#__PURE__*/React.createElement("div", {
    className: "rd"
  }, desc), on ? children : /*#__PURE__*/React.createElement("div", {
    style: {
      height: Math.round(H.compact * S),
      width: '100%'
    },
    "aria-hidden": "true"
  }));
}
function SocialProof({
  names = []
}) {
  if (!names.length) return null;
  const n = names.slice(0, 3);
  const list = n.length === 1 ? n[0] : n.length === 2 ? n[0] + ' and ' + n[1] : n[0] + ', ' + n[1] + ' and ' + n[2];
  return /*#__PURE__*/React.createElement("div", {
    "data-proof": true,
    style: {
      fontSize: 'var(--text-s)',
      lineHeight: 'var(--text-s-lh)',
      color: 'var(--ink-2)'
    }
  }, list, " and others are going");
}
function MetaLine({
  e
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 'var(--space-1) var(--space-3)',
      fontSize: 'var(--text-xs)',
      lineHeight: 'var(--text-xs-lh)',
      color: 'var(--c-convene-text)'
    }
  }, /*#__PURE__*/React.createElement("span", null, e.when), /*#__PURE__*/React.createElement("span", null, e.tz), /*#__PURE__*/React.createElement("span", null, e.place || 'Online'));
}
function EventRow({
  e,
  selected,
  onOpen,
  arrived
}) {
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onOpen,
    "data-arrived": arrived || undefined,
    "aria-current": selected ? 'true' : undefined,
    style: {
      all: 'unset',
      boxSizing: 'border-box',
      cursor: 'pointer',
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-2)',
      padding: 'var(--space-4)',
      width: '100%',
      borderRadius: 'var(--radius-l)',
      background: selected ? 'var(--c-convene-tint)' : 'var(--surface)',
      border: (selected ? 'var(--c-stroke)' : 'var(--border-card)') + ' solid var(--c-convene)',
      fontFamily: 'var(--font-sans)',
      color: 'var(--ink)',
      textAlign: 'left'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-2)'
    }
  }, /*#__PURE__*/React.createElement(CBadge, {
    c: "convene",
    size: 24
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--text-xs)',
      color: 'var(--ink-3)'
    }
  }, e.by, " hosts")), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: 'var(--font-display)',
      fontSize: 'var(--display-s)',
      lineHeight: 'var(--display-s-lh)'
    }
  }, e.title), /*#__PURE__*/React.createElement(MetaLine, {
    e: e
  }), /*#__PURE__*/React.createElement(SocialProof, {
    names: e.proof
  }));
}
function EventList({
  events = EVENTS,
  selectedId,
  dia,
  arrivedId,
  state
}) {
  if (state === 'loading') return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 'var(--space-4)',
      color: 'var(--ink-3)',
      fontSize: 'var(--text-m)'
    }
  }, "Loading events");
  if (state === 'error') return /*#__PURE__*/React.createElement("div", {
    role: "alert",
    style: {
      margin: 'var(--space-4)',
      padding: 'var(--space-4)',
      borderRadius: 'var(--radius-m)',
      background: 'var(--error-tint)',
      color: 'var(--error)',
      fontSize: 'var(--text-s)',
      lineHeight: 'var(--text-s-lh)'
    }
  }, "Events could not be loaded. Try again in a moment.");
  if (state === 'none') return /*#__PURE__*/React.createElement(EmptyState, {
    c: "convene",
    title: "No events yet",
    body: "Nobody has hosted a convening in your corridor. Be the one who does.",
    action: /*#__PURE__*/React.createElement(Button, {
      c: "convene"
    }, "Host an Event")
  });
  if (state === 'nomatch') return /*#__PURE__*/React.createElement(EmptyState, {
    c: "convene",
    title: "No events match these facets",
    body: "Clear a facet to widen the search, or host the convening you were looking for.",
    action: /*#__PURE__*/React.createElement(Button, {
      variant: "secondary",
      c: "convene"
    }, "Clear all")
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-3)'
    }
  }, dia && /*#__PURE__*/React.createElement(DiaLine, {
    state: "done",
    text: dia,
    onNotThis: () => {}
  }), events.map(e => /*#__PURE__*/React.createElement(EventRow, {
    key: e.id,
    e: e,
    selected: e.id === selectedId,
    arrived: e.id === arrivedId,
    onOpen: () => {}
  })));
}
function AttendeeList({
  attendees = [],
  viewer = VIEWER.name,
  connections = CONNECTIONS
}) {
  if (attendees.length < 5) return null;
  const sorted = attendees.slice().sort((a, b) => (connections.includes(b) ? 1 : 0) - (connections.includes(a) ? 1 : 0));
  return /*#__PURE__*/React.createElement("section", {
    "aria-label": "Who is coming",
    "data-attendees": true,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-3)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--text-xs)',
      letterSpacing: 'var(--tracking-caps)',
      textTransform: 'uppercase',
      color: 'var(--ink-3)'
    }
  }, "Who is coming"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-2)'
    }
  }, sorted.map(n => /*#__PURE__*/React.createElement("div", {
    key: n,
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-3)',
      fontSize: 'var(--text-s)'
    }
  }, /*#__PURE__*/React.createElement(Avatar, {
    name: n,
    size: 32
  }), /*#__PURE__*/React.createElement("span", null, n), connections.includes(n) && /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--text-xs)',
      color: 'var(--c-connect-text)'
    }
  }, "Your connection")))), /*#__PURE__*/React.createElement(Button, {
    variant: "ghost",
    size: "sm",
    style: {
      alignSelf: 'flex-start'
    }
  }, "Hide me from this list"));
}
function Media() {
  return /*#__PURE__*/React.createElement("div", {
    "aria-label": "Event photo",
    role: "img",
    style: {
      height: 180,
      borderRadius: 'var(--radius-l)',
      background: 'var(--bg-sunken)',
      display: 'flex',
      alignItems: 'flex-end',
      padding: 'var(--space-3)',
      color: 'var(--ink-3)',
      fontSize: 'var(--text-xs)'
    }
  }, "Photo from the host");
}

/** The event page body. viewer: member | guest | signedOut. variant: loaded | cancelled | past | capacity. */
function EventDetail({
  e = EVENTS[0],
  viewer = 'member',
  variant = 'loaded',
  attendees = ATTENDEES_ABOVE,
  rsvp,
  onRsvp,
  guestRsvp
}) {
  const cancelled = variant === 'cancelled',
    past = variant === 'past',
    full = variant === 'capacity',
    out = viewer === 'signedOut';
  const by = out ? 'The host' : e.by;
  if (cancelled) return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 'var(--space-6) var(--space-5)',
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-5)',
      background: 'var(--bg-sunken)',
      minHeight: '100%',
      boxSizing: 'border-box',
      color: 'var(--ink-2)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--text-xs)',
      letterSpacing: 'var(--tracking-caps)',
      textTransform: 'uppercase',
      color: 'var(--ink-3)'
    }
  }, "Cancelled"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: 'var(--font-display)',
      fontSize: 'var(--display-m)',
      lineHeight: 'var(--display-m-lh)',
      color: 'var(--ink)'
    }
  }, e.title, " will not happen"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 'var(--text-m)',
      lineHeight: 'var(--text-m-lh)'
    }
  }, e.reason), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--text-s)',
      lineHeight: 'var(--text-s-lh)'
    }
  }, /*#__PURE__*/React.createElement("div", null, e.when), /*#__PURE__*/React.createElement("div", null, e.tz), /*#__PURE__*/React.createElement("div", null, "Hosted by ", by)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 'var(--space-2)',
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement(Button, {
    c: "convene"
  }, "Find other events"), !out && /*#__PURE__*/React.createElement(Button, {
    variant: "ghost"
  }, "Message the host")));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 'var(--space-5)',
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-5)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-2)'
    }
  }, /*#__PURE__*/React.createElement(CBadge, {
    c: "convene",
    size: 24
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--text-xs)',
      color: 'var(--ink-3)'
    }
  }, "Presented by ", by)), /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: 0,
      fontFamily: 'var(--font-display)',
      fontSize: 'var(--display-m)',
      lineHeight: 'var(--display-m-lh)',
      fontWeight: 'var(--weight-regular)'
    }
  }, e.title), /*#__PURE__*/React.createElement(MetaLine, {
    e: e
  }), /*#__PURE__*/React.createElement(Media, null), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 'var(--text-m)',
      lineHeight: 'var(--text-m-lh)'
    }
  }, e.body), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--text-s)',
      lineHeight: 'var(--text-s-lh)',
      color: 'var(--ink-2)',
      padding: 'var(--space-3) var(--space-4)',
      borderRadius: 'var(--radius-m)',
      background: 'var(--bg-sunken)'
    }
  }, e.intent), past ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-3)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--text-s)',
      color: 'var(--ink-2)'
    }
  }, "This event has happened."), !out && /*#__PURE__*/React.createElement(Button, {
    c: "convene",
    onClick: onRsvp
  }, "Say you were there")) : full ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-3)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--text-s)',
      color: 'var(--ink-2)'
    }
  }, "This event is full."), /*#__PURE__*/React.createElement(Button, {
    variant: "secondary",
    c: "convene"
  }, "Tell me if a place opens")) : out ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-3)'
    }
  }, /*#__PURE__*/React.createElement(Button, {
    c: "convene",
    onClick: guestRsvp
  }, "Say you are coming with your email"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--text-xs)',
      color: 'var(--ink-3)'
    }
  }, "No account needed. You return by a link we email you.")) : rsvp === 'going' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 'var(--space-2)',
      flexWrap: 'wrap',
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--text-s)',
      color: 'var(--c-connect-text)'
    }
  }, "You are going"), /*#__PURE__*/React.createElement(Button, {
    variant: "ghost",
    size: "sm",
    onClick: onRsvp
  }, "Change")) : rsvp === 'not' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 'var(--space-2)',
      flexWrap: 'wrap',
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--text-s)',
      color: 'var(--ink-2)'
    }
  }, "You said you are not coming"), /*#__PURE__*/React.createElement(Button, {
    variant: "ghost",
    size: "sm",
    onClick: onRsvp
  }, "Change")) : /*#__PURE__*/React.createElement(Button, {
    c: "convene",
    onClick: onRsvp
  }, "Say you are coming"), !out && !past && /*#__PURE__*/React.createElement(SocialProof, {
    names: e.proof
  }), !out && !past && /*#__PURE__*/React.createElement(AttendeeList, {
    attendees: attendees
  }), out && /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--text-xs)',
      color: 'var(--ink-3)',
      borderTop: 'var(--border-thin) solid var(--line)',
      paddingTop: 'var(--space-3)'
    }
  }, "Members who are going are shown once you are signed in."));
}
function SignedOutHeader() {
  return /*#__PURE__*/React.createElement("header", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      height: 56,
      padding: '0 var(--space-4)',
      borderBottom: 'var(--border-thin) solid var(--line)',
      background: 'var(--bg)'
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: DS_ASSETS + 'logo.png',
    alt: "DNA",
    style: {
      height: 24
    }
  }), /*#__PURE__*/React.createElement(Button, {
    variant: "ghost",
    size: "sm"
  }, "Sign in"));
}
const DS_ASSETS = (() => {
  const s = document.querySelector('script[src*="_ds_bundle"]');
  return s ? s.src.replace(/_ds_bundle[^/]*$/, 'assets/') : '../../../assets/';
})();

/** The Convene canvas at each tier. 612: a surface carrying a pane does not render 79's right rail; the pane extends through that region. DIA is absent while the pane is open. */
function Canvas({
  tier,
  signedIn = true,
  facets = {},
  list,
  pane,
  paneProps = {},
  sheet,
  cold,
  dia,
  facetOpen,
  show
}) {
  const root = React.useRef(null);
  React.useEffect(() => {
    // frames only: bring the demonstrated element into view inside the frame
    if (!show || !root.current) return;
    const el = root.current.querySelector(show);
    if (!el) return;
    let p = el.parentElement;
    while (p && p !== root.current && !/(auto|scroll)/.test(getComputedStyle(p).overflowY)) p = p.parentElement;
    if (!p || p === root.current) p = root.current.querySelector('[data-canvas-body]');
    if (!p) return;
    const eb = el.getBoundingClientRect(),
      pb = p.getBoundingClientRect();
    p.scrollTop += (eb.top - pb.top) / S - 24;
  }, [show]);
  const hasPane = pane !== undefined;
  const rail = /*#__PURE__*/React.createElement(FacetRail, {
    tier: tier,
    label: "Filters",
    axes: AXES,
    value: facets,
    onChange: () => {},
    onClear: () => {},
    open: facetOpen,
    onOpenChange: () => {}
  });
  const header = signedIn ? /*#__PURE__*/React.createElement(AppHeader, {
    variant: tier === 'expanded' ? 'expanded' : 'compact',
    member: VIEWER
  }, /*#__PURE__*/React.createElement(NotificationBell, null)) : /*#__PURE__*/React.createElement(SignedOutHeader, null);
  const body = tier === 'expanded' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'minmax(0,260px) minmax(0,1fr)',
      gap: 'var(--pane-gap)',
      padding: 'var(--space-6) var(--space-8)',
      alignItems: 'start'
    }
  }, rail, /*#__PURE__*/React.createElement(Pane, _extends({
    tier: "expanded",
    title: "Event",
    selected: hasPane && pane !== null,
    cold: cold,
    list: list,
    listFallback: list,
    empty: /*#__PURE__*/React.createElement(EmptyState, {
      c: "convene",
      title: "Pick an event to read it",
      body: "The list stays where it is while you read."
    })
  }, paneProps), pane)) : hasPane ? /*#__PURE__*/React.createElement(Pane, _extends({
    tier: tier,
    title: "Event",
    selected: true,
    backLabel: "Events",
    cold: cold,
    onBack: () => {}
  }, paneProps), pane) : tier === 'medium' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'minmax(0,240px) minmax(0,1fr)',
      gap: 'var(--space-6)',
      padding: 'var(--space-5) var(--space-6)',
      alignItems: 'start'
    }
  }, rail, /*#__PURE__*/React.createElement("div", null, list)) : /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 'var(--space-4)',
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-4)'
    }
  }, rail, list);
  return /*#__PURE__*/React.createElement("div", {
    ref: root,
    style: {
      position: 'relative',
      height: '100%',
      display: 'flex',
      flexDirection: 'column',
      background: 'var(--bg)',
      color: 'var(--ink)',
      fontFamily: 'var(--font-sans)',
      overflow: 'hidden'
    }
  }, header, signedIn && tier !== 'compact' && /*#__PURE__*/React.createElement(PulseDock, {
    bar: true,
    active: "convene"
  }), /*#__PURE__*/React.createElement("div", {
    "data-canvas-body": true,
    style: {
      flex: 1,
      minHeight: 0,
      overflow: 'auto'
    }
  }, body), signedIn && tier === 'compact' && /*#__PURE__*/React.createElement(PulseDock, {
    active: "convene"
  }), sheet);
}
function Block({
  dark,
  title,
  note,
  children
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: "blk",
    "data-theme": dark ? 'dark' : undefined
  }, /*#__PURE__*/React.createElement("h2", {
    className: "th"
  }, title, " \xB7 ", dark ? 'Dark' : 'Light'), note && /*#__PURE__*/React.createElement("p", {
    className: "note"
  }, note), children);
}
const TIERS = ['compact', 'medium', 'expanded'];
Object.assign(window, {
  S,
  W,
  H,
  TIERS,
  AXES,
  EVENTS,
  CANCELLED,
  PAST,
  ATTENDEES_ABOVE,
  ATTENDEES_BELOW,
  CONNECTIONS,
  VIEWER,
  Frame,
  Row,
  SocialProof,
  EventRow,
  EventList,
  AttendeeList,
  EventDetail,
  Canvas,
  Block,
  SignedOutHeader
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/dna/convene/shared.jsx", error: String((e && e.message) || e) }); }

// ui_kits/dna/convey.jsx
try { (() => {
function StoryPage({
  go,
  desktop
}) {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(TopBar, {
    back: true,
    onBack: () => go('feed'),
    title: "Daily Edition",
    desktop: desktop,
    right: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(IconButton, {
      name: "bookmark",
      label: "Save"
    }), /*#__PURE__*/React.createElement(IconButton, {
      name: "share",
      label: "Share"
    }))
  }), /*#__PURE__*/React.createElement("img", {
    src: IMG,
    alt: "",
    style: {
      display: 'block',
      width: '100%',
      maxHeight: 300,
      objectFit: 'cover'
    }
  }), /*#__PURE__*/React.createElement(PageHead, {
    c: "convey",
    kicker: "Fri 4 Sep 2026",
    title: "Three intros that changed a harvest",
    meta: "By Adaeze Nwosu. Lagos, WAT"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '0 16px'
    }
  }, /*#__PURE__*/React.createElement(MediaBlock, {
    kind: "audio",
    title: "Read by the author",
    duration: "6:40"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 16px 0',
      fontSize: 19,
      lineHeight: 1.55,
      display: 'flex',
      flexDirection: 'column',
      gap: 16,
      maxWidth: 640
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0
    }
  }, "The cassava cooperative in Oyo had a good problem. Yield was up for the second year and nobody in the state could take the surplus. What it did not have was a buyer outside Nigeria, or anyone who knew how to find one."), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0
    }
  }, "Kwame Mensah did not know either. What he knew was two people in Houston who imported garri for West African grocers across Texas, and that one of them had complained about supply the week before."), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0
    }
  }, "He made the intro on a Tuesday. The first container left Lagos in November.")), /*#__PURE__*/React.createElement(Section, {
    title: "From this story"
  }, /*#__PURE__*/React.createElement(Card, null, /*#__PURE__*/React.createElement(ListRow, {
    leading: /*#__PURE__*/React.createElement(Avatar, {
      name: "Kwame Mensah",
      size: 40
    }),
    title: "Kwame Mensah",
    sub: "Agri-finance, cooperatives. Houston",
    onClick: () => go('profile', MEMBERS[1]),
    trailing: /*#__PURE__*/React.createElement(Button, {
      c: "connect",
      size: "sm",
      variant: "secondary"
    }, "Intro")
  }), /*#__PURE__*/React.createElement(ListRow, {
    leading: /*#__PURE__*/React.createElement(CBadge, {
      c: "collaborate"
    }),
    title: "Oyo Cassava Export",
    sub: "Space, active",
    onClick: () => go('space')
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 16
    }
  }, /*#__PURE__*/React.createElement(Button, {
    c: "convey",
    variant: "secondary",
    full: true,
    onClick: () => go('composer')
  }, "Share a Story")));
}
Object.assign(window, {
  StoryPage
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/dna/convey.jsx", error: String((e && e.message) || e) }); }

// ui_kits/dna/feed.jsx
try { (() => {
function Feed({
  go,
  desktop,
  openComposer
}) {
  const [lens, setLens] = React.useState('all');
  const [saved, setSaved] = React.useState({});
  const tog = k => () => setSaved(s => ({
    ...s,
    [k]: !s[k]
  }));
  const cards = [/*#__PURE__*/React.createElement(PostCard, {
    key: "e",
    c: "convene",
    author: "Nairobi chapter",
    meta: "Nairobi, EAT. Thu 16 Oct, 19:00",
    kicker: "Event",
    title: "Diaspora Builders Dinner",
    media: {
      kind: 'image',
      src: IMG,
      alt: ''
    },
    actions: /*#__PURE__*/React.createElement(Button, {
      c: "convene",
      size: "sm",
      onClick: () => go('event')
    }, "Get a ticket"),
    respondLabel: "Ask the host",
    saved: saved.e,
    onSave: tog('e'),
    onClick: () => go('event')
  }, "Bring one need and one offer. Tables by sector."), /*#__PURE__*/React.createElement(PostCard, {
    key: "s",
    c: "convey",
    author: "Adaeze Nwosu",
    meta: "Lagos, WAT. Daily Edition",
    kicker: "Story",
    title: "Three intros that changed a harvest",
    media: {
      kind: 'audio',
      title: 'Read by the author',
      duration: '6:40'
    },
    actions: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Button, {
      c: "convey",
      size: "sm",
      onClick: () => go('story')
    }, "Read the story"), /*#__PURE__*/React.createElement(Button, {
      c: "connect",
      variant: "secondary",
      size: "sm"
    }, "Make an intro")),
    saved: saved.s,
    onSave: tog('s'),
    onClick: () => go('story')
  }, "How a cassava cooperative in Oyo found its first buyer through two members in Houston."), /*#__PURE__*/React.createElement(PostCard, {
    key: "n",
    c: "contribute",
    author: "Amara Osei",
    meta: "Coastal Clinics Cold Chain. This week",
    kicker: "Need",
    title: "Refrigeration procurement review",
    actions: /*#__PURE__*/React.createElement(Button, {
      c: "contribute",
      size: "sm",
      onClick: () => go('opportunity')
    }, "Offer help"),
    respondLabel: "Ask a question",
    saved: saved.n,
    onSave: tog('n'),
    onClick: () => go('opportunity')
  }, "Two weeks reviewing supplier quotes for solar direct-drive fridges. Remote. Attested on completion."), /*#__PURE__*/React.createElement(PostCard, {
    key: "c",
    c: "collaborate",
    author: "Thandiwe Dube",
    meta: "Coastal Clinics Cold Chain",
    kicker: "Space update",
    title: "Takoradi site survey is done",
    media: {
      kind: 'gallery',
      items: [IMG, IMG, IMG]
    },
    actions: /*#__PURE__*/React.createElement(Button, {
      c: "collaborate",
      size: "sm",
      onClick: () => go('space')
    }, "Open Space"),
    saved: saved.c,
    onSave: tog('c'),
    onClick: () => go('space')
  }, "Report and photos in files. Procurement shortlist is next."), /*#__PURE__*/React.createElement(PostCard, {
    key: "i",
    c: "connect",
    author: "Kwame Mensah",
    meta: "Houston, CST",
    title: "Kwame made an intro",
    actions: /*#__PURE__*/React.createElement(Button, {
      c: "connect",
      size: "sm",
      onClick: () => go('profile', MEMBERS[1])
    }, "View profile"),
    saved: saved.i,
    onSave: tog('i'),
    respondLabel: "Congratulate"
  }, "Introduced Yusuf Diallo to the Oyo Cassava Export Space for trade documentation.")];
  const list = lens === 'saved' ? cards.filter(c => saved[c.key]) : lens === 'net' ? cards.slice(0, 3) : cards;
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(TopBar, {
    title: "",
    desktop: desktop,
    right: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(IconButton, {
      name: "bell",
      label: "Notifications",
      onClick: () => go('notifications')
    }), /*#__PURE__*/React.createElement(IconButton, {
      name: "message-circle",
      label: "Messages",
      onClick: () => go('thread', {
        name: 'Amara Osei'
      })
    }))
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 16px 0',
      display: 'flex',
      flexDirection: 'column',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement(LensBar, {
    lenses: [{
      id: 'all',
      label: 'Everything',
      icon: 'globe'
    }, {
      id: 'net',
      label: 'My network',
      icon: 'users'
    }, {
      id: 'saved',
      label: 'Saved',
      icon: 'bookmark'
    }],
    value: lens,
    onChange: setLens,
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement(IconButton, {
    name: "sliders-horizontal",
    label: "Filters"
  })), /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: openComposer,
    style: {
      all: 'unset',
      boxSizing: 'border-box',
      cursor: 'pointer',
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      minHeight: 52,
      padding: '0 14px',
      borderRadius: 14,
      border: '1px solid var(--line)',
      background: 'var(--surface)',
      color: 'var(--ink-3)',
      fontSize: 17
    }
  }, /*#__PURE__*/React.createElement(Avatar, {
    name: "Amara Osei",
    src: IMG,
    size: 32
  }), "What do you want to do")), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 16,
      display: 'flex',
      flexDirection: 'column',
      gap: 12
    }
  }, list.length === 0 ? /*#__PURE__*/React.createElement(EmptyState, {
    c: "brand",
    title: "Nothing saved yet.",
    body: "Save any card and it will wait here."
  }) : list));
}
function FeedComposerMock({
  open,
  onClose,
  go,
  desktop
}) {
  const [act, setAct] = React.useState(null);
  const close = () => {
    setAct(null);
    onClose();
  };
  const acts = [['connect', 'Make an Intro'], ['convene', 'Host an Event'], ['collaborate', 'Start a Space'], ['contribute', 'Post a Need'], ['convey', 'Share a Story']];
  return /*#__PURE__*/React.createElement(Dialog, {
    open: open,
    sheet: !desktop,
    title: act ? act[1] : 'What do you want to do',
    onClose: close,
    footer: act && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Button, {
      variant: "secondary",
      onClick: () => setAct(null)
    }, "Back"), /*#__PURE__*/React.createElement(Button, {
      c: act[0],
      onClick: () => {
        close();
        go(HOME[act[0]]);
      }
    }, act[1].split(' ')[0]))
  }, !act ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 2,
      margin: '-8px -8px'
    }
  }, acts.map(a => /*#__PURE__*/React.createElement(ActButton, {
    key: a[0],
    c: a[0],
    act: a[1],
    onClick: () => setAct(a)
  }))) : /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement(CBadge, {
    c: act[0],
    size: 32,
    label: true
  }), /*#__PURE__*/React.createElement(Input, {
    label: act[0] === 'convey' ? 'Title' : act[0] === 'connect' ? 'Who' : 'Name',
    placeholder: act[0] === 'connect' ? 'Two members' : 'Say it plainly'
  }), /*#__PURE__*/React.createElement(Input, {
    label: act[0] === 'connect' ? 'Why they should meet' : 'Details',
    multiline: true,
    rows: 3,
    placeholder: "A few sentences"
  }), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 15,
      fontWeight: 500,
      color: 'var(--ink-2)',
      marginBottom: 8
    }
  }, "Media"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 8,
      flexWrap: 'wrap'
    }
  }, ['image', 'images', 'video', 'link', 'mic'].map(i => /*#__PURE__*/React.createElement(IconButton, {
    key: i,
    name: i,
    label: i,
    style: {
      border: '1px solid var(--line)'
    }
  }))), act[0] === 'convey' && /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      color: 'var(--ink-3)',
      marginTop: 8
    }
  }, "Drag media above or below your text."))));
}
Object.assign(window, {
  Feed,
  FeedComposerMock
}); // Brief 1 mock. Never named Composer: the bundle carries this file and window.Composer would shadow StrandDNADesignSystem_3654dd.Composer for consumers.
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/dna/feed.jsx", error: String((e && e.message) || e) }); }

// ui_kits/dna/inbox.jsx
try { (() => {
function Notifications({
  go,
  desktop
}) {
  const items = [{
    c: 'connect',
    t: 'Kwame accepted your intro.',
    s: 'Just now',
    to: 'profile',
    urgent: false
  }, {
    c: 'convene',
    t: 'Doors open in two hours for Diaspora Builders Dinner.',
    s: 'Today',
    to: 'event',
    urgent: true
  }, {
    c: 'contribute',
    t: 'Your procurement review was attested by Amara Osei.',
    s: 'Yesterday',
    to: 'opportunity'
  }, {
    c: 'convey',
    t: 'Today\'s Edition is out. Three intros that changed a harvest.',
    s: 'Yesterday',
    to: 'story'
  }, {
    c: 'collaborate',
    t: 'Thandiwe posted a Space update.',
    s: 'Wed',
    to: 'space'
  }];
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(TopBar, {
    back: true,
    onBack: () => go('feed'),
    title: "Notifications",
    desktop: desktop,
    right: /*#__PURE__*/React.createElement(Button, {
      variant: "ghost",
      size: "sm"
    }, "Mark all read")
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 16
    }
  }, /*#__PURE__*/React.createElement(Card, {
    style: {
      padding: '4px 4px'
    }
  }, items.map((n, i) => /*#__PURE__*/React.createElement(ListRow, {
    key: i,
    leading: /*#__PURE__*/React.createElement("span", {
      style: {
        position: 'relative',
        display: 'inline-flex'
      }
    }, /*#__PURE__*/React.createElement(CBadge, {
      c: n.c,
      size: 32
    }), n.urgent && /*#__PURE__*/React.createElement("span", {
      style: {
        position: 'absolute',
        top: -3,
        right: -3,
        width: 10,
        height: 10,
        borderRadius: 999,
        background: 'var(--pulse-urgent)',
        border: '2px solid var(--surface)'
      }
    })),
    title: /*#__PURE__*/React.createElement("span", {
      style: {
        fontWeight: i < 2 ? 700 : 500
      }
    }, n.t),
    sub: n.s,
    onClick: () => go(n.to)
  })))));
}
function Thread({
  go,
  desktop,
  member
}) {
  const who = member && member.name || 'Amara Osei';
  const [msgs, setMsgs] = React.useState([{
    me: false,
    t: 'Thanks for reaching out. Which clinics are you thinking of first?'
  }, {
    me: true,
    t: 'Takoradi and Cape Coast. The survey report is in the Space files.'
  }, {
    me: false,
    t: 'Read it. I can do the procurement review if it is still open.'
  }]);
  const [draft, setDraft] = React.useState('');
  const send = () => {
    if (!draft.trim()) return;
    setMsgs(m => [...m, {
      me: true,
      t: draft.trim()
    }]);
    setDraft('');
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      minHeight: '100%'
    }
  }, /*#__PURE__*/React.createElement(TopBar, {
    back: true,
    onBack: () => go('feed'),
    title: who,
    desktop: desktop,
    right: /*#__PURE__*/React.createElement(IconButton, {
      name: "info",
      label: "Details"
    })
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      margin: '12px 16px 0',
      padding: '10px 12px',
      borderRadius: 10,
      border: '1.5px solid var(--c-collaborate)',
      background: 'var(--surface)',
      display: 'flex',
      alignItems: 'center',
      gap: 10,
      cursor: 'pointer'
    },
    onClick: () => go('space')
  }, /*#__PURE__*/React.createElement(CBadge, {
    c: "collaborate",
    size: 32
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      color: 'var(--ink-3)',
      letterSpacing: '0.04em',
      textTransform: 'uppercase'
    }
  }, "About"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 15,
      fontWeight: 500,
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap'
    }
  }, "Coastal Clinics Cold Chain")), /*#__PURE__*/React.createElement(Icon, {
    name: "chevron-right",
    size: 20,
    style: {
      color: 'var(--ink-3)'
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      padding: 16,
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, msgs.map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      alignSelf: m.me ? 'flex-end' : 'flex-start',
      maxWidth: '80%',
      padding: '10px 14px',
      borderRadius: 14,
      fontSize: 17,
      lineHeight: 1.45,
      background: m.me ? 'var(--ink)' : 'var(--surface)',
      color: m.me ? 'var(--inverse-ink)' : 'var(--ink)',
      border: m.me ? 'none' : '1px solid var(--line)'
    }
  }, m.t))), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'sticky',
      bottom: desktop ? 0 : 64,
      padding: '8px 16px 12px',
      background: 'var(--bg)',
      borderTop: '1px solid var(--line)',
      display: 'flex',
      gap: 8,
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement(IconButton, {
    name: "plus",
    label: "Add"
  }), /*#__PURE__*/React.createElement(Input, {
    placeholder: "Message",
    value: draft,
    onChange: e => setDraft(e.target.value),
    onKeyDown: e => e.key === 'Enter' && send(),
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement(IconButton, {
    name: "send",
    label: "Send",
    active: true,
    onClick: send
  })));
}
Object.assign(window, {
  Notifications,
  Thread
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/dna/inbox.jsx", error: String((e && e.message) || e) }); }

// ui_kits/dna/shell.jsx
try { (() => {
const DS = window.StrandDNADesignSystem_3654dd;
const {
  Button,
  IconButton,
  Icon,
  Avatar,
  CBadge,
  PulseDock,
  LensBar,
  Chip,
  Tabs,
  EmptyState,
  PostCard,
  MediaBlock,
  ActButton,
  TierBadge,
  Input,
  Select,
  Dialog,
  Toast,
  Switch,
  Checkbox
} = DS;
const IMG = '../../assets/imagery/hero-professional.jpeg';
const C_OF = {
  profile: 'connect',
  directory: 'connect',
  event: 'convene',
  space: 'collaborate',
  opportunity: 'contribute',
  story: 'convey',
  feed: null,
  notifications: null,
  thread: null
};
const HOME = {
  connect: 'directory',
  convene: 'event',
  collaborate: 'space',
  contribute: 'opportunity',
  convey: 'story'
};
function TopBar({
  title,
  back,
  onBack,
  right,
  desktop
}) {
  return /*#__PURE__*/React.createElement("header", {
    style: {
      position: 'sticky',
      top: desktop ? 56 : 0,
      zIndex: 10,
      display: 'flex',
      alignItems: 'center',
      gap: 4,
      height: 56,
      padding: '0 8px',
      background: 'rgba(250,247,242,0.92)',
      backdropFilter: 'blur(12px)',
      borderBottom: '1px solid var(--line)'
    }
  }, back ? /*#__PURE__*/React.createElement(IconButton, {
    name: "arrow-left",
    label: "Back",
    onClick: onBack
  }) : desktop ? /*#__PURE__*/React.createElement("span", {
    style: {
      width: 44
    }
  }) : /*#__PURE__*/React.createElement("img", {
    src: "../../assets/logo.png",
    alt: "DNA",
    style: {
      height: 26,
      marginLeft: 8
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      fontSize: 17,
      fontWeight: 500,
      textAlign: back ? 'left' : 'center',
      paddingLeft: back ? 4 : 0,
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap'
    }
  }, title), right);
}
function PageHead({
  c,
  kicker,
  title,
  meta,
  children
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '20px 16px 8px',
      display: 'flex',
      flexDirection: 'column',
      gap: 10
    }
  }, c && /*#__PURE__*/React.createElement(CBadge, {
    c: c,
    size: 32,
    label: true
  }), kicker && /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      letterSpacing: '0.06em',
      textTransform: 'uppercase',
      color: 'var(--ink-3)',
      fontWeight: 500
    }
  }, kicker), /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: 0,
      fontFamily: 'var(--font-display)',
      fontWeight: 400,
      fontSize: 32,
      lineHeight: 1.15,
      textWrap: 'pretty'
    }
  }, title), meta && /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 15,
      color: 'var(--ink-2)',
      lineHeight: 1.45
    }
  }, meta), children);
}
function Section({
  title,
  action,
  children,
  style
}) {
  return /*#__PURE__*/React.createElement("section", {
    style: {
      padding: '16px 16px 0',
      display: 'flex',
      flexDirection: 'column',
      gap: 10,
      ...style
    }
  }, title && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between'
    }
  }, /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: 0,
      fontFamily: 'var(--font-display)',
      fontWeight: 400,
      fontSize: 22,
      lineHeight: 1.25
    }
  }, title), action), children);
}
function ListRow({
  leading,
  title,
  sub,
  trailing,
  onClick
}) {
  const [h, setH] = React.useState(false);
  return /*#__PURE__*/React.createElement("div", {
    role: onClick ? 'button' : undefined,
    onClick: onClick,
    onMouseEnter: () => setH(true),
    onMouseLeave: () => setH(false),
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      minHeight: 56,
      padding: '8px 12px',
      borderRadius: 10,
      cursor: onClick ? 'pointer' : 'default',
      background: h && onClick ? 'var(--bg-sunken)' : 'transparent'
    }
  }, leading, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 17,
      fontWeight: 500,
      lineHeight: 1.3
    }
  }, title), sub && /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 15,
      color: 'var(--ink-3)',
      lineHeight: 1.4,
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap'
    }
  }, sub)), trailing);
}
function Card({
  children,
  c,
  style,
  onClick
}) {
  return /*#__PURE__*/React.createElement("div", {
    onClick: onClick,
    style: {
      background: 'var(--surface)',
      borderRadius: 14,
      border: c ? '1.5px solid var(--c-' + c + ')' : '1px solid var(--line)',
      padding: 16,
      cursor: onClick ? 'pointer' : 'default',
      ...style
    }
  }, children);
}
function Meta({
  icon,
  children
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      fontSize: 15,
      color: 'var(--ink-2)',
      lineHeight: 1.45
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: icon,
    size: 16,
    style: {
      color: 'var(--ink-3)'
    }
  }), /*#__PURE__*/React.createElement("span", null, children));
}
Object.assign(window, {
  DS,
  IMG,
  C_OF,
  HOME,
  TopBar,
  PageHead,
  Section,
  ListRow,
  Card,
  Meta,
  Button,
  IconButton,
  Icon,
  Avatar,
  CBadge,
  PulseDock,
  LensBar,
  Chip,
  Tabs,
  EmptyState,
  PostCard,
  MediaBlock,
  ActButton,
  TierBadge,
  Input,
  Select,
  Dialog,
  Toast,
  Switch,
  Checkbox
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/dna/shell.jsx", error: String((e && e.message) || e) }); }

__ds_ns.Avatar = __ds_scope.Avatar;

__ds_ns.Button = __ds_scope.Button;

__ds_ns.Checkbox = __ds_scope.Checkbox;

__ds_ns.Chip = __ds_scope.Chip;

__ds_ns.Dialog = __ds_scope.Dialog;

__ds_ns.Icon = __ds_scope.Icon;

__ds_ns.IconButton = __ds_scope.IconButton;

__ds_ns.Input = __ds_scope.Input;

__ds_ns.Segment = __ds_scope.Segment;

__ds_ns.Select = __ds_scope.Select;

__ds_ns.Sheet = __ds_scope.Sheet;

__ds_ns.Switch = __ds_scope.Switch;

__ds_ns.Tabs = __ds_scope.Tabs;

__ds_ns.Toast = __ds_scope.Toast;

__ds_ns.Tooltip = __ds_scope.Tooltip;

__ds_ns.ActButton = __ds_scope.ActButton;

__ds_ns.AppHeader = __ds_scope.AppHeader;

__ds_ns.AudienceSelect = __ds_scope.AudienceSelect;

__ds_ns.AUDIENCE_LABEL = __ds_scope.AUDIENCE_LABEL;

__ds_ns.CBadge = __ds_scope.CBadge;

__ds_ns.VERB_SCHEMA = __ds_scope.VERB_SCHEMA;

__ds_ns.Composer = __ds_scope.Composer;

__ds_ns.DiaLine = __ds_scope.DiaLine;

__ds_ns.EmptyState = __ds_scope.EmptyState;

__ds_ns.FacetRail = __ds_scope.FacetRail;

__ds_ns.LensBar = __ds_scope.LensBar;

__ds_ns.MediaBlock = __ds_scope.MediaBlock;

__ds_ns.NotificationBell = __ds_scope.NotificationBell;

__ds_ns.KIND_C = __ds_scope.KIND_C;

__ds_ns.NotificationListItem = __ds_scope.NotificationListItem;

__ds_ns.Pane = __ds_scope.Pane;

__ds_ns.PostCard = __ds_scope.PostCard;

__ds_ns.PulseDock = __ds_scope.PulseDock;

__ds_ns.RailWidget = __ds_scope.RailWidget;

__ds_ns.TierBadge = __ds_scope.TierBadge;

__ds_ns.VERB_ACT = __ds_scope.VERB_ACT;

__ds_ns.VerbChip = __ds_scope.VerbChip;

__ds_ns.C_LABEL = __ds_scope.C_LABEL;

__ds_ns.C_GLYPH = __ds_scope.C_GLYPH;

__ds_ns.C_ORDER = __ds_scope.C_ORDER;

})();
